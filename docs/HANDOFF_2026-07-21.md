# archeaxis-workspace 交接（2026-07-21）

## 交接状态

- 睡眠模式：已按用户要求停止；本机调度状态不作为跨机器交付事实。
- 本文只记录机器无关的能力和恢复顺序，不固定分支、HEAD、远端关系或本地 dirty 文件清单。
- 恢复时必须以实时 `git status`、当前分支的 upstream、远端 SHA 和该 SHA 的 CI 为准；候选树未 commit/push 前不得称为云端已交付。
- 家里和公司电脑是同一云端任务状态的可替换终端，不按机器拆分任务或长期分支。

## 当前候选基线

1. Workspace 已有打包后的本地浏览器入口、loopback-only 写入边界、安全响应头，以及网页/文件导入入口。
2. 普通网页、GitHub URL 和本地文件可形成持久候选研究包，并可从独立数据库连接严格读回。
3. Learning 审批收据与卡片投影同事务；相同语义重放幂等，冲突重放完整回滚。
4. Runtime 只读取已批准的 Machine Knowledge；审批与弃用使用 migration-owned append-only 收据，终态与冲突 fail closed。
5. 真实媒体基础链已验证：MP4 经 FFmpeg 提取 16 kHz mono PCM WAV、读取真实 WAV 元数据、抽取非空关键帧 PNG 并核验尺寸。
6. `python-multipart` 已同步到项目依赖、兼容清单、锁文件和最小 CI 安装面；发布证据必须读取最终 exact SHA 的 GitHub Actions，不能沿用本文日期对应的旧运行。
7. Workspace 总览只展示数据库聚合和 packaged unreleased Release Manifest；静态运营数字、伪服务状态和原型 Job Center 已从普通用户入口移除。

## OCR 当前状态

`shared/media_extractor.py` 与 `tests/test_media_extractor.py` 已纳入本轮候选树。Pillow/pytesseract 为显式依赖，项目本地官方 `eng.traineddata` 已完成真实图像识别，CI 安装并验证 Tesseract English data 与 DejaVu 字体，缺失能力时 fail closed。语言数据和下载缓存保持 Git 忽略，不提交机器绝对路径。

后续仍需把 OCR/ASR 绑定到媒体时间戳、内容匹配 Evidence 和人工真值准确率基准。

## 后续任务排序

### A0：发布门禁与媒体证据

继续 JS 语法、真实浏览器、Windows、fresh-wheel静态资源门禁，并推进 ASR、时间戳与内容匹配证据。FFmpeg 解码或基础图像 OCR 成功不代表 OCR/ASR 准确率达标；准确率仍须人工真值样本。

### A1：Workspace异步编排

本轮已交付正式 `workspace.sqlite` owner、apply/status/rollback、exact live-schema validation、connection-scoped Research writer、Research graph/Job/Outbox 同事务、第二连接严格 Job readback、同步命令终态、并发冲突与故障回滚。剩余真实工作是 Outbox dispatcher、SSE 审计时间线和不向普通用户暴露内部 ID 的前端 Job Center。

每个数据库切片必须执行完整 Root/Knowledge Base/Integration/Ruff/Architecture 门禁，并独立核验 exact-SHA CI；禁止运行时 DDL或修改已应用 migration baseline。

### 后续路线

依次推进 ProjectPack Runtime、项目级 Kimi adapter、Diagnostics、合同/文档漂移、VisualArtifact/Palace 和 Windows 产品化。普通用户界面继续使用“元枢系统 / ArcheAxis OS”和“元枢·观心 / ArcheAxis Cognitive Workspace”，不得暴露内部 command/package/outbox 等开发标识。

## 恢复前检查

```text
1. 读取本文件和云端最新分支/提交状态；
2. 核对 git status、HEAD 与远端，dirty 时不得自动 stash/reset/clean；
3. 执行锁定依赖同步、工具 doctor 和 SQLite migration readiness；
4. 多智能体可围绕同一任务并行审查/测试，最终由主写者整合并推送同一云端进度；
5. 禁止接触 E:、个人 Vault 和 Obsidian-Assistance；
6. 从文档记录的真实剩余任务继续，不重复实现已交付的 Workspace/OCR 边界。
```

`.hermes/` 中的睡眠模式账本、日志和运行时证据保持本地并受 Git 忽略，不属于仓库交付内容。
