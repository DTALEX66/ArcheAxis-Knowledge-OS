# Cognitive-Loop-OS 交接（2026-07-21）

## 交接状态

- 分支：`feat/cognitive-workspace-mvp`
- 交接前已推送 HEAD：`2bf557d408fd612d7401a59aa0dff9d38d19dd87`
- 睡眠模式：已按用户要求停止；Hermes cron `d3a3a63032f3` 已暂停。
- 远端关系：交接前本地 HEAD 与 `origin/feat/cognitive-workspace-mvp` 一致。
- 本地现场：保留两个归属于媒体 OCR 的未提交文件，不得 reset、clean、restore 或误并入文档提交。

## 本轮已推送能力

1. Workspace 已有打包后的本地浏览器入口、loopback-only 写入边界、安全响应头，以及网页/文件导入入口。
2. 普通网页、GitHub URL 和本地文件可形成持久候选研究包，并可从独立数据库连接严格读回。
3. Learning 审批收据与卡片投影同事务；相同语义重放幂等，冲突重放完整回滚。
4. Runtime 只读取已批准的 Machine Knowledge；审批与弃用使用 migration-owned append-only 收据，终态与冲突 fail closed。
5. 真实媒体基础链已验证：MP4 经 FFmpeg 提取 16 kHz mono PCM WAV、读取真实 WAV 元数据、抽取非空关键帧 PNG 并核验尺寸。
6. `python-multipart` 已同步到项目依赖、兼容清单、锁文件和最小 CI 安装面；最新已提交 HEAD 的 GitHub Actions 成功。

相关提交从 `90ebf7e` 至 `2bf557d`，Git 历史与对应 GitHub Actions 记录是执行证据。

## 必须保留的 OCR 现场

未提交路径：

```text
shared/media_extractor.py
tests/test_media_extractor.py
```

当前 WIP 新增本地 Tesseract OCR 适配器及真实生成图像测试。生产调用已到达 Tesseract，但本机安装没有任何语言模型：

```text
tesseract --list-langs
0 available languages
```

因此当前定向测试结果是 `2 passed, 1 failed`；失败发生在 OCR 返回错误对象后测试读取 `engine`。这不是可上传的 GREEN，不得通过跳过测试或伪造 OCR 文本收口。

恢复顺序：

1. 在项目受控边界中提供并核验所需 Tesseract 语言数据，至少包含 `eng.traineddata`；不要把下载缓存或机器专属绝对路径提交到仓库。
2. 重新运行 `tests/test_media_extractor.py`，确认真实生成图像能得到预期文字。
3. 检查错误语义、超时、approved-root containment 和语言参数白名单。
4. 运行 changed-file Ruff 与 `git diff --check`；只有 GREEN 后才提交上述两个文件。
5. 后续将 OCR 文字绑定到视频帧时间戳和 Evidence，而不是把随机帧视为内容证据。

## 后续任务排序

### P0：媒体闭环

继续真实 OCR/ASR、时间戳与内容匹配证据。FFmpeg 解码成功不代表 OCR/ASR 准确率达标；准确率仍须人工真值样本。

### P0：Workspace Job / Outbox

这项工作缺少的是待实现组件，而不是外部阻塞。不要再次仅因 migration owner 尚不存在就把任务判为 blocked。按独立高风险纵向切片推进：

1. 为 `workspace.sqlite` 建立正式 migration owner、apply/status/rollback 与 Schema 合同测试；
2. 增加 connection-scoped Research graph writer；
3. 在一个 SQLite 事务内写入研究图、Job 和 Outbox；
4. 用第二连接严格读回并验证 command 冲突与回滚；
5. 再接 Job 状态、审计时间线、SSE 和前端 Job Center。

每个数据库切片必须执行完整 Root/Knowledge Base/Integration/Ruff/Architecture 门禁，并独立核验 exact-SHA CI；禁止运行时 DDL或修改已应用 migration baseline。

### 后续路线

依次推进 ProjectPack Runtime、项目级 Kimi adapter、Diagnostics、合同/文档漂移、VisualArtifact/Palace 和 Windows 产品化。普通用户界面继续使用“元枢系统 / ArcheAxis OS”和“元枢·观心 / ArcheAxis Cognitive Workspace”，不得暴露内部 command/package/outbox 等开发标识。

## 恢复前检查

```text
1. 确认没有活跃 Hermes cron writer；
2. 读取本文件及项目内 .hermes/sleep-mode 状态；
3. 核对 git status、HEAD 与远端；
4. 将两个 OCR dirty path 归属给唯一 writer；
5. 禁止接触 E:、个人 Vault 和 Obsidian-Assistance；
6. 从 OCR 真实依赖恢复或选择一个工作区干净的独立 checkout 处理数据库高风险切片。
```

`.hermes/` 中的睡眠模式账本、日志和运行时证据保持本地并受 Git 忽略，不属于仓库交付内容。
