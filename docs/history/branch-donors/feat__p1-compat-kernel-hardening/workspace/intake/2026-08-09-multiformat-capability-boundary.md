# 2026-08-09 多格式能力边界与真实资料验证

## 决策

- 复用 MarkItDown 的 PDF、DOCX、PPTX、XLSX/XLS、CSV 转换能力，并把对应 extras 固化到项目依赖与锁文件。
- JSON Canvas 纳入格式识别与可恢复转换，但当前状态明确为 `degraded`：保留原文，节点、连线和几何语义尚未完成索引。
- 图片与音视频保持诚实状态：图片为 `metadata_only`，音视频为 `metadata_only`；OCR、ASR 和高阶视频知识呈现仍未实现。
- `/version` 暴露逐格式 capability 状态，避免把“依赖已安装”误报成结构锚点、索引、展示和重启读回都已完成。

## 真实资料范围

- 纳入：`10_课程库`、`20_知识原子库` 的 Markdown；Canvas、TXT、CSV 做独立格式验证。
- `30_复习系统` 仅用于复习结构测试。
- 排除：CFG、系统/隐藏目录、附件/媒体、模板、工程目录和旧库迁移目录。
- 外部 Vault 只读；转换产物与 JSONL manifest 均写入项目 `.hermes/task-artifacts/`。

## 证据

- 根测试：`1102 passed, 3 skipped`。
- 知识库与集成测试：`73 passed`。
- 当前工作树真实资料首次转换：`2466` 个成功，`1` 个无扩展名文件进入 `needs_review`。
- 重启续跑：已恢复既有转换记录，并保留需复核项。
- 应用代码与本轮修改测试的 Ruff 检查通过。

## 未完成/边界

- 未发布安装包是 `0.5.0`，仓库 Tauri/验证脚本仍是 `0.4.5`；当前不能把该安装包报告为已验收。
- 0.5.0 payload 已在项目隔离目录解包并用 bundled Python 3.11.15 启动：迁移、stdio-v1 readiness、HTTP `/version`、`/workspace`、停止和端口释放均通过；同一包的学习资料转换与续跑也通过。
- NSIS `/S` 在本机退出码为 0 但没有落地文件或卸载项；可见安装器能打开，但当前桌面自动化会话无法可靠触发按钮，因此完整 NSIS 安装/启动/卸载生命周期仍未验收。
- 0.5.0 包的 `/version` 没有当前工作树新增的逐格式 capability 字段，且包内 manifest 仍把音视频关键帧标为 `available`；这证明包与当前修复工作树不是同一可发布身份。
- 安装器若真实运行会写入当前用户 `%LOCALAPPDATA%` 与 HKCU；现有应用数据目录已存在，本轮未覆盖它。安装包未签名，缺少与当前源码对应的可信构建身份和版本匹配证据。
