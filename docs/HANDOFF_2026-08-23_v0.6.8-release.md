# ArcheAxis Knowledge v0.6.8 发布交接

日期：2026-08-23（Asia/Shanghai）

## 权威发布坐标

- Release：<https://github.com/DTALEX66/ArcheAxis-Knowledge-OS/releases/tag/v0.6.8>
- 发布标签提交：`93e58a3b2c537dd348903dd2296933e0cfb5a503`
- 源码树：`545eaa7ef62bab9e92e55a9ef598012bb368680a`
- PR：[#140](https://github.com/DTALEX66/ArcheAxis-Knowledge-OS/pull/140)
- PR head CI：`32606263256`，success
- main exact-SHA CI：`32607097436`，success
- Release run：`32607789507`，success
- 发布状态：public、non-draft、non-prerelease
- 公开资产：9 项；provider digest、独立下载 SHA-256、checksum allowlist 和
  schema v3 identity 均 PASS

机器可读权威收据：
`reports/release/v0.6.8/release-evidence.json`。

## 恢复与继续

1. 发布事实只认 `v0.6.8`、发布提交和上述两个 main/Release 运行 ID；不要以
   分支名、文档文字或本地测试替代 exact-SHA 与公开资产证据。
2. 源码中的 `unreleased / public=false` 是构建前占位合同；公开资产中的
   `stable / public=true` schema v3 identity 才是发布身份。
3. 继续开发时从最新 `origin/main` 新建分支；`v0.6.0` 至 `v0.6.8` 均不可改写。
4. 本次被明确保留的原工作区用户改动为
   `reports/current/CANDIDATE_ROUNDTRIP_PROOF.json` 的工作树状态和
   `tests/fixtures/readability_article.html` 的内容/换行差异；不得 reset、restore
   或随发布提交上传。
5. 外置 Rust/MSVC/Python/Playwright 工具链位于
   `D:\All projects\OS External Configuration`，仅用于本机构建，不属于发布资产。

## 后续优先级

1. 去除 Release 中重复 NSIS 全量构建，同时保持正式 identity、三包生命周期、
   checksum/SBOM 和公开下载读回不降级。
2. 把已通过的六空间真实 Chromium 联调固化成 CI 可重复的 Tauri-origin browser
   receipt，而不再只依赖人工驱动记录。
3. 完成 Recovery 日志/安全模式和整机 Golden Journey；随后补齐 Tier A、可访问性
   与四类账本静态/迁移矩阵。

## 清理边界

本轮缓存、浏览器数据、临时 Python/Rust/打包产物和资产下载均位于项目忽略的
`.hermes/`。交付结束时将删除可再生成缓存和临时 worktree；发布读回摘要已经
固化到仓库收据，用户原有未提交文件保持不动。
