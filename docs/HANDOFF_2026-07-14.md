# Cognitive-Loop-OS 项目交接

> 更新时间：2026-07-14
>
> 仓库：<https://github.com/DTALEX66/Cognitive-Loop-OS>
>
> 默认分支：`main`
>
> 功能与治理基线：`46076da8942ee7cc0846b4f2d4c5c5af8dfa0a49`
>
> 本次交接内容覆盖锚点：`e431f5a5489e91fc016955dd1429f13e811c456b`

## 1. 交接结论

Cognitive-Loop-OS 当前是一个本地优先、证据约束的认知与知识运行时。项目保持单仓库、模块化单体、单 FastAPI 网关和统一 SQLite 运行时数据根目录。

截至本次交接：

- Phase 0 仓库基线已建立；
- Knowledge Base 已迁为可安装的 `knowledge_base` 包并挂载到主网关；
- 生产鉴权、CORS、运行时路径和恢复候选流程已加固；
- TP1.0 命名、编码、Git index/HEAD 门禁已交付；
- README 与包 metadata 已更新为当前项目定位；
- 本地 `HEAD` 与 `origin/main` 一致，功能基线 CI 全绿；
- 下一项开发主线是 **Phase 1.1 Runtime Facade tracer bullet**。

本交接文档更新提交不在文内硬编码自身 SHA；上传后的准确提交和 CI 以 `git log -1` 与 GitHub Actions 为准，避免文档自引用失效。

## 2. 当前真实架构

```text
Client / Agent / Cron
        │
        ▼
app.main :8000
├── Core Runtime
│   ├── ingestion / routing / retrieval
│   ├── permission / registered tools / trace
│   ├── binary evaluation / candidate lesson
│   └── sleep-loop
└── /kb mounted application
    ├── stable composite / quality / projection routers
    ├── legacy domain routes in api.py
    └── search / cards / reviews / graph / dashboard
        │
        ▼
SQLite + FTS5 + sqlite-vec + NetworkX + local artifacts
```

当前主要目录：

| 路径 | 职责 |
| --- | --- |
| `app/` | 核心认知运行时、摄入、工具和工作流 |
| `knowledge_base/` | 可安装的知识、检索、学习和领域 API 包 |
| `Inspiration-Research/` | 研究发现与候选项目雷达；保留连字符兼容目录 |
| `shared/` | SQLite、配置、鉴权、证据、图谱和可复用管道 |
| `shared-contracts/` | Schema、fixture、适配器和项目注册 |
| `config/` | 运行时策略与 canonical naming registry |
| `scripts/` | 仓库门禁、日常任务与 worker |
| `migrations/reports/phase-0/` | Phase 0 历史基线与 Phase 1 输入 |

当前代码事实详见 [`architecture/CURRENT_ARCHITECTURE.md`](architecture/CURRENT_ARCHITECTURE.md)。

## 3. 已完成能力

### 3.1 运行时与知识入口

- `POST /run`：route → retrieve → fixed-step compile → permission → registered tool execution → trace → binary evaluation → memory。
- `POST /kb/pipeline`：摄入、提取、标签、摘要、候选事实与索引；不自动证明事实正确。
- `POST /kb/search`：关键词、向量或混合检索。
- `POST /kb/quality`：准确率和处理总账汇总；调用者提供的 evidence 仍为 candidate。
- `GET/POST /sleep-loop?action=...`：有证据约束的无人值守任务循环。
- `GET /health`：实时递归统计 HTTP operation，不依赖手写路由数字。

### 3.2 质量与证据

- 文件级 JSONL 处理总账、SHA-256、指纹恢复和失败重试；
- 原子 Markdown 输出与可恢复多格式目录摄入；
- 人工真值 CER/WER；没有真值时明确返回 `unverified`；
- 文本命中证据、内容质量静态审计和候选开放来源建议；
- caller-supplied evidence 最高只能成为 candidate，不能自动升级为可信事实。

### 3.3 安全与部署

- Core、Knowledge Base 和 Inspiration Research 使用统一鉴权边界；
- 生产模式拒绝关闭鉴权、弱密钥、非法 key 文件和通配 CORS；
- restore 只创建并校验离线候选，不在线覆盖活动数据库；
- n8n、Airflow、LiteLLM、Crawler 等集成不返回 stub 假成功；
- wheel 可在源码目录外安装并启动 Core/KB 路由；
- 运行时数据库、日志、用户知识、模型和缓存不得进入 Git。

## 4. TP1.0 命名与编码治理

### 4.1 Canonical IDs

唯一机器真相是 `config/naming-registry.yaml`：

| 服务 | Canonical ID | Python package | API prefix |
| --- | --- | --- | --- |
| Core Runtime | `core` | `app` | `/` |
| Knowledge Base | `knowledge-base` | `knowledge_base` | `/kb` |
| Inspiration Research | `inspiration-research` | `inspiration_research`；兼容目录 `Inspiration-Research` | `/` |

机器 ID 使用 ASCII；中文和英文展示名只用于 UI、文档和翻译值。历史名称只能作为显式 deprecated alias 保留。

### 4.2 仓库门禁

- `.editorconfig`：编辑器编码与换行合同；
- `.gitattributes`：Git blob/checkout 换行合同；
- `.pre-commit-config.yaml`：提交前扫描 staged index；
- `scripts/check_repository_conventions.py`：统一扫描 worktree、index 或 HEAD；
- `.github/workflows/ci.yml`：CI 扫描 Git HEAD；
- `shared/naming.py`：运行时读取 registry 的共享实现。

普通文本使用 UTF-8、NFC、LF、无 BOM、无尾随空白并以换行结尾；`.bat`、`.cmd`、`.ps1` 使用 CRLF。详细规则见 [`NAMING_ENCODING_CONVENTIONS.md`](NAMING_ENCODING_CONVENTIONS.md)。

## 5. 验证基线与远端健康核对

功能与治理基线及其对应 CI：

```text
baseline    46076da8942ee7cc0846b4f2d4c5c5af8dfa0a49
CI run      29323025774
CI URL      https://github.com/DTALEX66/Cognitive-Loop-OS/actions/runs/29323025774
conclusion  success
```

该 run 已通过：

- repository convention 与 Ruff；
- wheel build/install/smoke；
- Python 3.10、3.11、3.12 的 Root、Knowledge Base 和 integration tests。

`46076da` 是功能与治理验证锚点，不代表读取本文时的实时 `HEAD`。交接文档和 README 的后续修订会使分支 SHA 前进，因此本文不把可自我失效的“当前 HEAD”写成固定值。接手时必须直接核对云端：

```bash
git fetch --prune origin
git rev-parse HEAD
git rev-parse origin/main
git rev-list --left-right --count HEAD...origin/main
git status --short
gh run list --branch main --limit 5
```

只有 `HEAD == origin/main`、ahead/behind 为 `0/0`、工作树干净且该 HEAD 对应 CI 成功，才可宣称实时同步完成。

GitHub Actions 当前有一项非阻断提示：`actions/checkout@v4` 与 `actions/setup-python@v5` 的 Node.js 20 兼容层将废弃，GitHub runner 已强制使用 Node.js 24。不要把该 warning 误报为当前 CI 失败，但后续应在依赖维护任务中升级 action major version。

## 6. 启动与验证

### 6.1 开发环境

```bash
python -m pip install -e ".[dev]"
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

访问：

- Core API：<http://127.0.0.1:8000/docs>
- Knowledge Dashboard：<http://127.0.0.1:8000/kb>
- Knowledge API：<http://127.0.0.1:8000/kb/docs>
- Health：<http://127.0.0.1:8000/health>

Windows 当前工作机应使用项目解释器，例如：

```bash
./.venv/Scripts/python.exe -m pytest tests/test_naming_conventions.py -q --tb=short
```

不要误用 Hermes 自身的全局 venv。开发时应将示例替换为本次改动对应的真实定向测试。

### 6.2 最小验证策略

唯一流程记录是 [`VERIFICATION_POLICY.md`](VERIFICATION_POLICY.md)：

1. 开发中只运行受影响的定向测试；
2. diff 冻结后按变更类型执行一次必要完整本地门禁；
3. 失败后修根因，只重跑受影响门禁；
4. Python 版本矩阵交给 GitHub CI；
5. 独立 reviewer 仅用于安全、权限、数据库迁移、架构移动和高风险外部写入；
6. 推送后只验收新提交对应的一次 GitHub Actions run。

常用仓库门禁：

```bash
python scripts/check_repository_conventions.py --source worktree
python scripts/check_repository_conventions.py --source index
pre-commit run --all-files
git diff --check
git diff --cached --check
```

## 7. 强制边界

- 唯一开发目标是本仓库 `Cognitive-Loop-OS`；
- 不再扫描、测试、修改或同步外部 `Obsidian-Assistance`；
- 不访问个人 Vault、E 盘或其他项目目录；
- 外部资料与 AI 输出默认进入 quarantine/candidate；
- stub、echo、preview、dry-run、文件存在和置信度不得冒充真实完成；
- 数据库迁移、架构移动、安全修改和依赖大升级必须拆成独立可回滚提交；
- 临时数据只能写入本仓库 `.tmp/` 或明确忽略的测试/构建目录，并在结束前删除；
- 不提交密钥、活动数据库、缓存、用户知识、模型或生成物。

## 8. 当前真实限制

1. Planner 仍以固定 echo 步骤为主，不是动态规划器。
2. Evaluator 仍主要是二值评分，缺少多维 Evaluation 和可信 Lesson 反馈闭环。
3. `knowledge_base/api.py` 仍包含遗留领域路由，旧细粒度 API 尚未收口。
4. `Inspiration-Research` 仍保留连字符目录兼容逻辑。
5. Mypy 尚未成为零错误门禁。
6. OCR/ASR 准确率必须依赖人工标注金标准。
7. 生产容器、反向代理和并发负载尚未完成实机验证。
8. `migrations/reports/phase-0/PHASE_1_TASKPACK.md` 仍含旧 TP1.0 编号和旧“每任务全量门禁”规则，开始 Phase 1.1 前必须与路线图、验证策略同步；不要直接照旧段落执行。

## 9. 下一阶段任务

### P0：规划记录收口

1. 将 `PHASE_1_TASKPACK.md` 中旧 TP1.0 定义标记为历史基线；
2. 明确已完成的 TP1.0 是命名与编码治理；
3. 删除重复门禁，统一引用 `VERIFICATION_POLICY.md`；
4. 不重新生成整套 Phase 0 审计。

### P1：Runtime Facade tracer bullet

1. 先写失败合同测试，覆盖 `route → permission → execute → trace`；
2. 包装现有 `app/core` 与 `app/agent`，不复制业务实现；
3. 使用真实注册但无副作用的测试工具；
4. 对比 Facade 与旧入口的标准对象结果；
5. 保留旧入口和回滚路径。

### P2：Architecture Guard

1. 生成当前仓库内部导入方向的机器可读基线；
2. 用故意违规 fixture 证明 guard 会失败；
3. 阻止新增 `sys.path.insert`；
4. 阻止 Contracts/Platform 反向依赖业务模块；
5. 阻止运行时代码硬编码外部项目、个人目录或数据盘路径；
6. 只 grandfather 精确的现有兼容点，新增违规立即失败；
7. 将 guard 接入 CI。

### P3：其余 Facade

依次完成 Knowledge、Research、Enhancement、Contracts Facade，每个 Facade 至少有一个调用真实实现的合同测试。Phase 1 只建立可运行边界，不进行数据库迁移、目录树搬迁或 Planner/Evaluator 重写。

Phase 1 完成后再进入版本化 Contracts 和安全/数据正确性 P0。完整顺序见 [`EXECUTION_ROADMAP.md`](EXECUTION_ROADMAP.md)。

## 10. 推荐接手顺序

```text
1. git fetch --prune origin
2. 核对 HEAD、origin/main 和工作树
3. 阅读 PROJECT_STATUS、CURRENT_ARCHITECTURE、EXECUTION_ROADMAP
4. 同步 PHASE_1_TASKPACK 的旧编号与验证规则
5. 写 Runtime Facade RED 合同测试
6. 实现最小 Runtime Facade
7. 加 Architecture Guard
8. 小步提交、推送并核对对应 CI
```

## 11. 回滚锚点

```text
46076da  docs: refresh project description and verification guide
ec55df8  feat(governance): enforce naming and verification contracts
cd665c6  chore(repo): normalize tracked text encoding
42a615a  docs(phase0): refresh ephemeral baseline evidence
469c39d  fix(phase0): use ephemeral baseline runtime
```

- 描述文件可独立回滚 `46076da`；
- 命名治理可回滚 `ec55df8`，但应同时评估 pre-commit、CI、registry 和 scanner；
- 机械归一化可独立回滚 `cd665c6`，但回滚后 HEAD convention gate 会重新暴露历史格式债务；
- Facade 尚未切换生产入口，因此后续 Phase 1 应继续保留旧入口作为回滚路径。

## 12. 接手完成条件

接手人开始下一 TaskPack 前应确认：

- `HEAD == origin/main`；
- 工作树无未知修改；
- 没有活动数据库、缓存或构建产物进入 Git；
- 当前修改只在本仓库允许范围内；
- 使用项目 venv，而不是 Hermes 全局 venv；
- 目标测试先 RED 后 GREEN；
- 提交可单独回滚；
- 推送后只验收该提交对应的一次 CI。
