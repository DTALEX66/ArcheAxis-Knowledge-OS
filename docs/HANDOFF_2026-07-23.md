# Cognitive-Loop-OS 交接（2026-07-23）

## 云端交付基线

- 权威分支：`main`。
- 本次交接记录创建时，远端 `main` 已包含：
  - `b59bf9b feat(desktop): ship verified Windows workspace shell (#6)`
  - `604a412 feat(runtime): govern evaluation and durable sleep execution (#7)`
  - `551dae6 docs(desktop): record portable shell acceptance gaps`
- 恢复工作前必须重新执行 `git status --short --branch`、读取远端 SHA 与该 SHA 的 CI；本文不替代 exact-SHA CI。
- 家里和公司电脑是同一云端任务状态的可替换终端；不按机器拆分长期分支、租约或产品事实。

## 已交付的真实能力

### 产品外壳

1. Workspace 提供真实网页、GitHub URL 与本地文件导入；状态、诊断和只读同步 Job Center 为真实本地持久化投影。
2. 「察微研究」已成为实体页面：显示待人工审核的本地 Research，并通过无内部 ID 的来源地址与人工理由执行批准动作。
3. 普通 Workspace UI 不显示内部 `package_id`、`unit_id`、`artifact_id`、`command_id` 或 outbox 标识。
4. Windows Tauri desktop shell 已交付。它使用 bundled Python 启动本地 loopback backend，并以 WebView2 打开 `/workspace`；CI 已构建、安装、启动、关闭、强制清理和卸载验证 NSIS installer。

### 运行时与治理

1. Runtime evaluation、receipt、lease-fenced durable Sleep Loop 已进入 canonical runtime composition；重试、心跳、attempt receipt 和 review gate 均有合同与测试。
2. Workspace Outbox dispatcher 与 Research consumer 已接入受治理投影；Langfuse adapter 采用 payload-safe 边界。
3. Research 继续保持 candidate-only：不得把单一来源、导入文本或模型输出自动提升为 verified truth。
4. 开源能力吸收使用 ledger 区分已实现、adapter contract、reference-only 与 deferred review；高风险项目不自动安装、下载、导入或执行。

## 已验证证据

本轮合并前的本地门禁：

```text
pytest -q: 662 passed, 2 skipped
A0 Chromium browser smoke: passed
Rust desktop library tests: 8 passed
ruff: passed
git diff --check: passed
```

最新产品合并候选的 GitHub CI 已完成 `test` 三版本、lint、wheel smoke、browser smoke、Windows runtime smoke、desktop shell 与 `a0-gates`。后续变更必须重新以自己的 exact SHA 为准。

## 当前未完成项 / 真相边界

### P0：strict portable data mode

当前 release executable 可与 bundled runtime 一起复制运行，但其运行数据和 WebView2 profile 默认使用 Tauri per-user local app-data。它不是“所有数据跟随程序目录”的严格 portable release。

- 详细验收证据与完成条件：[`workspace/intake/2026-07-23-desktop-shell-portable-acceptance.md`](../workspace/intake/2026-07-23-desktop-shell-portable-acceptance.md)。
- 下一切片必须增加显式 portable data-root 合同，默认安装与 portable copy 分别测试，且 portable 模式禁止回退到 user profile。

### P1：桌面产品说明

`desktop/README.md` 尚未提供普通用户的安装、启动、数据目录、卸载与 portable 边界说明。后续文档不得暴露开发侧的 command/package/artifact 名称。

### P1：Workspace 的完整用户闭环

Research 审核已接入，但 Knowledge、Learning、Machine Knowledge 等导航仍缺少普通用户可用的投影与动作契约。桌面壳可启动不等于产品闭环、公开 Alpha 或公开 release。

### 后续产品能力

- ASR、媒体时间戳、内容匹配 Evidence 与人工真值准确率尚未闭环。
- 异步 worker、SSE 审计时间线和更完整的普通用户 Job/生命周期中心仍待推进。
- 仍禁止接触 E:、个人 Vault 与 `Obsidian-Assistance`；不把本机 `.hermes/` runtime、日志、数据库、桌面 build/installer 或缓存提交到 Git。

## 恢复顺序

1. 切到 `main` 并执行 `git pull --ff-only`；确认干净工作树。
2. 阅读本文件、最新 `workspace/intake/` 记录和 `docs/PROJECT_STATUS.md`；不要凭历史手工总结推断当前事实。
3. 为任何隔离验证设置项目内 `COGNITIVE_DATA_DIR`，先执行 `python -m app.runtime_entrypoint migrate`，再启动服务或测试。
4. 桌面开发使用仓库 `.venv`；release 验证必须确认使用 bundled Python，而非 PATH Python。
5. 修改前先读相关实现和测试；修改后运行最小定向门禁，冻结 diff 后跑必要完整门禁，推送后只认新 exact-SHA CI。
6. 不自动删除 `.hermes/`、虚拟环境、runtime data、日志或 build 输出；只有在确认可再生成且用户授权时才清理。
