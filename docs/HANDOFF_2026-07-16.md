# Cognitive-Loop-OS 正式交接（2026-07-16）

> 本文是当前接手入口。`HANDOFF_2026-07-14.md` 已降级为历史快照。
> 实时 Git 状态和 CI 必须用本文命令核对；本文不硬编码自身发布 SHA，避免自引用失效。

## 1. 当前结论

- Phase 0 基线、Phase 1 Facade/Architecture Guard、Phase 2 首批版本化 Contracts 已完成。
- 当前处于 **Phase 3：安全和数据正确性 P0**。
- Phase 3 已完成：
  - 移除 runtime 源码内置管理员 Key；
  - 阻止 Token 请求者通过请求字段自行选择管理员角色；
  - 主网关 Rate Limiter、身份分桶、proxy trust、早期拒绝预算与真实 Uvicorn 启动入口加固。
- Phase 3 当前刀：**Stable persistent hash 已完成；下一项为可重建、可回滚的 FTS/Vector migration 边界**。
- Phase 7 已有 `read file: <repo-relative-path>` 的 Planner → Permission → real Evidence → Evaluation → Lesson 纵向 tracer，但不等于通用 Dynamic Planner。
- Phase 8 已有 Sleep ledger task → `TaskPackV1` fail-closed Adapter，但 Sleep Loop 尚未统一委托 Runtime execution port。
- Phase 9 五条 Minimum Complete System Alpha 闭环尚未完成。

## 2. 已发布证据锚点

| 能力 | Commit | GitHub Actions |
| --- | --- | --- |
| Phase 2 Contracts Release Train | `40f97ce494dbf8d1e79fe80a55b1be1e0d4b529e` | [29432438911](https://github.com/DTALEX66/Cognitive-Loop-OS/actions/runs/29432438911) success |
| 移除内置管理员凭据 | `5165938aa73ef5d2c4190a8d4552afe00b564a51` | [29433144432](https://github.com/DTALEX66/Cognitive-Loop-OS/actions/runs/29433144432) success |
| 主网关 Rate Limiter | `af6dc2672d66e5c7ed5e8de67f32b3e3ea3a47d2` | [29436639815](https://github.com/DTALEX66/Cognitive-Loop-OS/actions/runs/29436639815) success |

Rate Limiter 发布候选的 exact frozen tree 是 `5d8ac06b23aae10c0c755634c7e3a7d177021b08`，独立 superseding-tree reviewer verdict 为 `GO`。该候选发布前验证为 Root 347、Knowledge Base 28、Integration 1，完整 Ruff、Architecture Guard、convention、diff 与 staged secret scan 通过。

## 3. 当前本地 Release Train 内容

本交接候选包含 `scripts/run_taskpack_agent.py` 的终局复审修复：

- `max_review_rounds=N` 表示最多允许 N 次 NO-GO 修复；
- 第 N 次修复后必须执行第 N+1 次终局 review；
- 终局 `GO` 才能发布；
- 终局仍 `NO-GO` 时停止，不能再启动越界修复；
- 回归测试覆盖三次 NO-GO 修复后第四次 review `GO`；runner 定向套件 7 passed。

接手时不要根据本段猜测是否已经上传，必须执行第 8 节命令确认 `HEAD == origin/main` 和 exact-SHA CI。

## 4. 当前真实能力边界

### Contracts

Phase 2 已建立并验证：

- `TaskPackV1`、`ExecutionTraceV1`、`EvaluationV1`、`LessonV1`；
- `SourceRecordV1`、`ClaimV1`、`EvidenceV1`、`ResearchPackageV1`；
- `KnowledgeUnitV1`、`RelationV1`；
- `LearningArtifactV1`、`MasterySignalV1`；
- `MachineKnowledgeUnitV1`。

Legacy 可表示字段必须无损往返；未知字段 fail closed；caller-supplied/legacy-unverified 数据不得自动提升为 verified、approved、published 或 reviewed。

### Runtime

当前真实纵向链：

```text
Goal: read file: AGENTS.md
→ Planner: file_read
→ Permission
→ registered tool execution
→ path + content, non-dry-run Evidence
→ execution/status/evidence Evaluation
→ trace-bound Lesson
```

不支持的 Goal 不得 fallback 为 echo 成功。echo、no-op、preview 和 dry-run 不是成功证据。其他 Goal 仍需逐个增加真实意图和工具证据，不得宣称通用 Planner 已完成。

### Authentication and gateway

- runtime 源码不保存默认管理员 Key；开发、测试和生产凭据必须显式提供；
- Token 签发重新认证管理员调用者，普通用户不能通过 `role=admin` 提权；
- Rate Limiter 对普通读、敏感写和 `/auth/token` 使用独立策略；
- API Key/JWT/匿名 peer 使用不可逆身份桶，不保存原始凭据；
- 双凭据请求 fail closed，认证凭据与 bucket 凭据不能分离；
- 所有仓库拥有的 Uvicorn 入口禁用隐式 proxy-header rewriting；
- 未受信代理头和双凭据早期 400 同样消费 pre-auth 预算并最终达到 429；
- limiter 当前是单进程内存实现，不具备多进程/分布式一致性。

## 5. 下一 TaskPack：通用 Migration Runner 与 Phase 3 收口

### 目标

把当前分散的 migration/rebuild owner 统一到一个可注册、可查询、可运维的边界：

- 复用 `shared/migration.py` 已验证的 ledger、backup、manifest、幂等与 offline rollback；
- 注册 TaskPack SQLite migration、Vector shadow candidate 和 FTS shadow candidate owner；
- 提供 operator `status/apply/rollback` 边界，明确 pending、applied、failed 与 rollback provenance；
- owner 名称、版本和目标数据库冲突必须 fail closed；
- Vector/FTS activation 只能发生在 candidate 完整验证后，失败必须保留旧活动索引；
- fresh、partial、legacy、重复运行、并发、WAL 与 Windows replace failure 均需真实动态测试。

### 执行边界

- 这是数据库与架构边界，必须独立 frozen tree、完整本地门禁、exact-tree reviewer、GO-only 发布与 exact-SHA CI；
- 不在同一 TaskPack 扩展 Dynamic Planner、Sleep Loop、UI 或外部写入；
- 不访问 `E:`；
- `Obsidian-Assistance` 严格只读且不得扫描、测试、修改、同步或作为迁移目标；
- 正式 runtime 应用前必须通过 live storage SSOT 再次确认数据库路径，不得只信文档；
- 不打印、转写或提交凭据、数据库、backup、manifest 或 runtime 数据。

## 6. Phase 3 收口顺序

```text
通用 Migration Runner registry
→ operator status/apply/rollback
→ TaskPack + Vector + FTS owner 接入
→ fresh/legacy/partial/WAL/rollback 动态演练
→ Phase 3 安全与数据正确性集成验收
→ Phase 4 Research 真实闭环
```

现有 Vector/FTS shadow rebuild、verify、switch 和 rollback 是可复用资产，但在 owner 接入与 operator provenance 完成前，不宣称通用 migration 边界完成。数据库、迁移和架构移动继续独立审查，不能进入普通低风险批量。

## 7. 验证和发布策略

- 每个新行为：定向 RED → GREEN；
- 低风险同阶段切片：受影响测试、changed-file Ruff、diff/convention、本地 checkpoint；
- 阶段 Release Train：聚合 frozen tree 后只运行一次 Root/KB/Integration、完整 Ruff、Architecture Guard、convention、secret scan 和 exact-SHA CI；
- 安全、权限、数据库、迁移、架构、依赖：不等待阶段末，独立完整门禁和 reviewer；
- reviewer 只授权精确 `git write-tree`，任何编辑、rebase、rebuild 或 amend 都使 verdict 失效；
- review 不自动授权 commit/push。

权威频率规则见 [`VERIFICATION_POLICY.md`](VERIFICATION_POLICY.md)。

## 8. 接手核对命令

```bash
git fetch --prune origin
git rev-parse HEAD
git rev-parse origin/main
git rev-list --left-right --count HEAD...origin/main
git status --short
gh run list --commit "$(git rev-parse HEAD)" --limit 10 \
  --json status,conclusion,name,url,databaseId
```

只有同时满足以下条件才能宣称交接发布完成：

- `HEAD == origin/main`；
- ahead/behind 为 `0/0`；
- 工作区 clean；
- 当前 HEAD 的 GitHub Actions completed/success。

正式本地门禁：

```bash
python -m pytest tests -q --tb=short -p no:cacheprovider
python -m pytest knowledge_base/tests -q --tb=short -p no:cacheprovider
python -m pytest integration-tests -q --tb=short -p no:cacheprovider
python -m ruff check app shared knowledge_base inspiration_research \
  Inspiration-Research shared-contracts/adapters app/workflow \
  integration-tests scripts
python scripts/check_architecture.py
python scripts/check_repository_conventions.py --source index
git diff --check
```

测试必须使用隔离 `COGNITIVE_DATA_DIR`；不得读写或提交真实 runtime SQLite、backup、日志、缓存、coverage、wheel、`build/`、`dist/`、`*.egg-info/` 或 `.hermes/`。

## 9. 回滚与恢复

- Phase 2 contracts Release Train 可从 `40f97ce` 独立定位；Contracts/Adapter 变更不要求删除 legacy 表。
- 管理员凭据变更位于 `5165938`；回滚前必须重新评估凭据默认值风险，禁止恢复源码硬编码 Key。
- Rate Limiter 位于 `af6dc26`；回滚会移除网关限流与 proxy trust 启动约束，必须作为安全降级处理，不能静默操作。
- 数据库 migration 使用 verified backup/manifest 和 offline rollback；禁止 hard reset、force push 或覆盖 Git 历史。
- 本仓库 runtime 数据库 SSOT 为 `data/cognitive_os.sqlite`，但任何正式 migration 前必须再次通过 live storage path 核对，不能只信文档。

## 10. 完成定义

接手完成不等于读完本文。下一 writer 开始 Migration Runner 前应确认：

- 实时 Git/CI 满足第 8 节；
- 没有活动后台 writer 占用主 checkout；
- Migration Runner ownership、允许路径、禁止路径、owner registry 和动态 rollback 测试已写入 TaskPack；
- 高风险 reviewer 可同步等待；
- HERMES 任务栏已将通用 Migration Runner 标为 in progress。
