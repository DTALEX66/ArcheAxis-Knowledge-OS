# CODEX入口

阅读EXECUTOR-START.md。唯一任务权威为同目录TASKS.json，计划ID沿用AAK-FOLLOWUP-20260908-R3、修订R4。

作为选定实现执行器，按实际授权连续完成就绪X任务和挂载切片；使用当前仓库正式命令、精确路径与工作树，不因包内历史路径硬编码而误操作。不要将独立Q00/Q01变成自己的通过声明。另一执行器若已持有活动队列，先在现有台账交接，再接管；不并发写同一权威状态。
