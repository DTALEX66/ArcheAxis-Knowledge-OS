# DSH／DeepSeek执行入口

阅读EXECUTOR-START.md。与CODEX使用同一TASKS.json，不另建DP_TASKS，不引入模型品牌审批链。

DSH是执行工具：必须先确认当前工作根是DTALEX66/ArcheAxis-Knowledge-OS的实际checkout；D:\All projects\DSH不是据此推断的本项目路径。按当前许可连续执行就绪X与切片，保持运行输出治理、原件保全和原门禁。最后交独立GPT审计；不能用模型自评替代实际运行收据。
