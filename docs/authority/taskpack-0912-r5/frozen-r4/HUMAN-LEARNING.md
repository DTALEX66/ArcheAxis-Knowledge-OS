# V15人类学习承接｜12项任务内切片

原23任务保持权威；本文件从HUMAN-LEARNING.json生成。所有切片均为待实现，原父任务依赖与门禁仍适用。

## HL01 对话连续性、来源导入与历史资格

- 阶段：M0；父任务：X00, X02, X05；切片前置：无
- 功能：CF01, CF03, CF04；模板：无
- 执行：保全R3.1和V14/V15原件；登记来源hash、命名空间及历史/候选状态；40方法、35研究、36学科、104资源全量登记但不自动启用。连续性记录区分用户要求、助手提案、检索摘要、历史事实、待核缺口。
- 交付：来源与连续性索引；原ID到Core ID映射；导入/重试收据和历史可见性规则。
- 验收：同包重导不重复；旧固定日历/旧评分只在历史视图可见，不可因检索命中恢复当前权威。不同版本同名记录不互相覆盖。
- 场景：HL-AC-01
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL02 逐主张证据关联与研究使用边界

- 阶段：M0；父任务：X04, X07；切片前置：HL01
- 功能：CF03, CF05；模板：T11
- 执行：在现有来源/锚点/知识契约上补claim—研究—定位—关系。关系区分支持、限制、反证、待核和应用推导；阅读深度与证据强度分开；记录研究对象、结果类型、勘误。只对首批实际使用的主张补原文锚点，不把35篇全文审完作为导入前置。
- 交付：首批主张证据映射和研究采用台账；R0仅参考；R1不得伪称全文偏倚评估。
- 验收：M15的R19必须标待核边界线索；R33/R34仅勘误记录，不当独立实验；无依据的个人定义仍能保存。
- 场景：HL-AC-02
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL03 提示、作答与学习事件契约

- 阶段：M0；父任务：X04, X05, X08；切片前置：HL01
- 功能：CF08, CF09, CF11, CF12；模板：T01, T06
- 执行：沿已有事件契约增加活动/题目/知识版本、原回答、提示暴露、允许工具、答案曝光、评价来源、幂等键与事件版本。提示曝光由受信入口记录，客户端不能自报无提示获得掌握升级；离线输入保留来源及未核状态。
- 交付：契约迁移、受信曝光事件、断线重试与冲突收据、旧客户端兼容适配。
- 验收：看过答案后同次重答不能计为独立；同键同payload幂等、同键异payload冲突；缺未知字段按明确版本策略处理；屏幕阅读器等必要支持不误判代答。
- 场景：HL-AC-03
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL04 提取反馈与实际学习活动

- 阶段：M0；父任务：X08；切片前置：HL02, HL03
- 功能：CF06, CF07, CF08, CF10；模板：T01, T12
- 执行：复用现有DeepTutor或活动组件完成一个主题的目标、先修、材料、独立提取、可靠反馈、错误重练；候选题有来源、参考答案、适用条件和可纠正rubric。不要批量把整本书全部制卡。
- 交付：一个真实主题的题目、回答、参考依据、纠错及新题操作轨迹。
- 验收：活动打开、视频看完或AI赞扬不算完成；发现题目错误时暂停相关判分并修题，不降低学习者能力。
- 场景：HL-AC-04
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL05 样例、缺步与提示撤除

- 阶段：M0；父任务：X08；切片前置：HL03
- 功能：CF07, CF08, CF10, CF12；模板：T03
- 执行：以现成样例展示和题目组件实现完整例→补关键步→独立变式；根据连续表现恢复或撤除必要指导，首版用可解释规则及人工纠正，不开发全面自适应算法。
- 交付：同技能三种支持级别的活动模板和转换理由。
- 验收：跟做成功与独立成功分开；不能单次失败永久贴能力标签；提示数量与长度不是效果指标。
- 场景：HL-AC-05
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL06 交错辨别与变式选法

- 阶段：M0；父任务：X08；切片前置：HL03
- 功能：CF08, CF10, CF12；模板：T04
- 执行：选择已具备基本解法的易混类别，混排且不在题头泄露类别；先记方法选择与依据，再做执行。将类别判断错误与步骤执行错误分开，不把跨软件随意切换当通用交错。
- 交付：已核答案的小题单、混排顺序、选法与执行两维评价。
- 验收：不强制所有词语/长文使用交错；测试含新表述而非仅改数字；解释规则选择理由。
- 场景：HL-AC-06
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL07 讲回、信心校准与独立测验

- 阶段：M0；父任务：X08；切片前置：HL02, HL03
- 功能：CF09, CF10, CF12；模板：T01, T05, T06
- 执行：讲回包含原因、条件、正反例；作答前可选信心记录，与结果对照。测验模式隔离完整解答，使用未曝光题；允许学习者选择直接看答案，但明确切回教学模式，相关尝试不再当独立测试。
- 交付：原始讲回、逐维rubric、评价者/模型版本、人工纠正与留出题结果。
- 验收：流利但条件错不能判理解；单个自信错答不永久判人格；保留无障碍支持；必须区分学习辅助与能力测量。
- 场景：HL-AC-07
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL08 复习负担、卡片质量与方法停用

- 阶段：M0；父任务：X08；切片前置：HL03
- 功能：CF10, CF11, CF23；模板：T10, T12
- 执行：继续单一FSRS/Anki排程权威；记录算法/参数版本与原始真实评分。展示到期/新增/耗时，识别坏卡、频繁失败和低价值新增；方法可调整暂停，保留记录。不把自定义三档评价直接误映射算法按钮。
- 交付：评分映射表、版本化排程回放、卡片复核队列、方法试用成本记录。
- 验收：忘记不能暗转Hard；固定日历不是默认最优；暂停不删除资产；总成本含制作、编码、练习和维护，不虚报个人因果提升。
- 场景：HL-AC-08
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL09 学科活动、表征与空间助记

- 阶段：M0_existing_activity_then_M1；父任务：X08, X12；切片前置：HL02, HL03
- 功能：CF12, CF24, CF25, CF26, CF27；模板：T02, T05, T07, T08, T09
- 执行：M0仍按原要求接至少一个现成可视化/互动活动；M1验证数理、文学历史、实际技能三个主题，36方案保留为内容候选。T02先用现成文本/2D路线；稳定知识位置与线索关联，禁止借此激活大型3D自研。
- 交付：活动—知识版本—可采集事件说明；三主题的目标、材料、产物、测试及成本；小路线位置表。
- 验收：播放完成不当理解；外部无成绩接口采用明确手动记录；公式/单位/参数由可核计算或教材检查；开放作品用多维rubric；专业模拟需实际允许环境。
- 场景：HL-AC-09
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL10 研究更正与人机双侧影响

- 阶段：M0；父任务：X07, X08, X09；切片前置：HL02, HL03
- 功能：CF04, CF20, CF21, CF22, CF23；模板：T11
- 执行：把研究更正/知识修订连接到主张、活动模板、题目和机器方法。未读勘误时记录待核影响，不猜测整篇失效；已确认影响后重新判断资格，保留原事件，并分别复测人和真实机器客户端。
- 交付：原文—更正—主张—题目/方法影响表及两侧再使用收据。
- 验收：R20/R34、R32/R33可作待核资料；具体传播回归用明确标注的合成更正，不编造真实勘误内容；索引延迟/重启不恢复旧资格。
- 场景：HL-AC-10
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL11 学习资产离线、导出和恢复

- 阶段：M0；父任务：X05, X08, X10, X11；切片前置：HL03
- 功能：CF11, CF28, CF29；模板：T01, T06, T10
- 执行：把活动/题目/知识版本、原回答、提示暴露、评分纠正和排程记录纳入既有备份；停用学习插件后仍能读历史；恢复按staging/校验，不同步活动主库。不强制未下载资源离线可用。
- 交付：非空学习资产导出、恢复差分和替换组件兼容说明。
- 验收：离线事件重发不重复；缺插件/模型时仍可访问资料和记录并显示能力限制；旧包未含新字段需显式迁移。
- 场景：HL-AC-11
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## HL12 产品验收与学习效果分别证明

- 阶段：M0_and_M1_audit；父任务：X11, Q00, Q01；切片前置：HL04, HL05, HL06, HL07, HL08, HL10, HL11
- 功能：CF12, CF17, CF21, CF23；模板：T10
- 执行：在G01—G14中补对应学习验收，不另建最终审计权威。M0检查活动和延迟测验机制真实可用；真实时间未到的长期结果记待观察。M1补三个主题实测及负担反馈。人类教学证据不能替代机器方法留出评测。
- 交付：同候选源码/包hash的独立GPT报告；产品行为测试与实际学习效果观察分表。
- 验收：模拟时钟只证明排程行为；不能声称记忆提升。没有真实长期数据不报提升率，也不让90天研究计划阻塞软件交付；原在线核查/迁移/机器/Windows门仍必须通过。
- 场景：HL-AC-12
- 回滚：保留原件/旧版本/原始事件；回退本切片适配与配置，使用经验证备份恢复；不直接回写旧活动库。

## 跨任务切片执行规则

切片是跨任务功能索引，不是额外串行队列。depends_on_slices为整体行为验收前置，不阻塞契约/fixture先行。父任务按原DAG及本表归属贡献验收，不以尚未就绪下游贡献阻塞上游；整条切片闭环在各贡献汇合后验收。HL09/HL12按M0/M1分别检查。具体归属见CONTRIBUTION-MATRIX.md。
