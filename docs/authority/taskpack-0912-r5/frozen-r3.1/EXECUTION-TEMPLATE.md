# 执行与独立审计交接字段

模板不是新台账。继续使用仓内现有 EXECUTION 和决策账本；X00登记唯一活动位置。

每个切片至少：
- parent_task_id / slice_label / audit_issue / feature_ids / acceptance_case_ids；
- baseline_sha、工作树差分、允许的精确路径、实际授权依据；
- donor路径/版本/hash、复用方式、必要改动及不采用替代的理由；
- command（真实完整参数、无密钥）、cwd、环境/依赖/模型/工具版本；
- tested_source_sha、source_tree、锁文件和包hash、run_id；
- 输入fixture和输出hash、实际退出码、正例/负例结果、日志定位；
- 人工操作步骤、窗口/客户端证据、读回证据；截图不能替代数据库/接口结果；
- changed_paths、失败路径、回滚、限制、remaining_work；
- implementation_state 与 independent_audit_state 分开。

公共仓只提交脱敏清单和合成/许可公开fixture；真实资料、凭据、私密会话、原始私密日志不上传。ignored原始证据由本机保存，云端拿不到应标不可复核或请求最小脱敏/公开复跑，不强迫外传。

## 最终交接必须回答

1. 用户运行哪个实际文件/命令，首次下载什么，断网还能做什么？
2. 本次候选SHA、源码树、依赖/模型版本与安装包hash是否对应？
3. G01—G14及M1增强逐项是什么结果？哪些没测或被资源阻塞？
4. 29功能哪些仅实现首版子集，哪些仍冻结？不能用全局DONE盖住缺项。
5. 清理前后同范围字节/排除项/卷可用空间、唯一资料保全和再增长结果是什么？
6. 旧版备份是否在新版本恢复，真实用户库是否实际迁移（未做就说未做）？
7. 哪个缺陷对应回到哪个原任务，怎么复现，下一条可执行切片是什么？

文档更新不改变原始tested SHA。新代码影响旧结论时补复测；不改写以前失败的运行结果。
