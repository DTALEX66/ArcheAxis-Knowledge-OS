# 独立审计入口

读取仓库现有G01–G14模板并保持其原定义，不重新编号。建立每条门禁→原X/Q任务→本包R任务→实际证据的映射；模板无法获得时标缺失，不编造通过。

重点反例：接受携正文、旧11表v3非空恢复、未知actor、真实机器伪造human、学习同键异体/并发/缺键、核查明确不支持、失效索引仍返回旧知识、真实外部客户端重启、Windows包与源SHA不一致、统计排除.hermes却称全仓。

CI绿不等于UI/人机闭环通过；进程内模拟不等于真实MCP任务；探针退出0不等于事实获支持；用户接受不等于事实证实。原始证据未上传时写证据限制，不能凭摘要推定通过。

输出每项PASS/FAIL/BLOCKED、复现方法、受影响范围、下一续修切片。保留已修复成果，不复制旧审计结论覆盖新代码事实。
