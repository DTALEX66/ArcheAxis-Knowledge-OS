# R12 cleanup manifest (proposal only - nothing deleted)

Read-only measurement, 2026-09-12, same tool and scope as the R01/R12 census
(`scripts/maintenance/inventory_project.py`). Logical bytes of regular files; this
is not allocated space. **No deletion has been performed and none is authorized by
this document**: each path below needs its own authorization.

## Regenerable candidates (measure and authorize individually)

| Path | Logical size | Files | What it is | How it is regenerated | Recommendation |
| --- | --- | --- | --- | --- | --- |
| `.project-local/build` | 15.464 GiB | 52,788 | cargo output (the sealed target-dir) | `cmd /c call .project-local\runs\cargo-full-workspace.bat` rebuilds what a test run needs; a full rebuild costs minutes, not data | **largest reclaim**; safe to delete wholesale when no run is in flight |
| `target` (repo root) | 5.729 GiB | 19,949 | the legacy pre-seal cargo output, frozen since the X01 seal | not regenerated by bare cargo any more (`.cargo/config.toml` redirects output); it would only come back if that config is removed | **safe to delete**; keeps the growth-stop story intact |
| `.project-local/cache` | 1.148 GiB | 743 | tool caches created by scripts | recreated on demand | safe to delete |
| `.venv` | 0.859 GiB | 22,138 | project virtualenv used by every Python test and worker | `python -m venv .venv` plus the pinned requirements; needs network/registry access | **do not delete now**: every verification run depends on it, so removing it would block the remaining slices |
| `.project-local/inventory` | 0.404 GiB | 8 | two full file-listing inventories (`files.jsonl` ~222 MB and ~211 MB) from the X14 waves | the census tool can regenerate a listing on demand | keeping the summaries is enough; the two `files.jsonl` bodies could go if a fresh listing is ever needed |

Total regenerable candidates if all were approved: about **22.4 GiB** logical
(.project-local/build 15.464 + target 5.729 + cache 1.148 + inventory listings
0.404), of which the two clearly inert items (root `target`, `cache`) are
**6.877 GiB**.

## Preserve (evidence, unique history, or user data)

| Path | Logical size | Why it stays |
| --- | --- | --- |
| `.project-local/runs` | 3.500 GiB | every receipt the ledger cites lives here (census JSONs, test logs, smoke databases, wrapper scripts) |
| `docs`, taskpacks, `workspace/intake` | small | the authority record itself |
| `.project-local/deeptutor-val` | 0.077 GiB | the running host's data, logs and UI evidence |
| `data` | 0.094 GiB | local runtime data, not a cache |
| `.hermes` | 42.853 GiB (separate fingerprint) | preserved legacy material: opaque, no new writes, **never** blanket-deleted; its content was not read |

## Method, deltas and unknowns

- Same tool, same scope as R01: observed logical total 23.423 -> 27.469 GiB and
  files 130,120 -> 152,230; the increase is this session's real build/test/evidence
  output, all inside the governed `.project-local` locations.
- Root `target` is unchanged at 5.729 GiB, so the growth stop still holds.
- D: free space moved 231.15 -> 227.29 GiB, consistent with that increase.
- `.hermes` newest write is still 2026-09-06 11:29: zero growth this session.
- Unknown and stated as unknown: `.hermes` and other opaque private roots are not
  measured by the census, allocated (physical) space is not measured, and hard
  links are counted per path.

## After an authorized deletion (the checks R12 requires)

1. Re-run the same census and report the logical delta and the volume free delta
   separately.
2. Run one real build and one real test to confirm nothing needed was removed:
   `cmd /c call .project-local\runs\cargo-full-workspace.bat`.
3. Re-measure and confirm `.hermes` and the tracked source tree are untouched.
