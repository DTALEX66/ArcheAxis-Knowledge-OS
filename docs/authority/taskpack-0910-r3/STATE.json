{
  "R00": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "package installed single-level at docs/authority/taskpack-0910-r3 with reference-r2 snapshot (24 files)",
      "verify_package.py exit 0 via 'python -X utf8' (hashes, 17 task dependencies, all 23 original tasks retained)",
      "R00-BASELINE-REVIEW.md: baseline locked at HEAD 985a219, 17-slice map, inherited defects re-checked",
      "inherited defects re-checked, not assumed: R02 fixed (288991a), R05 fixed (288991a), R03 fixed (985a219), R06 partial (288991a, live run environment-gated)",
      "fresh baselines at 985a219: cargo workspace exit 0 / 52 groups; python run_tests.ps1 --full 2347 passed / 7 skipped / 0 failed",
      "gates: check_repository_conventions worktree passed; check_architecture passed",
      "entry registration: AGENTS.md section 6, DOCUMENTATION_AUTHORITY_INDEX, CONFIGURATION_AUTHORITY_INDEX, DECISION_SUPERSESSION_LEDGER SUP-018, workspace/intake/2026-09-10-next-taskpack-0910.md",
      "mojibake scan over 451 active docs: no corruption in entry documents; single U+FFFD is a binary PNG header snippet in docs/authority/legacy (data, not document damage)"
    ]
  },
  "R01": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "R01-CAPACITY-BASELINE.md: method, scope, inclusion/exclusion/unknown, volume trend with causes",
      "same-tool census 23.423 GiB / 130,120 files / 20 errors / 60 excluded opaque / 10 reparse (receipt .project-local/runs/r01-census.json)",
      "hermes fingerprinted separately: 42.853 GiB / 718,077 files, newest write 2026-09-06 (zero growth)",
      "D: free 231.15 GiB / used 234.61 GiB recorded independently of logical bytes",
      "growth-stop proof: bare cargo build -p archeaxis-api exit 0 via .project-local/runs/cargo-build-api.bat; root target/ byte-identical 6,151,810,038 / 19,949 before and after; +52.6 MiB into .project-local/build/cargo; artifact 7,492,608 bytes",
      "remaining scope recorded: packaging-side path diff, __pycache__/ruff cache redirect decision, physical allocation measurement"
    ]
  },
  "R02": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "288991a REVISION-01: accept/reject/deprecate carrying new_body is rejected and body bytes stay immutable; only modified creates a new superseding revision",
      "cbe253b: API review-decisions forwards new_body so corrected content reaches the domain revision path (accept + new_body still rejected)",
      "domain suite exit 0 / 9 groups ok incl. accept_with_new_body_is_rejected_and_body_stays_immutable, accept_commits_status_and_one_event_atomically, failed_review_leaves_no_partial_state, modified_creates_traceable_successor (receipt .project-local/runs/r02-domain.log)"
    ]
  },
  "R03": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "985a219 ARCHIVE-01: layout resolved from the manifest table set (v2 8-table, v3 11-table, v3 13-table, current 13-table); unknown v3 layout and current archive missing a table rejected; additive nullable columns filled NULL; unknown column rejected",
      "archive suite exit 0 / 7 groups ok (receipt .project-local/runs/r03-archive.log)",
      "GAP CLOSED: four NON-EMPTY v3 archives were exported by the OLD implementation in a scratch worktree (store+archive from 968c479, a2dbef5, 60b355a, 3d65609) and committed as fixtures crates/archeaxis-archive/tests/fixtures/v3-{ten,eleven,twelve,thirteen}-tables (each with PROVENANCE.txt); sources=1, knowledge=2, objects=1 per fixture",
      "v3 layout history verified from git: version 3 shipped 10, 11, 12 and 13-table export layouts; all four are now accepted by exact table set, v1 (7 tables, no workspace_meta) stays unsupported, and any other v3 set is rejected",
      "new test genuine_v3_era_archives_restore_with_content restores each fixture, asserts schema upgrade to 4, 2 knowledge rows with body bytes preserved and 1 source row",
      "archive suite exit 0 / 7 groups ok (receipt .project-local/runs/r03-archive-v2.log); full workspace cargo test exit 0 / 52 groups (receipt .project-local/runs/r03-full-workspace.log)",
      "environment pitfall recorded: a scratch worktree under the repo inherits the main .cargo/config.toml target-dir, which mixed v3/v4 artifacts in one build directory; the worktree wrapper now pins its own CARGO_TARGET_DIR"
    ]
  },
  "R04": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "fail-open hole fixed: Launch::from_stdin accepted any actor string and the middleware mapped every non-machine value to human, so an unknown role silently received human authority; from_stdin now rejects anything outside {human, machine} before a session or workspace exists",
      "real-process regression: unknown_launch_actor_is_rejected_and_never_grants_human_authority covers alien / Human / owner / empty / trailing-space values (none may start, none may create a workspace) and confirms actor=human still starts and serves /api/v1/system/version 200",
      "client-supplied x-archeaxis-actor continues to be overwritten by the launch-session claim (existing C02 coverage), so it cannot escalate or downgrade",
      "api suite exit 0 incl. launch_auth 6 tests (receipt .project-local/runs/r04-api.log)",
      "REMAINING: real host-signed and host-held machine/human credential scope is still to be implemented; that part is not claimed"
    ]
  },
  "R05": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "scheduler worker landed: services/python-workers/learning/worker_schedule.py reuses the existing donor LearningScheduler (not the ladder); stdin JSON -> stdout JSON; unusable requests fail closed with an explicit error object and never invent an interval",
      "finding: py-fsrs fuzzes intervals by default (Scheduler(enable_fuzzing=True)), so identical card+rating calls returned different due dates; the donor now takes an additive enable_fuzzing keyword (default True unchanged) and the worker requests False, giving reproducible intervals for replay/restart/audit",
      "tests: tests/test_worker_schedule.py 8 passed (mature-card interval exceeds the ladder's 14-day ceiling, rating sensitivity, determinism, correct->rating mapping, explicit errors incl. subprocess fail-closed) plus donor tests/test_learning_scheduler.py 6 passed - 14 passed / 0 failed",
      "Core-side adapter landed: crates/archeaxis-application/src/scheduler.rs exposes SchedulerClient (python from ARCHEAXIS_PYTHON + repository worker path), a subprocess JSON contract that validates authority == fsrs, and returns Unavailable/Rejected on failure - never a ladder interval",
      "integration tests 5 passed (receipt .project-local/runs/r07-scheduler-adapter.log): real Rust->Python->FSRS donor, mature card interval > the ladder's 14-day ceiling, reproducible identical requests, missing rating rejected, missing interpreter and missing worker script both reported as Unavailable",
      "keyed review path parametrized (R05b): ScheduleSource::{Ladder, Explicit(Option<i64>)} plus a private record_review_keyed_impl, with public record_review_scheduled(..., next_review_days: Option<i64>); None means the scheduler was unavailable, so the event is still recorded with its persistent key but next_review NULL, an explicit schedule:unavailable payload marker and the SCHEDULE_UNAVAILABLE (-2) sentinel - never a ladder interval; record_review_keyed keeps its previous Ladder behaviour",
      "EVENT-01 semantics preserved and re-asserted for the scheduled path: replay returns the original receipt (SCHEDULE_DUPLICATE -1) and writes nothing, same key with another item or payload conflicts, empty key is rejected",
      "tests: crates/archeaxis-domain/tests/learning_schedule_authority.rs (4 tests) - domain suite 10 groups ok (receipt .project-local/runs/r13-r05-domain.log); api suite 14 groups ok incl. learning_events_api and machine_correction_loop, no regression (receipt .project-local/runs/r13-r05-api.log)",
      "API wiring landed (R05c): LearningEventBody accepts optional schedule_state + now; the handler resolves the authority before writing - card state present -> SchedulerClient (reused FSRS worker), scheduler unusable -> record_review_scheduled(None) as an unscheduled review, no card state -> the placeholder ladder explicitly; every response now carries schedule_authority (fsrs / unavailable / placeholder_ladder) so a caller cannot mistake the ladder for real scheduling",
      "wire-level tests: crates/archeaxis-api/tests/learning_schedule_api.rs (3 tests) - card state yields fsrs with >14 days, no card state reports placeholder_ladder, unusable state yields unavailable with the -2 sentinel and a history row whose next_review is null; api suite 15 groups ok (receipt .project-local/runs/r15-r05-api.log)",
      "restart consistency asserted (R05d): schedule_survives_a_workspace_reopen - a scheduled review keeps its due date after reopening the workspace, an unscheduled review stays unscheduled, and a post-restart retry is still recognised as a replay with the same receipt",
      "full workspace re-run: cargo test --workspace --offline exit 0 / 55 groups ok (receipt .project-local/runs/r16-full-workspace.log), covering the new domain/api/application test files",
      "REMAINING (not claimed): product callers must always submit schedule_state; that caller-side integration belongs to the R10 host wiring, after which no product path reaches the placeholder ladder"
    ]
  },
  "R06": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "offline deliverable covered by regression: tests/test_x07_public_check_probe.py 13 passed - a radius sentence with object+unit+number is support; a diameter hit is recorded as weaker diameter_only and never as support; an unrelated same number (e.g. a year) yields none; model verdicts \u4e0d\u652f\u6301 / \u65e0\u6cd5\u5224\u65ad / empty / error can never become support; an unreachable source raises instead of silently becoming no-evidence",
      "cloud cross-check requires owner-configured and owner-approved endpoints and credentials (project default is zero paid budget): recorded as BLOCKED_EXTERNAL, no cloud verification is claimed",
      "REMAINING: live run (public retrieval plus local-model verdict) needs working outbound HTTPS (previously intermittent) and ollama (currently reachable on :11434) - queued as the next work unit",
      "LIVE RUN SUCCEEDED (round 18): real public retrieval of the Wikipedia Earth extract produced a locatable quote; classification was diameter_only (the 12,742 km mention is the diameter, not the radius) and the local qwen3:8b answered \u65e0\u6cd5\u5224\u65ad (source does not state the mean radius) which parsed to undeterminable with supported=false - the 6371 claim was correctly NOT marked supported. Receipt: .project-local/runs/r18-x07-live.log",
      "CLOUD SUB-ITEM: BLOCKED_EXTERNAL - cross-checking against a cloud endpoint needs owner-configured and owner-approved credentials; not performed and not claimed"
    ]
  },
  "R07": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "non-empty migration path proven on a synthetic legacy library (notes 3 rows, docs, attachments, links, learning_history 3 rows): existing stage_demo_semantic_import plus a new stage_legacy_learning_history that routes learning writes through archeaxis_domain so the Core stays the only learning-event authority",
      "fidelity rules: a POSITIVE legacy interval is preserved; a missing interval and a legacy 0 (due immediately, which vNext history cannot represent) are both recorded as unscheduled, and the counters match what is actually stored",
      "idempotency: deterministic persistent keys (legacy-table-rowid) mean a re-run replays receipts (3 replayed, 0 new) instead of accumulating history",
      "no dual-write: the legacy database is byte-identical after the migration (sha256 before/after)",
      "receipts: .project-local/runs/r19-r07-migration.log (5 groups ok), .project-local/runs/r19-full-workspace.log (56 groups ok after the new archeaxis-domain dependency)",
      "REMAINING (not claimed): the owner real private library was not provided, so real-library migration qualification is still open; attachments/links remain explicit loss rows rather than transported assets; legacy timestamps are not preserved (the persistent key stores the legacy table and row id only)"
    ]
  },
  "R08": {
    "status": "IN_PROGRESS",
    "evidence": [
      "native PDF route landed: services/python-workers/document/worker_pdf.py returns the same contract as the text worker (engine/engine_version/text/structure/loss_receipt), so both routes feed one ingestion path",
      "anchors carry page AND line (path [page-N, line-M]) plus character offsets into the projected text, so knowledge can navigate back to the page/region it came from",
      "explicit degradation, no silent success: a corrupt PDF and bytes a lenient engine accepts as a zero-page document both raise; a page with no extractable text (scan/screenshot) returns its real empty text with a loss note stating that OCR is required",
      "the receipt records engine, version, page coverage and losses only - no accuracy figure and no model confidence is presented as recognition accuracy",
      "tests: tests/test_worker_pdf_native.py 5 passed (numbers and units survive extraction, every page is addressable, no-text page degrades to an OCR note, corrupt file fails explicitly, CLI exits 1 without publishing text)",
      "REMAINING: scanned/screenshot OCR route through the same contract; the Rust-side unified job contract (the executor currently knows only the text worker); quality display; and the end-to-end import-to-retrievable-knowledge run for all three sample classes",
      "OCR route conformed (R08b): worker_ocr now emits the same structure anchors as the text and PDF routes - kind=region, path [region-N], character offsets into text, plus bbox - with anchor_summary coverage; per-word confidence is kept only as a review field and the receipt still contains no accuracy claim",
      "tests: tests/test_worker_ocr_route.py 2 passed against real Tesseract on a synthetic screenshot (text including 6371 recognised, region anchors carry bbox and offsets inside text, coverage reported, blank image does not claim recognition)",
      "ENVIRONMENT FACT: this shell TESSDATA_PREFIX points at a directory that does not exist, while the repository ships tools/tesseract/tessdata/eng.traineddata - passing tessdata_dir explicitly works, and the pre-existing tests/workers/test_bulk_ocr.py skips for the same reason",
      "GAP ANALYSIS (R08c, read-only this round): the unified job contract is blocked by three concrete pins - executor.rs hardcodes the python-worker-text-ndjson v1 identity; transport/text_ndjson.py accepts only capability text.extract v1; and the new PDF/OCR workers are plain CLIs without the sidecar NDJSON envelope. Next unit: generalize the transport capability set with per-route dispatch, add NDJSON mains to the pdf/ocr workers declaring their own capability/identity, widen the executor hello check to a controlled identity set (or a profile-declared identity), then run one job type over the three sample classes end to end",
      "UNIFIED JOB CONTRACT LANDED (R08d): transport/text_ndjson.py now dispatches a controlled ROUTES table (text.extract, pdf.extract, image.ocr) through one request/response envelope, one rejection vocabulary and one artifact triple (text + document_structure + loss_report); capability version, accepted media types and worker module are declared per route",
      "integration fix found by the new tests: staging inputs are content-addressed (no extension) while the OCR worker validates the file extension, so the transport now materialises a route-local view with the suffix implied by the declared media type, leaving the original input untouched and still hash-checked",
      "tests: tests/test_unified_job_contract.py (three routes on the shared envelope plus unknown-capability, wrong-version and per-route media-type rejections); combined route suites 21 passed; full Python suite run_tests.ps1 --full 2381 passed / 7 skipped / 0 failed (receipt .project-local/runs/r24-python-full.log)",
      "REMAINING for R08: (b) NDJSON mains for the pdf/ocr workers declaring their own capability and identity; (c) executor.rs hello check widened from the hardcoded python-worker-text-ndjson v1 to a controlled identity set or a profile-declared identity; (d) one job type over the three sample classes end to end into retrievable knowledge",
      "PDF ROUTE ON THE SIDECAR LOOP (R08e): transport gained a reusable serve_stdio(worker_name, capabilities, staging_root) that keeps handshake, request validation, response envelope and error vocabulary identical; the text worker now calls it unchanged, and worker_pdf.py declares its own identity python-worker-pdf-ndjson and capability pdf.extract via a --staging-root mode",
      "gap found and fixed in my own step (a): the route table is shared, so without an owner check a worker could serve a capability it never advertised; serve_stdio now rejects any capability outside the worker own advertised list (same unsupported-capability vocabulary, retryable false)",
      "tests: tests/test_pdf_sidecar_job.py 2 passed - a real subprocess handshake asserts the advertised identity/capabilities and a successful job returns the artifact triple with 6371 in the text artifact; a foreign capability is rejected with no outputs. Combined five suites 23 passed; full Python suite 2383 passed / 7 skipped / 0 failed (receipt .project-local/runs/r25-python-full.log)",
      "REMAINING for R08: the OCR worker NDJSON entry (same pattern); executor.rs hello check from hardcoded python-worker-text-ndjson v1 to a controlled identity set or profile-declared identity; then one job type over the three sample classes end to end into retrievable knowledge",
      "OCR ROUTE ON THE SIDECAR LOOP - step (b) complete (R08f): worker_ocr gains a --staging-root mode advertising python-worker-ocr-ndjson / image.ocr through the same serve_stdio; all three routes now share one job loop and envelope (text, pdf, image.ocr), with the CLI paths unchanged",
      "tests: tests/test_ocr_sidecar_job.py 2 passed on a real subprocess handshake and job (succeeded, artifact triple, real recognised text, region anchors, foreign capability rejected retryable=false); combined sidecar/contract suites 10 passed; CLI regression tests/test_worker_ocr_route.py 2 and tests/test_worker_pdf_native.py 5 passed",
      "REMAINING for R08: (c) executor.rs hello identity from the hardcoded python-worker-text-ndjson v1 to a controlled identity set or a profile-declared identity, keeping attempt/error/cancel semantics; (d) one job type over the three sample classes end to end into retrievable knowledge",
      "EXECUTOR IDENTITY GATE WIDENED (R08g): executor.rs now accepts a controlled KNOWN_WORKER_IDENTITIES set (text, pdf, ocr) with version 1 instead of pinning the text worker; a real executor test shows the PDF route identity is accepted (the failure no longer mentions unexpected worker identity) and the pre-existing real-job/restart/cancel tests stay green - application suite 7 groups ok, receipt .project-local/runs/r27-executor.log",
      "SECOND PIN FOUND (next unit): crates/archeaxis-sidecar-protocol/src/worker.rs hardcodes text.extract both in the request constructor (~line 86) and in the hello compatibility check (~line 97), so PDF/OCR stop at incompatible worker hello; the handshake must become capability-agnostic - the worker must advertise the requested capability, and the request capability must come from the job/route",
      "CAPABILITY DE-HARDCODING DONE - step (c) complete (R08h): Request gained a generic job(...) constructor taking the route capability (Request::text is now a thin wrapper), decode_hello became capability-agnostic (protocol shape, non-empty identity, unique non-empty capability/schema lists), and the executor checks where the request is known that the worker advertises the job capability, failing with an explicit worker does not advertise the requested capability message",
      "tests: full workspace cargo test --workspace --offline exit 0 / 56 groups with the new executor test proving a PDF worker handed the text route is refused for the capability reason (not a pinned identity, not a generic hello mismatch), and the pre-existing handshake/version/capability protocol test still green (receipt .project-local/runs/r28-full-workspace.log)",
      "REMAINING for R08 step (d): job kind to capability/worker mapping (the executor currently holds a single worker path), one job type over the three sample classes end to end into retrievable knowledge, and quality display",
      "ROUTE TABLE (R08i, step d part 1): attempts.rs declares ROUTES mapping job kind to capability and input media type (text/text.extract -> text.extract + text/plain, pdf -> pdf.extract + application/pdf, image -> image.ocr + image/png) with route_for_kind(); claim() no longer uses a text-only allow-list or a hardcoded text/plain media type, and an undeclared kind is refused at claim time leaving the job queued (never marked running)",
      "tests: tests/route_capabilities.rs 3 tests - every declared route selects its capability and media type, an unknown kind is refused without changing job state, and route lookup is explicit; application suite 8 groups ok (.project-local/runs/r29-app.log); full workspace cargo test exit 0 / 57 groups (.project-local/runs/r29-full-workspace.log)",
      "REMAINING for R08 step (d): the executor must select the route worker by job kind (it still holds a single worker path), then the three sample classes must run end to end into retrievable knowledge with quality display",
      "ROUTE REGISTRY IN THE EXECUTOR (R08j, verified part of step d): Executor::open_routes registers capability -> worker (open() stays compatible as the text-only case); execute_owned dispatches by the claimed request capability and refuses with no worker registered for capability when none matches, instead of sending the job to the wrong process",
      "launch policy fixed: stdlib-only text routes keep the hardened -B -S launch, while engine-backed routes (PDF/OCR) must keep the interpreter site-packages - with -S the PDF engine import failed as unavailable, which the end-to-end attempt exposed",
      "tests: full workspace cargo test exit 0 / 58 groups with a_job_without_a_registered_route_worker_fails_explicitly and registering_a_route_makes_its_capability_dispatchable; PDF worker and unified-contract pytest suites still green (receipt .project-local/runs/r30-full-workspace.log)",
      "REMAINING for R08 (recorded, not claimed): a real PDF job still stops at the Core receipt validation, which is correct behaviour - the receipt must supply covered/total/coverage together, and structure/coverage must match the projected text so anchors must be derived from the FINISHED text (page separators otherwise shift offsets and line counts). Both are PDF-worker fixes to be committed together with the end-to-end job test; the attempted rewrite was reverted rather than committed unverified",
      "PDF END-TO-END STATUS (R08k, investigation only, no code kept): the first Core rejection was cleared by giving the receipt line-based covered/total/coverage and deriving anchors from the FINISHED text, but the job is still refused with invalid receipt: structure or coverage does not match projected text. The exact rule has not been read yet, so the attempt was reverted instead of guessed at; the worktree is back at the last verified commit (3a0662d, 0 dirty files).",
      "NEXT UNIT FIRST STEP: read the validator that raises structure or coverage does not match projected text (attempts::finish or the jobs receipt validation), implement the worker against its exact predicate (for example covered == len(structure) and the last anchor char_end == len(text)), then add the PDF end-to-end job test together with the fix",
      "EXACT CORE RECEIPT PREDICATE FOUND (R08l, read-only): attempts.rs lines 104-106 is text-route specific - it requires loss.engine == python-worker-text and engine_version == 0.1.0, requires structure to equal the canonical line anchors the Core recomputes from the text (kind line, path [line-N]), and requires loss.covered == structure.len() with loss.total == expected line count. That is why the PDF job is refused: the PDF worker reports engine pymupdf-native-pdf and page-qualified paths [page-N, line-M].",
      "NEXT UNIT DECISION: (A) generalise the Core validator - a controlled set of route engine identities plus span/kind-based structure comparison that accepts a page prefix in path (recommended, keeps page anchors and true engine identity); (B) make PDF/OCR receipts impersonate the text engine and [line-N] paths (not recommended: loses page semantics and fabricates engine identity)",
      "ENGINE GATE IS ROUTE-AWARE NOW (R08m, first half of option A): attempts.rs declares ENGINE_PROFILES (python-worker-text/0.1.0, pymupdf-native-pdf/pymupdf, python-worker-ocr/0.1.0) and the receipt check accepts any declared route engine whose version matches, instead of demanding the text engine from every route. Text behaviour is unchanged.",
      "tests: application suite 9 groups ok and full workspace cargo test exit 0 / 58 groups (receipts .project-local/runs/r34-app.log, r34-full-workspace.log)",
      "STILL BLOCKED (second half of option A): structure must still equal the canonical line anchors the Core recomputes from the text, so page-qualified paths [page-N, line-M] are still rejected; the comparison needs to become kind+span based, tolerating a page prefix. Until then no PDF end-to-end test is committed and no PDF job is claimed to work.",
      "CORE VALIDATOR FULLY ROUTE-AWARE (R08n, option A complete): same_spans compares anchors by kind, character span and FINAL path segment, so a page-qualified anchor [page-3, line-12] is equivalent to the canonical [line-12] while a wrong line or span is still refused. Together with the route engine set this makes all three conditions of attempts.rs route-aware.",
      "tests: application suite 10 groups ok with the new receipt_spans.rs (page-qualified anchor accepted and the real engine pymupdf-native-pdf recorded; wrong final segment refused; undeclared engine refused) and full workspace cargo test exit 0 / 59 groups (receipts .project-local/runs/r35-app.log, r35-full-workspace.log)",
      "LAST STEP FOR R08 (d), worker side: the committed PDF worker still omits coverage and counts coverage in pages, and its anchors must be derived from the finished text - exactly the two worker fixes written and reverted in rounds 30/31. The Core now accepts them, so the next unit re-lands those worker fixes together with the PDF end-to-end job test."
    ]
  },
  "R09": {
    "status": "TODO",
    "evidence": []
  },
  "R10": {
    "status": "TODO",
    "evidence": []
  },
  "R11": {
    "status": "TODO",
    "evidence": []
  },
  "R12": {
    "status": "TODO",
    "evidence": []
  },
  "R13": {
    "status": "TODO",
    "evidence": []
  },
  "R14": {
    "status": "TODO",
    "evidence": []
  },
  "R15": {
    "status": "TODO",
    "evidence": []
  },
  "R16": {
    "status": "TODO",
    "evidence": []
  }
}
