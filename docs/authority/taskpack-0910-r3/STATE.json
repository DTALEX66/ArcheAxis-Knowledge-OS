{
  "R00": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "package installed single-level at docs/authority/taskpack-0910-r3 with reference-r2 snapshot (24 files)",
      "verify_package.py exit 0 via 'python -X utf8' (hashes, 17 task dependencies, all 23 original tasks retained)",
      "R00-BASELINE-REVIEW.md: baseline locked at HEAD 985a219, 17-slice map, inherited defects re-checked",
      "inherited defects re-checked, not assumed: R02 fixed (288991a), R05 fixed (288991a), R03 fixed (985a219), R06 partial (288991a, live run environment-gated)",
      "fresh baselines at 985a219: cargo workspace exit 0 / 52 groups; python run_tests.ps1 --full 2347 passed / 7 skipped / 0 failed",
      "gates: check_repository_conventions worktree passed; check_architecture passed",
      "entry registration: AGENTS.md section 6, DOCUMENTATION_AUTHORITY_INDEX, CONFIGURATION_AUTHORITY_INDEX, DECISION_SUPERSESSION_LEDGER SUP-018, workspace/intake/2026-09-10-next-taskpack-0910.md",
      "mojibake scan over 451 active docs: no corruption in entry documents; single U+FFFD is a binary PNG header snippet in docs/authority/legacy (data, not document damage)",
      "VERIFICATION ROUND (round 65, tested_source_sha d482c8): cargo test --workspace --offline exits 0 with 68 test-result groups all ok, 149 passed, 0 failed, 0 ignored, no error[ or panicked lines and doc-tests included (log .project-local/runs/r65-full-workspace.log). This is the first full workspace run since R11c, which reported 67 groups; the extra group is the Obsidian vault round-trip target added in round 62.",
      "PACK INTEGRITY FINDING: the shipped verify_package.py hashes all 23 MANIFEST.json entries against the live pack, but EXECUTION.md and STATE.json are the two files this pack instructs the executor to write progress into, so it necessarily fails with 'AssertionError: EXECUTION.md' once any work is recorded. Measured directly: 21 of 23 entries match live, and exactly EXECUTION.md and STATE.json diverge. Both match the install snapshot preserved at .project-local/runs/taskpack-0910-shipped/, so the divergence is the ledger growing, not a damaged package.",
      "PACK INTEGRITY FIX: scripts/check_taskpack_integrity.py separates the two questions. Frozen entries must hash exactly (measured: 21/21 match, zero frozen pack files changed); the two live progress files must hash to the install snapshot so their change is attributable to growth (EXECUTION.md 140 -> 71177 bytes, +71037; STATE.json 971 -> 71981 bytes, +71010); and the plan structure stays verified - unique task ids, an acyclic fully-resolvable dependency graph, and all 23 predecessor tasks still represented. Result: exit 0 with 52 ledger rows and 17 slices. It is deliberately not a CI gate, because the snapshot lives under an ignored path that a fresh checkout does not have; the checker says so in its own docstring. tests/test_taskpack_integrity.py 8 passed.",
      "PROCESS CORRECTION from this round: PowerShell -match is case-insensitive, so a pattern intended to find failing targets also matched the words '0 failed' and produced the absurd verdict that all 68 groups failed. Parsing the same log with Python gives 0 failures. When a test verdict matters, parse the log, do not grep it in PowerShell."
    ]
  },
  "R01": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "R01-CAPACITY-BASELINE.md: method, scope, inclusion/exclusion/unknown, volume trend with causes",
      "same-tool census 23.423 GiB / 130,120 files / 20 errors / 60 excluded opaque / 10 reparse (receipt .project-local/runs/r01-census.json)",
      "hermes fingerprinted separately: 42.853 GiB / 718,077 files, newest write 2026-09-06 (zero growth)",
      "D: free 231.15 GiB / used 234.61 GiB recorded independently of logical bytes",
      "growth-stop proof: bare cargo build -p archeaxis-api exit 0 via .project-local/runs/cargo-build-api.bat; root target/ byte-identical 6,151,810,038 / 19,949 before and after; +52.6 MiB into .project-local/build/cargo; artifact 7,492,608 bytes",
      "remaining scope recorded: packaging-side path diff, __pycache__/ruff cache redirect decision, physical allocation measurement"
    ]
  },
  "R02": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "288991a REVISION-01: accept/reject/deprecate carrying new_body is rejected and body bytes stay immutable; only modified creates a new superseding revision",
      "cbe253b: API review-decisions forwards new_body so corrected content reaches the domain revision path (accept + new_body still rejected)",
      "domain suite exit 0 / 9 groups ok incl. accept_with_new_body_is_rejected_and_body_stays_immutable, accept_commits_status_and_one_event_atomically, failed_review_leaves_no_partial_state, modified_creates_traceable_successor (receipt .project-local/runs/r02-domain.log)"
    ]
  },
  "R03": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "985a219 ARCHIVE-01: layout resolved from the manifest table set (v2 8-table, v3 11-table, v3 13-table, current 13-table); unknown v3 layout and current archive missing a table rejected; additive nullable columns filled NULL; unknown column rejected",
      "archive suite exit 0 / 7 groups ok (receipt .project-local/runs/r03-archive.log)",
      "GAP CLOSED: four NON-EMPTY v3 archives were exported by the OLD implementation in a scratch worktree (store+archive from 968c479, a2dbef5, 60b355a, 3d65609) and committed as fixtures crates/archeaxis-archive/tests/fixtures/v3-{ten,eleven,twelve,thirteen}-tables (each with PROVENANCE.txt); sources=1, knowledge=2, objects=1 per fixture",
      "v3 layout history verified from git: version 3 shipped 10, 11, 12 and 13-table export layouts; all four are now accepted by exact table set, v1 (7 tables, no workspace_meta) stays unsupported, and any other v3 set is rejected",
      "new test genuine_v3_era_archives_restore_with_content restores each fixture, asserts schema upgrade to 4, 2 knowledge rows with body bytes preserved and 1 source row",
      "archive suite exit 0 / 7 groups ok (receipt .project-local/runs/r03-archive-v2.log); full workspace cargo test exit 0 / 52 groups (receipt .project-local/runs/r03-full-workspace.log)",
      "environment pitfall recorded: a scratch worktree under the repo inherits the main .cargo/config.toml target-dir, which mixed v3/v4 artifacts in one build directory; the worktree wrapper now pins its own CARGO_TARGET_DIR"
    ]
  },
  "R04": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "fail-open hole fixed: Launch::from_stdin accepted any actor string and the middleware mapped every non-machine value to human, so an unknown role silently received human authority; from_stdin now rejects anything outside {human, machine} before a session or workspace exists",
      "real-process regression: unknown_launch_actor_is_rejected_and_never_grants_human_authority covers alien / Human / owner / empty / trailing-space values (none may start, none may create a workspace) and confirms actor=human still starts and serves /api/v1/system/version 200",
      "client-supplied x-archeaxis-actor continues to be overwritten by the launch-session claim (existing C02 coverage), so it cannot escalate or downgrade",
      "api suite exit 0 incl. launch_auth 6 tests (receipt .project-local/runs/r04-api.log)",
      "REMAINING: real host-signed and host-held machine/human credential scope is still to be implemented; that part is not claimed"
    ]
  },
  "R05": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "scheduler worker landed: services/python-workers/learning/worker_schedule.py reuses the existing donor LearningScheduler (not the ladder); stdin JSON -> stdout JSON; unusable requests fail closed with an explicit error object and never invent an interval",
      "finding: py-fsrs fuzzes intervals by default (Scheduler(enable_fuzzing=True)), so identical card+rating calls returned different due dates; the donor now takes an additive enable_fuzzing keyword (default True unchanged) and the worker requests False, giving reproducible intervals for replay/restart/audit",
      "tests: tests/test_worker_schedule.py 8 passed (mature-card interval exceeds the ladder's 14-day ceiling, rating sensitivity, determinism, correct->rating mapping, explicit errors incl. subprocess fail-closed) plus donor tests/test_learning_scheduler.py 6 passed - 14 passed / 0 failed",
      "Core-side adapter landed: crates/archeaxis-application/src/scheduler.rs exposes SchedulerClient (python from ARCHEAXIS_PYTHON + repository worker path), a subprocess JSON contract that validates authority == fsrs, and returns Unavailable/Rejected on failure - never a ladder interval",
      "integration tests 5 passed (receipt .project-local/runs/r07-scheduler-adapter.log): real Rust->Python->FSRS donor, mature card interval > the ladder's 14-day ceiling, reproducible identical requests, missing rating rejected, missing interpreter and missing worker script both reported as Unavailable",
      "keyed review path parametrized (R05b): ScheduleSource::{Ladder, Explicit(Option<i64>)} plus a private record_review_keyed_impl, with public record_review_scheduled(..., next_review_days: Option<i64>); None means the scheduler was unavailable, so the event is still recorded with its persistent key but next_review NULL, an explicit schedule:unavailable payload marker and the SCHEDULE_UNAVAILABLE (-2) sentinel - never a ladder interval; record_review_keyed keeps its previous Ladder behaviour",
      "EVENT-01 semantics preserved and re-asserted for the scheduled path: replay returns the original receipt (SCHEDULE_DUPLICATE -1) and writes nothing, same key with another item or payload conflicts, empty key is rejected",
      "tests: crates/archeaxis-domain/tests/learning_schedule_authority.rs (4 tests) - domain suite 10 groups ok (receipt .project-local/runs/r13-r05-domain.log); api suite 14 groups ok incl. learning_events_api and machine_correction_loop, no regression (receipt .project-local/runs/r13-r05-api.log)",
      "API wiring landed (R05c): LearningEventBody accepts optional schedule_state + now; the handler resolves the authority before writing - card state present -> SchedulerClient (reused FSRS worker), scheduler unusable -> record_review_scheduled(None) as an unscheduled review, no card state -> the placeholder ladder explicitly; every response now carries schedule_authority (fsrs / unavailable / placeholder_ladder) so a caller cannot mistake the ladder for real scheduling",
      "wire-level tests: crates/archeaxis-api/tests/learning_schedule_api.rs (3 tests) - card state yields fsrs with >14 days, no card state reports placeholder_ladder, unusable state yields unavailable with the -2 sentinel and a history row whose next_review is null; api suite 15 groups ok (receipt .project-local/runs/r15-r05-api.log)",
      "restart consistency asserted (R05d): schedule_survives_a_workspace_reopen - a scheduled review keeps its due date after reopening the workspace, an unscheduled review stays unscheduled, and a post-restart retry is still recognised as a replay with the same receipt",
      "full workspace re-run: cargo test --workspace --offline exit 0 / 55 groups ok (receipt .project-local/runs/r16-full-workspace.log), covering the new domain/api/application test files",
      "REMAINING (not claimed): product callers must always submit schedule_state; that caller-side integration belongs to the R10 host wiring, after which no product path reaches the placeholder ladder"
    ]
  },
  "R06": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "offline deliverable covered by regression: tests/test_x07_public_check_probe.py 13 passed - a radius sentence with object+unit+number is support; a diameter hit is recorded as weaker diameter_only and never as support; an unrelated same number (e.g. a year) yields none; model verdicts \u4e0d\u652f\u6301 / \u65e0\u6cd5\u5224\u65ad / empty / error can never become support; an unreachable source raises instead of silently becoming no-evidence",
      "cloud cross-check requires owner-configured and owner-approved endpoints and credentials (project default is zero paid budget): recorded as BLOCKED_EXTERNAL, no cloud verification is claimed",
      "REMAINING: live run (public retrieval plus local-model verdict) needs working outbound HTTPS (previously intermittent) and ollama (currently reachable on :11434) - queued as the next work unit",
      "LIVE RUN SUCCEEDED (round 18): real public retrieval of the Wikipedia Earth extract produced a locatable quote; classification was diameter_only (the 12,742 km mention is the diameter, not the radius) and the local qwen3:8b answered \u65e0\u6cd5\u5224\u65ad (source does not state the mean radius) which parsed to undeterminable with supported=false - the 6371 claim was correctly NOT marked supported. Receipt: .project-local/runs/r18-x07-live.log",
      "CLOUD SUB-ITEM: BLOCKED_EXTERNAL - cross-checking against a cloud endpoint needs owner-configured and owner-approved credentials; not performed and not claimed"
    ]
  },
  "R07": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "non-empty migration path proven on a synthetic legacy library (notes 3 rows, docs, attachments, links, learning_history 3 rows): existing stage_demo_semantic_import plus a new stage_legacy_learning_history that routes learning writes through archeaxis_domain so the Core stays the only learning-event authority",
      "fidelity rules: a POSITIVE legacy interval is preserved; a missing interval and a legacy 0 (due immediately, which vNext history cannot represent) are both recorded as unscheduled, and the counters match what is actually stored",
      "idempotency: deterministic persistent keys (legacy-table-rowid) mean a re-run replays receipts (3 replayed, 0 new) instead of accumulating history",
      "no dual-write: the legacy database is byte-identical after the migration (sha256 before/after)",
      "receipts: .project-local/runs/r19-r07-migration.log (5 groups ok), .project-local/runs/r19-full-workspace.log (56 groups ok after the new archeaxis-domain dependency)",
      "REMAINING (not claimed): the owner real private library was not provided, so real-library migration qualification is still open; attachments/links remain explicit loss rows rather than transported assets; legacy timestamps are not preserved (the persistent key stores the legacy table and row id only)"
    ]
  },
  "R08": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "native PDF route landed: services/python-workers/document/worker_pdf.py returns the same contract as the text worker (engine/engine_version/text/structure/loss_receipt), so both routes feed one ingestion path",
      "anchors carry page AND line (path [page-N, line-M]) plus character offsets into the projected text, so knowledge can navigate back to the page/region it came from",
      "explicit degradation, no silent success: a corrupt PDF and bytes a lenient engine accepts as a zero-page document both raise; a page with no extractable text (scan/screenshot) returns its real empty text with a loss note stating that OCR is required",
      "the receipt records engine, version, page coverage and losses only - no accuracy figure and no model confidence is presented as recognition accuracy",
      "tests: tests/test_worker_pdf_native.py 5 passed (numbers and units survive extraction, every page is addressable, no-text page degrades to an OCR note, corrupt file fails explicitly, CLI exits 1 without publishing text)",
      "REMAINING: scanned/screenshot OCR route through the same contract; the Rust-side unified job contract (the executor currently knows only the text worker); quality display; and the end-to-end import-to-retrievable-knowledge run for all three sample classes",
      "OCR route conformed (R08b): worker_ocr now emits the same structure anchors as the text and PDF routes - kind=region, path [region-N], character offsets into text, plus bbox - with anchor_summary coverage; per-word confidence is kept only as a review field and the receipt still contains no accuracy claim",
      "tests: tests/test_worker_ocr_route.py 2 passed against real Tesseract on a synthetic screenshot (text including 6371 recognised, region anchors carry bbox and offsets inside text, coverage reported, blank image does not claim recognition)",
      "ENVIRONMENT FACT: this shell TESSDATA_PREFIX points at a directory that does not exist, while the repository ships tools/tesseract/tessdata/eng.traineddata - passing tessdata_dir explicitly works, and the pre-existing tests/workers/test_bulk_ocr.py skips for the same reason",
      "GAP ANALYSIS (R08c, read-only this round): the unified job contract is blocked by three concrete pins - executor.rs hardcodes the python-worker-text-ndjson v1 identity; transport/text_ndjson.py accepts only capability text.extract v1; and the new PDF/OCR workers are plain CLIs without the sidecar NDJSON envelope. Next unit: generalize the transport capability set with per-route dispatch, add NDJSON mains to the pdf/ocr workers declaring their own capability/identity, widen the executor hello check to a controlled identity set (or a profile-declared identity), then run one job type over the three sample classes end to end",
      "UNIFIED JOB CONTRACT LANDED (R08d): transport/text_ndjson.py now dispatches a controlled ROUTES table (text.extract, pdf.extract, image.ocr) through one request/response envelope, one rejection vocabulary and one artifact triple (text + document_structure + loss_report); capability version, accepted media types and worker module are declared per route",
      "integration fix found by the new tests: staging inputs are content-addressed (no extension) while the OCR worker validates the file extension, so the transport now materialises a route-local view with the suffix implied by the declared media type, leaving the original input untouched and still hash-checked",
      "tests: tests/test_unified_job_contract.py (three routes on the shared envelope plus unknown-capability, wrong-version and per-route media-type rejections); combined route suites 21 passed; full Python suite run_tests.ps1 --full 2381 passed / 7 skipped / 0 failed (receipt .project-local/runs/r24-python-full.log)",
      "REMAINING for R08: (b) NDJSON mains for the pdf/ocr workers declaring their own capability and identity; (c) executor.rs hello check widened from the hardcoded python-worker-text-ndjson v1 to a controlled identity set or a profile-declared identity; (d) one job type over the three sample classes end to end into retrievable knowledge",
      "PDF ROUTE ON THE SIDECAR LOOP (R08e): transport gained a reusable serve_stdio(worker_name, capabilities, staging_root) that keeps handshake, request validation, response envelope and error vocabulary identical; the text worker now calls it unchanged, and worker_pdf.py declares its own identity python-worker-pdf-ndjson and capability pdf.extract via a --staging-root mode",
      "gap found and fixed in my own step (a): the route table is shared, so without an owner check a worker could serve a capability it never advertised; serve_stdio now rejects any capability outside the worker own advertised list (same unsupported-capability vocabulary, retryable false)",
      "tests: tests/test_pdf_sidecar_job.py 2 passed - a real subprocess handshake asserts the advertised identity/capabilities and a successful job returns the artifact triple with 6371 in the text artifact; a foreign capability is rejected with no outputs. Combined five suites 23 passed; full Python suite 2383 passed / 7 skipped / 0 failed (receipt .project-local/runs/r25-python-full.log)",
      "REMAINING for R08: the OCR worker NDJSON entry (same pattern); executor.rs hello check from hardcoded python-worker-text-ndjson v1 to a controlled identity set or profile-declared identity; then one job type over the three sample classes end to end into retrievable knowledge",
      "OCR ROUTE ON THE SIDECAR LOOP - step (b) complete (R08f): worker_ocr gains a --staging-root mode advertising python-worker-ocr-ndjson / image.ocr through the same serve_stdio; all three routes now share one job loop and envelope (text, pdf, image.ocr), with the CLI paths unchanged",
      "tests: tests/test_ocr_sidecar_job.py 2 passed on a real subprocess handshake and job (succeeded, artifact triple, real recognised text, region anchors, foreign capability rejected retryable=false); combined sidecar/contract suites 10 passed; CLI regression tests/test_worker_ocr_route.py 2 and tests/test_worker_pdf_native.py 5 passed",
      "REMAINING for R08: (c) executor.rs hello identity from the hardcoded python-worker-text-ndjson v1 to a controlled identity set or a profile-declared identity, keeping attempt/error/cancel semantics; (d) one job type over the three sample classes end to end into retrievable knowledge",
      "EXECUTOR IDENTITY GATE WIDENED (R08g): executor.rs now accepts a controlled KNOWN_WORKER_IDENTITIES set (text, pdf, ocr) with version 1 instead of pinning the text worker; a real executor test shows the PDF route identity is accepted (the failure no longer mentions unexpected worker identity) and the pre-existing real-job/restart/cancel tests stay green - application suite 7 groups ok, receipt .project-local/runs/r27-executor.log",
      "SECOND PIN FOUND (next unit): crates/archeaxis-sidecar-protocol/src/worker.rs hardcodes text.extract both in the request constructor (~line 86) and in the hello compatibility check (~line 97), so PDF/OCR stop at incompatible worker hello; the handshake must become capability-agnostic - the worker must advertise the requested capability, and the request capability must come from the job/route",
      "CAPABILITY DE-HARDCODING DONE - step (c) complete (R08h): Request gained a generic job(...) constructor taking the route capability (Request::text is now a thin wrapper), decode_hello became capability-agnostic (protocol shape, non-empty identity, unique non-empty capability/schema lists), and the executor checks where the request is known that the worker advertises the job capability, failing with an explicit worker does not advertise the requested capability message",
      "tests: full workspace cargo test --workspace --offline exit 0 / 56 groups with the new executor test proving a PDF worker handed the text route is refused for the capability reason (not a pinned identity, not a generic hello mismatch), and the pre-existing handshake/version/capability protocol test still green (receipt .project-local/runs/r28-full-workspace.log)",
      "REMAINING for R08 step (d): job kind to capability/worker mapping (the executor currently holds a single worker path), one job type over the three sample classes end to end into retrievable knowledge, and quality display",
      "ROUTE TABLE (R08i, step d part 1): attempts.rs declares ROUTES mapping job kind to capability and input media type (text/text.extract -> text.extract + text/plain, pdf -> pdf.extract + application/pdf, image -> image.ocr + image/png) with route_for_kind(); claim() no longer uses a text-only allow-list or a hardcoded text/plain media type, and an undeclared kind is refused at claim time leaving the job queued (never marked running)",
      "tests: tests/route_capabilities.rs 3 tests - every declared route selects its capability and media type, an unknown kind is refused without changing job state, and route lookup is explicit; application suite 8 groups ok (.project-local/runs/r29-app.log); full workspace cargo test exit 0 / 57 groups (.project-local/runs/r29-full-workspace.log)",
      "REMAINING for R08 step (d): the executor must select the route worker by job kind (it still holds a single worker path), then the three sample classes must run end to end into retrievable knowledge with quality display",
      "ROUTE REGISTRY IN THE EXECUTOR (R08j, verified part of step d): Executor::open_routes registers capability -> worker (open() stays compatible as the text-only case); execute_owned dispatches by the claimed request capability and refuses with no worker registered for capability when none matches, instead of sending the job to the wrong process",
      "launch policy fixed: stdlib-only text routes keep the hardened -B -S launch, while engine-backed routes (PDF/OCR) must keep the interpreter site-packages - with -S the PDF engine import failed as unavailable, which the end-to-end attempt exposed",
      "tests: full workspace cargo test exit 0 / 58 groups with a_job_without_a_registered_route_worker_fails_explicitly and registering_a_route_makes_its_capability_dispatchable; PDF worker and unified-contract pytest suites still green (receipt .project-local/runs/r30-full-workspace.log)",
      "REMAINING for R08 (recorded, not claimed): a real PDF job still stops at the Core receipt validation, which is correct behaviour - the receipt must supply covered/total/coverage together, and structure/coverage must match the projected text so anchors must be derived from the FINISHED text (page separators otherwise shift offsets and line counts). Both are PDF-worker fixes to be committed together with the end-to-end job test; the attempted rewrite was reverted rather than committed unverified",
      "PDF END-TO-END STATUS (R08k, investigation only, no code kept): the first Core rejection was cleared by giving the receipt line-based covered/total/coverage and deriving anchors from the FINISHED text, but the job is still refused with invalid receipt: structure or coverage does not match projected text. The exact rule has not been read yet, so the attempt was reverted instead of guessed at; the worktree is back at the last verified commit (3a0662d, 0 dirty files).",
      "NEXT UNIT FIRST STEP: read the validator that raises structure or coverage does not match projected text (attempts::finish or the jobs receipt validation), implement the worker against its exact predicate (for example covered == len(structure) and the last anchor char_end == len(text)), then add the PDF end-to-end job test together with the fix",
      "EXACT CORE RECEIPT PREDICATE FOUND (R08l, read-only): attempts.rs lines 104-106 is text-route specific - it requires loss.engine == python-worker-text and engine_version == 0.1.0, requires structure to equal the canonical line anchors the Core recomputes from the text (kind line, path [line-N]), and requires loss.covered == structure.len() with loss.total == expected line count. That is why the PDF job is refused: the PDF worker reports engine pymupdf-native-pdf and page-qualified paths [page-N, line-M].",
      "NEXT UNIT DECISION: (A) generalise the Core validator - a controlled set of route engine identities plus span/kind-based structure comparison that accepts a page prefix in path (recommended, keeps page anchors and true engine identity); (B) make PDF/OCR receipts impersonate the text engine and [line-N] paths (not recommended: loses page semantics and fabricates engine identity)",
      "ENGINE GATE IS ROUTE-AWARE NOW (R08m, first half of option A): attempts.rs declares ENGINE_PROFILES (python-worker-text/0.1.0, pymupdf-native-pdf/pymupdf, python-worker-ocr/0.1.0) and the receipt check accepts any declared route engine whose version matches, instead of demanding the text engine from every route. Text behaviour is unchanged.",
      "tests: application suite 9 groups ok and full workspace cargo test exit 0 / 58 groups (receipts .project-local/runs/r34-app.log, r34-full-workspace.log)",
      "STILL BLOCKED (second half of option A): structure must still equal the canonical line anchors the Core recomputes from the text, so page-qualified paths [page-N, line-M] are still rejected; the comparison needs to become kind+span based, tolerating a page prefix. Until then no PDF end-to-end test is committed and no PDF job is claimed to work.",
      "CORE VALIDATOR FULLY ROUTE-AWARE (R08n, option A complete): same_spans compares anchors by kind, character span and FINAL path segment, so a page-qualified anchor [page-3, line-12] is equivalent to the canonical [line-12] while a wrong line or span is still refused. Together with the route engine set this makes all three conditions of attempts.rs route-aware.",
      "tests: application suite 10 groups ok with the new receipt_spans.rs (page-qualified anchor accepted and the real engine pymupdf-native-pdf recorded; wrong final segment refused; undeclared engine refused) and full workspace cargo test exit 0 / 59 groups (receipts .project-local/runs/r35-app.log, r35-full-workspace.log)",
      "LAST STEP FOR R08 (d), worker side: the committed PDF worker still omits coverage and counts coverage in pages, and its anchors must be derived from the finished text - exactly the two worker fixes written and reverted in rounds 30/31. The Core now accepts them, so the next unit re-lands those worker fixes together with the PDF end-to-end job test.",
      "PDF JOB END TO END WORKS (R08o): a kind=pdf job selects pdf.extract, dispatches to the PDF worker (launched with -B and site-packages preserved), passes the Core receipt validation, and its text is stored in job_outputs (3 artifacts, text contains 6371) with jobs.engine recording the real route engine pymupdf-native-pdf. The two worker fixes re-landed here are the ones the Core now accepts: line-based covered/total/coverage supplied together, and anchors derived from the FINISHED text so page separators cannot shift offsets or line counts.",
      "tests: application suite 10 groups ok including pdf_job_is_dispatched_to_the_pdf_worker_and_its_text_is_stored; full workspace cargo test exit 0 / 59 groups; pytest 13 passed (PDF native 5, unified contract 6, OCR route 2) - receipts .project-local/runs/r36-app.log and r36-full-workspace.log",
      "R08 STILL IN PROGRESS: the OCR route has no end-to-end job test yet (same machinery, expected to work by symmetry but not claimed), and retrieval of the extracted knowledge plus the quality display remain",
      "OCR END-TO-END CONFLICT IDENTIFIED (R08p, read-only this round): the OCR worker emits word/region anchors (kind=region) and carries only an anchor_summary, while the Core requires loss.covered == structure.len(), loss.total == the expected line count, and same_spans(structure, the canonical LINE anchors). Region anchors can therefore never equal the canonical line projection, and the Core accepts exactly three outputs, so regions cannot move to a fourth artifact.",
      "DECISION PROPOSED (A, recommended): keep the Core projection contract line-based - the OCR structure becomes canonical line anchors (same shape as text and PDF, satisfying same_spans) with covered/total/coverage in line units, and the word/region boxes are demoted to review metadata inside loss_report.params.regions (still three artifacts). Option (B), generalising the Core to accept region granularity, is weaker and expands the Core surface; it is not recommended.",
      "OCR JOB END TO END WORKS - all three routes unified (R08q): option (A) implemented - the OCR structure artifact is now the canonical line anchors of the recognised text with covered/total/coverage in line units, and OCR word boxes are demoted to review metadata in loss_receipt.params.regions (still three artifacts, Core validation not weakened, region information preserved).",
      "real defect fixed on the way: a Windows extended path (\\\\?\\...) produced by canonicalize() reached tesseract through the transport and it could not open its language data; the transport now strips that prefix before calling OCR.",
      "tests: application suite 11 groups ok including ocr_job_is_dispatched_to_the_ocr_worker_and_its_text_is_stored (succeeded, three artifacts, empty-text check, capability image.ocr with media image/png in the request, engine python-worker-ocr recorded, structure contains line-1); full workspace cargo test exit 0 / 60 groups; pytest 17 passed across the OCR/PDF/contract suites (receipts .project-local/runs/r38-app.log and r38-full-workspace.log)",
      "ROUTE STATUS: text.extract (pre-existing), pdf.extract (R08o), image.ocr (R08q) all run through one job contract. R08 STILL IN PROGRESS: retrieval of the extracted text as searchable knowledge and the quality display are not done",
      "RETRIEVAL STATUS AND DECISION (R08r, read-only): job completion already persists the extracted text as a transforms row (source_id, engine, text, loss_note) with jobs.transform_id linking them, so the text is stored with its original-file provenance, engine and loss note. However the retrieval\u51fa\u53e3 (GET /api/v1/search) still covers only knowledge, so extracted text is not yet searchable.",
      "DECISION PROPOSED (a, recommended): extend the search\u51fa\u53e3 to cover transforms.text with source/engine/loss attribution, without auto-converting extracted text into knowledge (type, evidence and qualification semantics belong after R09). Option (b), auto-creating knowledge candidates on completion, is heavier and should wait. The quality display that accompanies this reports facts only - coverage, loss count, engine, region count - and never an accuracy figure or a confidence-as-accuracy claim.",
      "EXTRACTED TEXT IS RETRIEVABLE (R08s, decision (a) implemented): search.rs gained a transform_fts index created on demand (same pattern as knowledge_fts, no schema-version bump) with reindex_transforms and search_transforms returning transform_id/source_id/engine/head; GET /api/v1/search now returns transforms and transform_count alongside the unchanged items array.",
      "semantic boundary kept explicit: this is retrieval of extracted text with original-file and engine attribution, NOT a claim that the text is knowledge - a test asserts the knowledge index does not start reporting extracted text, and terms absent from extracted text return nothing.",
      "tests: domain suite 11 groups ok (search_transforms.rs), api suite 15 groups ok, full workspace cargo test exit 0 / 61 groups (receipts .project-local/runs/r40-domain.log, r40-api.log, r40-full-workspace.log)",
      "R08 REMAINING: the quality display (facts only - coverage, loss count, engine, region count; never an accuracy figure). Auto-creating knowledge candidates on completion stays deferred until after R09 per the earlier decision.",
      "QUALITY VIEW LANDED (R08t): GET /api/v1/jobs/:job_id/quality returns facts only - state, engine, engine_version, coverage, covered, total, loss_count, region_count, pages - plus a note stating that model or recogniser confidence is not accuracy. The response has no accuracy key at all, an unknown job is 404, and a queued job reports zero counts with null coverage instead of inventing a coverage figure.",
      "tests: api suite 16 groups ok with job_quality.rs (unknown job 404; queued job reports facts with no accuracy key and the confidence-is-not-accuracy note) and full workspace cargo test exit 0 / 62 groups (receipts .project-local/runs/r41-api.log and r41-full-workspace.log)",
      "R08 SUMMARY: all three routes run end to end through one job contract, extracted text is retrievable with source and engine attribution, and the quality view is fact-only. The remaining idea (auto-creating knowledge candidates when a job completes) stays deferred until after R09 by the earlier decision.",
      "STATUS CHANGE: R08 is now IMPLEMENTED_PENDING_AUDIT. Non-empty synthetic samples for all three routes (text, native PDF, image OCR) import and run end to end through one job contract, their text is retrievable with source and engine attribution, and the quality view is fact-only. The one element deliberately deferred is converting extracted text into knowledge candidates, which waits for the type/evidence/qualification semantics of R09 and later (recorded decision (a) in R08r)."
    ]
  },
  "R09": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "scoping (read-only): the root gap is that learning records and knowledge revisions have no link at all. learning_events holds only (item_key, kind, outcome, next_review, event_id) and learning_event_keys only event_id - no knowledge_id or revision - and archeaxis-domain/src/learning.rs never mentions knowledge. R09 cannot express a card or learning record that references a revision, so the required version checks at consumption entries have no carrier yet.",
      "ALREADY AVAILABLE from earlier slices: search active_only filtering, the qualification endpoint, superseded-not-current and the revision chain, and extracted text retrievable with source attribution.",
      "DECISION PROPOSED (A, recommended, additive and history-preserving): add a card_references table (item_key, knowledge_id, created_event_id) - or a nullable knowledge_id on learning_events - recording the referenced revision when a card or learning item is created; read-side entries (history, questions, machine context, result write-back) annotate knowledge_active / superseded / revision_changed against current qualification; old learning history is always preserved and only annotated, never rewritten, and no claim is made that an already-issued external answer was recalled. This needs a schema bump (v4 to v5) using the guarded migration pattern from EVENT-01. Option (B), annotating only from the knowledge side, cannot answer which cards referenced a revoked revision and is not recommended.",
      "CARRIER LANDED (R09b, decision A first slice, deliberately without a schema-version bump): learning.rs now provides card_references(item_key, knowledge_id, created_event_id, referenced_at) created on demand exactly like the FTS indexes, so SCHEMA_VERSION is untouched and no archive-layout cascade is triggered. record_card_reference is idempotent (INSERT OR IGNORE), refuses empty keys and revisions that do not exist; references_for_card returns (knowledge_id, active) annotating whether the referenced revision is still current via is_knowledge_active.",
      "semantics: a reference is never rewritten - after the revision is superseded the historical link stays and is merely annotated as not current, which is what R09 asks for (old learning history preserved and annotated, no claim of recalling already-issued external answers).",
      "tests: domain suite 12 groups ok with card_references.rs (supersede keeps the reference but reports active=false; re-recording is idempotent; unknown revision and empty keys refused; a reopened workspace keeps both the reference and its validity); full workspace cargo test exit 0 / 63 groups (receipts .project-local/runs/r43-domain.log and r43-full-workspace.log)",
      "RECORDED LIMITATION: the table is created on demand and is not in EXPORT_TABLES, so archives do not carry card references yet. REMAINING for R09: wire the annotation into the consumption entries (history, questions, machine context reads, and result write-back version checks)",
      "CONSUMPTION ENTRY WIRED (R09c): POST /api/v1/learning/items/:item_key/references records the revision a learning item was built from (immutable once recorded; an unknown revision returns 400), and GET /api/v1/learning/events/:item_key now returns references as [{knowledge_id, active}] using is_knowledge_active to annotate whether that revision is still current.",
      "HTTP-level regression (card_reference_api.rs): a recorded reference reports active=true; after a modified review and acceptance of the successor, re-reading the same item still names the ORIGINAL revision and reports active=false - the history was not rewritten and no claim is made that an already-issued answer was recalled; an unknown revision is refused with 400.",
      "tests: api suite 17 groups ok, full workspace cargo test exit 0 / 64 groups (receipts .project-local/runs/r44-api.log and r44-full-workspace.log). Known cosmetic issue: one unused mut warning in the new test, no effect on results.",
      "REMAINING for R09: version checks for machine-context reads and for result write-back, plus filtering stale entries when an asynchronous index lags behind a revision",
      "REMAINING ITEMS RESOLVED BY DESIGN OR SCOPED (R09d, read-only check): (1) stale-index filtering cannot happen here because both indexes are rebuilt per query and every row is annotated with is_knowledge_active, with active_only applied server-side - a future asynchronous index must keep that per-row check (recorded as a constraint); (2) result write-back has no knowledge-revision dimension today because jobs produce extracted text (transforms) and not knowledge, so the check becomes relevant only once job completion creates knowledge candidates - which the earlier decision (a) deferred past R09; (3) there is no separate machine-context endpoint yet, and any new one must reuse the same validity check.",
      "STATUS: R09 is IMPLEMENTED_PENDING_AUDIT. Its carrier (card_references with current-validity annotation), the consumption entry (reference recording plus annotated history reads) and restart consistency are implemented and tested; the candidate-creation plus write-back version check is recorded as a dependent follow-up slice."
    ]
  },
  "R10": {
    "status": "IN_PROGRESS",
    "evidence": [
      "DeepTutor local host restarted as a managed background job (deeptutor start with HOME/XDG_ pointing at .project-local/deeptutor-val): backend :8001, frontend :3782 and ollama :11434 all HTTP 200; the frontend log reports Next.js 16.2.3 and Ready in 0ms (log .project-local/deeptutor-val/start-round46.log)",
      "the Core-side real journey already exists as crates/archeaxis-api/tests/v01_journey.rs::v01_twelve_step_journey, which keeps passing in the full workspace run",
      "R10 GAPS, not claimed: (a) a host-to-Core adapter so the DeepTutor surface and capabilities read/write the Core through a controlled interface instead of separate stores; (b) a human journey over the real UI (import, reading position, explanation, practice or teach-back, correction, review, resume); (c) user learning state and AI capability state displayed separately; (d) when activity results cannot flow back, show that explicitly and allow manual recording",
      "next unit: build the adapter as the smallest slice (a controlled process or interface mapping host-side input to Core API calls: import, search, learning events, correction), then the UI journey and the two-state separation",
      "ADAPTER FIRST SLICE (R10b): shared/core_client.py lets the host side address the Core only through the Core API - it builds and issues import (base64), search (optional active_only, URL-encoded query), learning event (mandatory persistent client_event_id, schedule_state passed as data so the reused FSRS worker decides), item-to-revision reference, and human review (modified carries new_body and never overwrites the existing body) requests. It never opens the Core database.",
      "boundaries pinned by tests: authentication travels only in the x-archeaxis-launch-token header, no request body ever carries an actor or token (identity is a server-side claim), an unavailable Core returns status 0 with an explicit error instead of raising (a real gap found and fixed this round: only HTTPError was caught, so a refused connection crashed the caller).",
      "tests: pytest tests/test_core_client.py 8 passed; architecture guard passed (receipt .project-local/runs recorded in the ledger row R10b)",
      "REMAINING for R10: connect this client to the real host process/UI, then the two-state display (user learning state vs AI capability state) and manual recording when activity results cannot flow back",
      "JOURNEY SEQUENCER (R10c): core_client.run_journey orders the host-side journey in one place - reachability, import, search with active_only=true, learning event, then recording the revision the item was built from. A failed step stops the journey and reports failed_step instead of skipping silently, and the journey never performs a human review itself (its note states that review stays a human action). The call function is injected, so the order is testable without a running Core.",
      "tests: pytest tests/test_core_client.py 11 passed (journey order with no review call, failure stops later steps, search carries active_only=true)",
      "SUPERSEDED by the host wiring entries below: REMAINING for R10: a smoke run against a real running Core (spawn archeaxis-api with launch JSON and read its ready port), then the real host UI wiring, the separated display of user learning state versus AI capability state, and manual recording when activity results cannot flow back",
      "LIVE CORE SMOKE PASSED (R10d): scripts/probes/r10_core_journey_smoke.py spawns the REAL archeaxis-api with a launch JSON on stdin, reads its readiness port, and runs shared/core_client.run_journey over HTTP. Receipt (exit 0): reachability 200, import 202 (source_id src_3f83059f699322666aa2fd3d), search q=6371 active_only=true 200 with count 1, learning event 201, reference 201 citing revision k_8f390d95872e39a2e2bb110b; one accepted fact was created first (201). No token is printed.",
      "real protocol detail found and fixed: the Core reads its launch claim to EOF, so the caller must close stdin after writing it or the process waits and never reports readiness.",
      "SUPERSEDED by the host wiring entries below: REMAINING for R10: connect the adapter to the real host UI, show user learning state separately from AI capability state, and surface plus manually record activity results that cannot flow back",
      "TWO-STATE VIEW LANDED (R10e, acceptance items 2 and 3): GET /api/v1/learning/items/:item_key/state reports the learner side (event_count, correct_streak, scheduled_events, unscheduled_events, references with active flags, plus a recording note) separately from the machine side, which returns status not_recorded with the note that machine capability receipts come from the machine loop and learner progress is never presented as machine competence.",
      "the same tests cover both recording paths: no card state uses the placeholder ladder and counts as scheduled, while an unavailable scheduler is stored as unscheduled - which is exactly the manual-recording route for an activity result that could not flow back automatically.",
      "tests: api suite 18 groups ok with item_state_api.rs; full workspace cargo test exit 0 / 65 groups (receipts .project-local/runs/r50-api.log and r50-full-workspace.log)",
      "REMAINING for R10: connect the journey and this state view to the real DeepTutor host UI",
      "HOST WIRING BOUNDARY (round 66): docs/integrations/DEEPTUTOR_PRODUCT_BASE.md records DeepTutor v1.5.17 as an immutable external dependency (external library path, not committed, no upstream patch applied) and states that branding, route mounting and authority-adapter injection are project-side integration work. The DeepTutor UI is therefore NOT modified; what is delivered is the project-side mountable surface.",
      "HOST PANEL RAW (commit e442f56): scripts/host/journey_panel.py serves loopback only - GET / renders one page with two separate panels, GET /api/state?item_key= returns the learner reading and the machine reading as the Core reports them, GET /api/health reports reachability. It reads the Core through shared/core_client.py and takes the /api/v1 prefix from the adapter's own BASE constant instead of duplicating it.",
      "HOST PANEL HONESTY RULES: an unreachable Core or any non-200 status returns HTTP 503 with learner and machine explicitly null plus a page banner, never zeros that look like real progress; the two panels are separate readings of one Core and learner progress is never rendered as machine competence; the launch token is read from an environment variable named by --token-env, never enters the page, is never accepted from a query string, and the access log drops everything after '?' so a token cannot land in a log.",
      "HOST PANEL MEASURED (round 66 real end-to-end receipt, scripts/probes/r10_host_panel_smoke.py exits 0): a real archeaxis-api process reached readiness, the real journey returned steps 200/202/200/200 (reachability, import, search, learning_event, with references empty and reported as such because no accepted revision existed), the page returned 200 with both panels, health returned 200 with reachable true, state returned 200 with learner event_count 1 and machine not_recorded, and pointing the panel at a closed port returned 503 with learner and machine null and the real connection-refused reason. tests/test_host_journey_panel.py 9 passed; the architecture, path, language boundary and format matrix checks all exit 0; the convention check still reports only the two frozen-package files missing a final newline.",
      "TWO REAL DEFECTS the smoke and the tests caught in this slice: (1) the panel first called /system/version without the Core's /api/v1 prefix, so the Core answered 404 and the panel - by design - refused to render state instead of showing zeros; (2) urllib.parse.quote leaves '/' unescaped by default, so an item key containing a slash would have added a path segment; it is now quote(item_key, safe='').",
      "NOT CLAIMED for host wiring: the DeepTutor UI, its branding and its route mounting are unchanged, because the upstream body is immutable and unpatched - only a project-side URL is provided. GitHub Actions was not run here.",
      "R10 STATUS AFTER ROUND 66: the adapter, the journey order, the two-state view and a mountable project-side panel all exist and are verified end to end against a real Core. What remains is not repository code: the DeepTutor body is an immutable external dependency with no upstream patch, so mounting this URL inside its navigation is either an owner-side configuration of that external installation or a recorded upstream patch with source commit, patch hash, license/NOTICE impact and rollback - which this repository does not do unilaterally. Until that choice is made, R10 stays IN_PROGRESS rather than being called complete, and the panel is reachable at its own loopback URL."
    ]
  },
  "R11": {
    "status": "IMPLEMENTED_PENDING_AUDIT",
    "evidence": [
      "scoping (read-only): what already exists on the machine side - the qualification endpoint (GET /knowledge-items/:id/qualification), is_knowledge_active and knowledge_status for consumer-side checks, search with active_only, the machine identity boundary (a machine may only propose candidates and can neither self-accept nor review), and the machine correction loop from cbe253b (candidate, human review, new version, re-call, restart-consistent).",
      "what is missing: there is NO MCP or tool-call surface anywhere in the Rust crates (a search for mcp/tool_call matches nothing), so there is no path for a real external client or controlled tool call, and no place to record a machine task receipt (task conditions, knowledge/method/tool/model versions, call scope, outcome, failure, retest).",
      "SMALLEST HONEST SLICE PROPOSED: (1) add a machine task receipt as an additive table created on demand (no schema-version bump, the same approach as card_references), writable by a machine principal only, with candidate-or-measurement semantics that never auto-accept; fields cover task conditions, knowledge/method/tool/model versions, scope, outcome, failure and retest; (2) connect one real external client (a controlled tool or MCP equivalent) using the R10 adapter pattern to run one observable task; (3) keep evaluation practice isolated from unseen tests, mark unmeasured as UNMEASURED, and never present a knowledge update, a fetched document or a single correct answer as weight training.",
      "MACHINE TASK RECEIPTS LANDED (R11b): archeaxis-domain/src/machine.rs adds a machine_tasks table created on demand (no schema-version bump) with record_machine_task recording conditions, knowledge/method/tool/model versions, scope, outcome, failure and retest_of, plus machine_task and machine_task_counts (total, unmeasured).",
      "deliberate refusals, each tested: only a machine principal may write (a human outcome belongs in human-owned learning records); the outcome must be succeeded, failed or unmeasured, so an unmeasured result stays visible instead of being rounded up to success; a failed task must state its reason and a succeeded task may not carry one; and a receipt is immutable - the same task id cannot be rewritten after the fact.",
      "tests: domain suite 13 groups ok with machine_tasks.rs (versions and scope recorded, unmeasured counted separately from verified, failure requires a reason, and non-machine principal / unknown outcome / success-with-failure / duplicate task id all refused without landing); full workspace cargo test exit 0 / 66 groups (receipts .project-local/runs/r53-domain.log and r53-full-workspace.log)",
      "RECORDED LIMITATIONS: created on demand, so not in EXPORT_TABLES (archives do not carry machine receipts yet); and these receipts are task/measurement facts only - they never claim that model weights were trained. REMAINING for R11: one real external client running an observable task through the R10 adapter pattern",
      "MACHINE RECEIPTS EXPOSED OVER THE API (R11c): POST /api/v1/machine/tasks records a receipt and is writable by a machine principal only - a human principal gets 403 and nothing lands; GET /api/v1/machine/tasks/:task_id is readable by any principal because a receipt is evidence about the machine side and grants no authority, and its readback states that a task receipt is a measurement fact rather than a claim that weights were trained.",
      "refusals re-checked at the API level: a failure without a reason is 400, re-recording the same task id is 400 so a receipt cannot be rewritten, and an unmeasured outcome is preserved verbatim instead of being rolled up as success.",
      "tests: api suite 19 groups ok with machine_task_api.rs; full workspace cargo test exit 0 / 67 groups (receipts .project-local/runs/r55-api.log and r55-full-workspace.log). Also fixed this round: the api crate was missing the machine module import (compile error E0433).",
      "REMAINING for R11: one real external client running an observable task through the R10 adapter pattern and recording its receipt here",
      "LIVE MACHINE RECEIPT ROUND-TRIP (R11d): scripts/probes/r11_machine_receipt_smoke.py launches the REAL archeaxis-api with a machine launch claim (the claim is the identity; a client header cannot change it), writes a receipt (POST /machine/tasks -> 201) and reads it back (GET /machine/tasks/:id -> 200) with conditions, knowledge/method/tool/model versions, scope and outcome, plus the note that a task receipt is a measurement fact rather than a claim that weights were trained.",
      "R11 acceptance mapping: a real process request with versions and result (this smoke), failure cases (a failed receipt must state its reason, refused at both domain and API level), a retest field, the machine being unable to self-grant human acceptance (403 guards plus tests), and knowledge/policy updates kept distinct from weight training.",
      "STATUS: R11 is IMPLEMENTED_PENDING_AUDIT. NOT CLAIMED: evaluation of capability improvement on independent unseen examples, and a real MCP client (the current client is a controlled HTTP client through the R10 adapter); both are recorded as follow-up work."
    ]
  },
  "R12": {
    "status": "IN_PROGRESS",
    "evidence": [
      "read-only terminal capacity proof (no deletions this round): the same tool and scope report 27.469 GiB observed logical across 152,230 files (26 observation errors, 76 opaque exclusions). Largest residual groups: .project-local 20.641 GiB / 101,176 files, target 5.729 GiB / 19,949, .venv 0.858 GiB, frontend 0.099 GiB, data 0.094 GiB.",
      ".hermes fingerprinted separately: 42.853 GiB / 718,077 files with the newest write still 2026-09-06 11:29, i.e. zero growth. Volume state: D: free 227.29 GiB.",
      "same-tool comparison with the R01 census: total 23.423 -> 27.469 GiB and files 130,120 -> 152,230, with .project-local 16.595 -> 20.641 GiB (+4.05 GiB). The whole increase is this session real build, test and evidence output, all inside the governed location (cargo output sealed under .project-local/build/cargo, receipts under .project-local/runs); the root target/ stays 5.729 GiB, so the growth stop still holds; D: free moved 231.15 -> 227.29 GiB, consistent with that increase.",
      "receipt: .project-local/runs/r12-census.json (the same tool output). CLASSIFICATION WITHOUT DELETION: regenerable candidates are .project-local/build (cargo output), the legacy root target at 5.729 GiB, .venv at 0.858 GiB and .project-local/cache; preserved are .project-local/runs receipts, docs, the taskpacks and .hermes (opaque - unknown stays unknown). Removing a regenerable cache still requires per-path authorization, and .hermes content was neither read nor deleted.",
      "CLEANUP MANIFEST PROPOSED (R12 second step, docs only, nothing deleted): R12-CLEANUP-MANIFEST.md measures each candidate with the same tool - .project-local/build 15.464 GiB (52,788 files, sealed cargo output), root target 5.729 GiB (legacy pre-seal output, frozen), .project-local/cache 1.148 GiB, .venv 0.859 GiB, .project-local/inventory 0.404 GiB (two full file listings).",
      "recommendations recorded rather than executed: the two clearly inert items (root target and cache) are 6.877 GiB and are safe; .venv is regenerable but every verification run depends on it, so deleting it now would block the remaining slices; the inventory files.jsonl bodies are the only part of .project-local/inventory worth dropping. Preserved: .project-local/runs receipts (3.500 GiB, every ledger citation lives there), docs/taskpacks, .project-local/deeptutor-val (0.077 GiB, the running host), data (0.094 GiB), and .hermes (42.853 GiB, opaque, never blanket-deleted).",
      "the manifest also records the post-deletion checks R12 requires: same-scope census with logical and volume deltas reported separately, one real build plus test to confirm nothing needed was removed, and a re-measure confirming .hermes and the tracked tree are untouched. Each path still needs its own authorization before any deletion."
    ]
  },
  "R13": {
    "status": "IN_PROGRESS",
    "evidence": [
      "FIRST STEP (R13, no deletions): scripts/launch/core_launch.py provides --check (dependency diagnostics for the core binary, the Python interpreter, the three workers, the pdf engine pymupdf, the tesseract executable, the repository eng.traineddata and the cargo wrapper, plus port diagnostics for 8001/3782/11434), --probe (spawn the Core with a fresh launch claim, read the readiness port, then stop it) and a foreground mode that prints the listening address.",
      "observed this round: --check exit 0 with 9/9 dependencies ok and all three ports reported in use (the host is running); --probe exit 0 with requested_port 64436 and ready_port 64436, so a real process started and reported readiness.",
      "behaviour on failure: a missing or unbuilt dependency is reported by name with an exit code, never as an empty success, and every path is resolved from the repository root rather than inferred from an installer directory.",
      "NOT CLAIMED: the Windows candidate package itself, a same-source artifact hash bound to a tested SHA, and the one-click stop/restore/uninstall instructions (R13 follow-ups). R12 deletions remain unauthorized per path, so nothing was deleted this round.",
      "SECOND STEP RAW (R13, commits c77bff5 and 100978c): core_launch.py gained --stop, --backup, --restore and --manifest. --stop only terminates the pid it recorded and refuses (exit 4) a pid whose image is not archeaxis-api, so a recycled pid is never killed; no recorded session and a gone process are idempotent no-ops. --backup uses SQLite VACUUM INTO, opens the source with mode=ro, and records sha256 plus write-ahead-log facts before and after, so source_unchanged is a measurement rather than an assumption; it refuses (exit 5) while a launch session is recorded as running and (exit 6) when the database does not exist. --restore moves the database it replaces aside together with its -wal and -shm journals, refuses (exit 7) a backup that is unreadable or whose sha256 sidecar disagrees, and asserts the restored file hashes to the backup. --manifest binds each artifact's bytes and sha256 to tested_source_sha and refuses (exit 8) to describe a commit while tracked files are dirty; untracked paths are recorded as a limitation and never change the sha.",
      "SECOND STEP MEASURED (real receipts, tested_source_sha 100978c): --backup on the real Core database (118784 B plus an 8272 B uncheckpointed write-ahead log) reported opened_read_only true, source_unchanged true, wal_before equal to wal_after, backup sha256 f0c00376c7423f87babd93e2a8d561e6096f7d7de9765540cacab80a3a881298; --restore --from that backup reported restored true, sidecar_matched true, restored_sha256 equal to backup_sha256, and preserved the replaced database plus its journals at core.sqlite.replaced-20260910T165851Z; --probe then reported ok true on the restored database, so a real Core process still starts and serves from it; --manifest wrote two artifact hashes with written true, and with one tracked file modified it exited 8 and produced no file; --check remained 9/9 dependencies ok; pytest tests/test_core_launch.py 18 passed and tests/test_core_client.py brought the pair to 30 passed; the architecture guard passed and the convention check still reports only the two frozen-package files missing a final newline.",
      "TWO REAL DEFECTS FOUND AND FIXED by running the receipts: (1) the backup originally opened the source read-write, which checkpointed its write-ahead log and made source_unchanged false on a real database - it now opens mode=ro and the same measurement comes back true without touching the journals; (2) the restore originally replaced only the main file and left the old -wal and -shm beside it, which would have let SQLite replay the replaced database's journal into the restored file - the journals are now moved aside with the database they belong to and nothing is deleted.",
      "STILL NOT CLAIMED for R13: an MSI/EXE installer, code signing and an uninstaller; start-up and restart-recovery on a clean machine without this toolchain; and the end-user import-understand-practise-review plus machine-call-correction-recall journey, which belongs to R10 host wiring. R12 deletions remain unauthorized per path, so nothing was deleted this round either."
    ]
  },
  "R14": {
    "status": "TODO",
    "evidence": []
  },
  "R15": {
    "status": "IN_PROGRESS",
    "evidence": [
      "FIRST STEP RAW (R15, commit 7d353fb): scripts/check_language_boundaries.py checks the boundary stated in docs/LANGUAGE_BOUNDARY_AUTHORITY_INDEX.md against the real tree instead of the prose. Ownership: an SQL driver may only be declared by crates/ (a Cargo.toml outside crates/ declaring rusqlite, sqlx, libsqlite3-sys, diesel or sqlite fails); services/python-workers/** may not import a database driver or mention rusqlite; apps/ArcheAxis.Desktop/** may not reference an SQL client package or use Sqlite from code.",
      "FIRST STEP RAW, second half: interoperability. Every archeaxis.worker-*/vN envelope literal in Rust, Python and the JSON schemas must carry the same major; that major must equal PROTOCOL_VERSION in crates/archeaxis-sidecar-protocol/src/lib.rs, the version directory under packages/contracts, the major the Python hello advertises, and the major const in the worker-protocol schema. The expected version is derived from the artefacts themselves, so the checker holds no magic number.",
      "WIRING RAW: scripts/ci/check_vnext_contracts.py - the script the CI contracts-vnext job actually runs - now calls the boundary check and prefixes its failures. Its docstring used to defer semantic cross-language consistency to a later freeze; that deferral is gone because the check now exists. The checker module is loaded from the script's own location while the tree under test is passed in, so the two can never be confused.",
      "FIRST STEP MEASURED (real repository, tested_source_sha 7d353fb): exit 0 with protocol_version 1, rust/python/schemas majors all [1], python_hello_major 1, schema_hello_major 1, and envelope names hello/request/response. The combined gate scripts/ci/check_vnext_contracts.py printed 'structure ok and the language boundary agrees on protocol major 1' and exited 0.",
      "NEGATIVE CASES ON THE REAL TREE (not only synthetic): adding services/python-workers/document/tmp_boundary_violation.py containing 'import sqlite3' made the checker exit 1 naming that file; removing it returned to exit 0. Changing only the Rust PROTOCOL_VERSION to 2 made it exit 1 with 'packages/contracts/v1 disagrees with PROTOCOL_VERSION=2' and 'PROTOCOL_VERSION=2 but the envelope literals are [1]'; git checkout restored the file and the checker returned to exit 0. The combined gate failed and recovered the same way.",
      "SUITES: pytest tests/test_language_boundaries.py 16 passed, of which 12 are synthetic trees that break one rule each (SQL driver outside crates/, worker importing a driver, worker mentioning rusqlite, desktop SQL package, desktop SQL using, hello major mismatch, dropped envelope literal, two versions inside one language, contracts directory disagreeing, missing PROTOCOL_VERSION, plus two byte-order-mark cases); tests/contract 64 passed through scripts/runtime/dev.py --pytest; the architecture guard passed and the convention check still reports only the two frozen-package files missing a final newline.",
      "TWO SILENT-PASS DEFECTS IN THIS CHECKER, FOUND BY RUNNING IT: (1) ignored directories were matched against the absolute path, so a test tree living under .project-local/ was skipped entirely and the synthetic negatives proved nothing; matching is now relative to the tree under test. (2) the desktop SQL rule lowercased the file but not the package name it searched for, so it could never fire; and reads did not strip a byte-order mark, which makes every ^-anchored rule miss where a BOM exists (the first real-tree negative did not fire because of this). Both are fixed and pinned by tests. Current fact: 132 files fall in the scanned scopes and none of them carries a BOM, so the hole was latent rather than hiding a live violation.",
      "NOT CLAIMED: R15 formally depends on R14, the independent GPT gate, which has not run, so this is a self-contained implementation slice inside R15 rather than R15 completion. Still missing for R15 as of this step: the per-item 16-group format matrix with complete/partial/custody-only status, the semantic half of the Obsidian round-trip (the link graph, embeds and anchors are preserved as text and bytes but not parsed or rebuilt - see the second step below), the repository-wide path convention, and the old-asset disposition record.",
      "SECOND STEP RAW (R15, commit c9d1072, no production code changed): a five-file Obsidian vault fixture at crates/archeaxis-archive/tests/fixtures/obsidian-vault with its own PROVENANCE.txt declaring it as authored-in-repo sample material with no private content. notes/index.md carries a wiki-link, an aliased wiki-link, a heading anchor, a block reference, an embed, a markdown link to the canvas, exactly one external link, a callout and a task list; notes/atomic.md carries the heading and the block id those anchors point at plus the reverse reference; attachments/diagram.png is a real 69-byte PNG whose bytes are not valid UTF-8, so a byte-level round-trip is meaningful; vault.canvas is a JSON Canvas 1.0 document with four nodes and two edges.",
      "SECOND STEP MEASURED (tested_source_sha c9d1072): the test first proves the fixture is a genuine vault by resolving every wiki-link, alias, heading anchor and block reference inside it (path, then basename, then stem; a heading anchor must find that heading in the target file and a block reference must find that block id), requiring at least 8 internal links and exactly 1 external link so that external targets are demonstrably not required to resolve. It then imports all five files as sources, exports an archive, restores it into a fresh workspace and asserts every file is byte-identical when read back through the raw object store by digest, that the source name set is unchanged, that the restored canvas is still a parseable four-node/two-edge document, and that the index note's text still contains all four link samples verbatim.",
      "SECOND STEP EVIDENCE: cargo test -p archeaxis-archive --offline exits 0 with 16 tests passing and 0 failing across six test binaries plus doc-tests in that package (lib 7, archive_rejection 4, archive_roundtrip 1, executor_roundtrip 1, obsidian_vault_roundtrip 1, raw_recovery 1, doc-tests 0), including obsidian_vault_roundtrip_keeps_bytes_names_and_links_and_states_the_gaps (log .project-local/runs/r62-archive-test.log); fixture is 4381 bytes across five files with sha256 prefixes diagram.png a74be423c3b9df93, atomic.md 91e3adc4ecc8ecc9, index.md 76fc557e49d76823, PROVENANCE.txt 139e8519e67e3d6f, vault.canvas 7c28a78de60bf618; the architecture guard passed and the convention check still reports only the two frozen-package files missing a final newline.",
      "THE HONEST HALF IS ASSERTED, NOT SILENT: importing this vault writes no anchor rows (anchors count is 0, with the assertion message saying import must not invent anchors it did not parse); no table in the restored schema has a name containing link or embed (and the assertion says that if such a table lands it must be replaced by a real graph check rather than deleted); route_for_kind('markdown') is None, so markdown travels through the text route, and route_for_kind('canvas') is None, so a canvas is custody-only with no ingest route. What this slice does NOT show is semantic link, embed or anchor reconstruction: it proves bytes, names and text survive, not that the link graph was absorbed.",
      "PROCESS NOTE for whoever runs this next: reading a cargo exit code through a PowerShell pipeline that merges stderr reports a failure that is not there. The first run of the archive suite looked like exit 1 while every suite printed ok; the same command with output redirected to a file exited 0. Redirect to a log, then read the file.",
      "THIRD STEP RAW (R15, commit 791c909): docs/authority/taskpack-0910-r3/R15-FORMAT-STATUS.json carries all sixteen format groups forward from the 2026-09-07 FORMAT-COVERAGE.json (schema archeaxis.format-retention/v1). The carried fields release_scope, formats and required_output are kept verbatim so the requirement cannot be redefined to match what was built, and each row adds status, implemented_now, gap and evidence (core route triples, worker routes, code paths, test paths, legacy references).",
      "THIRD STEP RESULT against the current implementation: complete 0, partial 4 (F01 text/markdown/CSV/JSON/XML, F04 images, F05 text PDF, F06 scanned and mixed PDF), custody_only 12. Custody is universal rather than a gap: any bytes can be imported as a content-addressed source and survive export/restore byte-identically, which is why a custody_only row states that preservation works and that parsing does not exist yet. The specific gaps are named per row, for example the Core still assigns image/png for every image job although the worker accepts png/jpeg/tiff/webp/bmp, and the PDF job does not chain into the OCR route by itself.",
      "THIRD STEP WIRING: scripts/check_format_matrix.py refuses drift instead of trusting the prose. It requires exactly F01-F16 in order, requires the carried fields to match the 2026-09-07 record verbatim, requires the status vocabulary and the summary counts to agree with the rows, parses the real ROUTES table in crates/archeaxis-application/src/attempts.rs and the real route table in services/python-workers/transport/text_ndjson.py, and then requires every claimed core route triple to exist, every claimed capability to be served by the worker the row names, every evidence path to exist, a custody_only row to claim no extraction route, a partial or complete row to claim at least one route and one test, and partial or custody_only rows to state their gap. The CI job that already runs the architecture and convention guards (lint, reached with static and py-primary for unknown paths) now runs this check and the language boundary check as well.",
      "THIRD STEP MEASURED (tested_source_sha 791c909): scripts/check_format_matrix.py exits 0 reporting 16 groups carried, 0 complete, 4 partial, 12 custody only, with 4 core routes and 3 worker routes parsed from the code; pytest tests/test_format_matrix.py 12 passed, and together with tests/test_language_boundaries.py 28 passed; check_language_boundaries.py and check_architecture.py both exit 0. Refusability is shown by eight negatives that each mutate one thing in a copy of the real matrix and are checked against the real route tables: a dropped group, a redefined requirement, an invented route, a route paired with the wrong worker, a custody-only row claiming extraction, a partial row with no route, a missing evidence path, and a stale summary.",
      "NOT CLAIMED for the third step: GitHub Actions was not run here, so only the local scripts and the workflow YAML parse were verified - CI is not claimed as passing. Whether each row's prose is sufficient remains a judgement for the independent audit (R14); the checker only guarantees that the mechanical claims are not false. R15 still formally depends on R14, which has not run, and R15 still owes the repository-wide path convention and the old-asset disposition record.",
      "FOURTH STEP RAW (R15, commit 25a3000): DIRECTORY_AUTHORITY.yaml declares its own matching semantics and precedence (deny, then exact path, then highest literal segment count, then longest literal prefix, with a tie rejected as ambiguous) but nothing evaluated that declaration against the tracked tree. scripts/check_path_conventions.py now does, over the full git ls-files output, and docs/authority/taskpack-0910-r3/R15-PATH-DISPOSITION.json records the result.",
      "FOURTH STEP MEASURED (tested_source_sha 25a3000): 1672 of 1741 tracked paths owned (96.04%), zero tracked paths matching a deny-commit rule, zero ambiguous matches. The honest conclusion is that the repository is NOT fully classified: 69 paths match no authority rule and therefore have no write lane at all under the file's write-denied default - the whole shared-contracts tree (38 files, inventoried in LEGACY_MANIFEST.yaml but never owned by the authority file), 20 root files, .worklab (2), .codex.example (2), apps (2, being apps/.gitignore and apps/desktop/README.md), contracts (2), .cargo/config.toml, crates/README.md and a stray archeaxis/ module.",
      "FOURTH STEP RECORD: the disposition record lists all 69 unowned paths exactly, classifies all 65 top-level entries (33 directories plus the 32 root files in four groups) with a class, a disposition, an existing owner document and a note, and records the six legacy roots declared by the authority file against LEGACY_MANIFEST.yaml: 1246 inventoried assets in total and every one of them still INVENTORIED_NOT_SEMANTICALLY_REVIEWED (app 183, shared 91, frontend 53, knowledge_base 38, desktop 32, src-tauri 13, and the rest), so no root may claim absorption. Bulk cleanup or moving directories is explicitly not semantic absorption.",
      "FOURTH STEP ENFORCEMENT DESIGN (written down so it is not undone by accident): snapshot totals (tracked, owned, coverage) are informational and carry measured_at_commit, because freezing them would turn every ordinary commit red - this slice itself landed three files and the tree still measures 96.04%. What is enforced: the unowned set must equal the record exactly, so a newly unowned path fails immediately until somebody classifies or records it; deny-commit and ambiguity are invariants that must stay at zero; every top-level entry must be matched by exactly one rule whose owner_doc exists; and the six legacy roots' inventory counts must equal what LEGACY_MANIFEST.yaml actually says, so an inventory number cannot go stale.",
      "FOURTH STEP EVIDENCE: check_path_conventions.py exits 0 reporting 1672/1741 owned (96.04%), 69 unowned and all recorded, 0 deny-commit tracked, 0 ambiguous, 65 top-level entries classified and 1246 legacy assets across 6 legacy roots (log .project-local/runs/r64-path.log); pytest tests/test_path_conventions.py 17 passed, and the three gate suites together 44 passed; the architecture, language boundary and format matrix checks all exit 0; the convention check returned to the two known frozen-package lines after it caught a real defect of mine - the generator wrote the record in Windows text mode, so the working copy was CRLF (git had already normalised the committed blob to LF) and the file is now LF on disk with the generator writing newline='\\n'.",
      "NOT CLAIMED for the fourth step: DIRECTORY_AUTHORITY.yaml is a protected governance file and was NOT edited, so the 69 unowned paths are a recorded finding for the owner rather than a fix. No legacy asset received a semantic absorption review: all 1246 remain INVENTORIED_NOT_SEMANTICALLY_REVIEWED. GitHub Actions was not run here. R15 formally depends on R14, which has not run.",
      "FIFTH STEP RAW (R15, commit b9df0ed): the Core announced every image job to the OCR worker as image/png even though the worker accepts png, jpeg, tiff, webp and bmp, which the format matrix recorded as F04's gap. claim() now reads the source's original_name and resolves the media type from its extension through attempts::media_type_for_name, and a new ROUTE_MEDIA_TYPES table mirrors the transport's own declared accepted sets (text 7 types, pdf 1, image 5).",
      "FIFTH STEP REFUSALS: a name whose media type the route cannot accept - a PDF payload named shot.png, or a text job whose file is shot.jpg - is refused with the new JobError::MediaTypeNotAccepted, which lists the accepted set and maps to HTTP 400, and the job stays queued. A name with no extension the Core is willing to name (clipboard, scan.dat, input.bin) is refused rather than guessed, because guessing is what the old pinned value did.",
      "FIFTH STEP MEASURED: cargo test -p archeaxis-application exits 0 with route_capabilities.rs covering all ten kind/name/media cases, the derivation table, the mismatch refusal and the unnamed-extension refusal; the full workspace exits 0 with 68 groups, 152 passed, 0 failed and no errors (three tests more than round 65); tests/test_image_media_types.py 7 passed, rendering one synthetic screenshot and driving it through the real Tesseract engine as png, jpg, tiff, webp and bmp, each returning text containing 6371 with line anchors, matching coverage counts and word boxes retained; the format matrix gate exits 0 with the F04 row updated in the same commit, still 0 complete / 4 partial / 12 custody-only; the architecture, path and language boundary gates exit 0 and the convention check still reports only the two frozen-package files.",
      "FIFTH STEP LIMITS AND SELF-CORRECTION: F04 stays partial because reading order is still only line order and there is no figure description, and only the five declared image media types are accepted - image/gif and anything unnamed are refused rather than silently accepted. While tidying I renamed a fixture in receipt_spans.rs from sample.pdf to sample.txt and the new rule refused it immediately with MediaTypeNotAccepted { kind: 'pdf', name: 'sample.txt' }; that test is a PDF job, so the name belonged as .pdf and was restored - the rule caught a wrong edit rather than hiding it.",
      "SIXTH STEP RAW (R15, commit 9ac1db9): the text route now reports the structure it can really derive, using the media type the Core declares (round 67). The projection stays canonical - text verbatim, line anchors, covered == total == len(structure), coverage == 1.0 - and the format facts ride in loss_receipt.params.format: markdown headings with level, line and text (headings inside a code fence excluded) plus wiki-link, embed, markdown-link, code-fence, list-item counts and frontmatter detection; CSV/TSV delimiter, row count, column count, header and ragged row count; JSON parse result with top-level type, key count and nested depth; XML parse result with root tag, element count, child count and depth.",
      "SIXTH STEP HONESTY: a document that does not parse is still projected as text and the fact says parsed false with the reason, which also joins the loss list, so nothing is guessed and nothing is silent. The format facts are facts about the document, not a second addressing scheme: anchors remain line based. The transport gained a media_type_arg flag so a route that wants the declared media type receives it instead of the worker sniffing the file name, and only the text route declares it.",
      "SIXTH STEP MEASURED: pytest tests/test_text_format_facts.py 10 passed, covering six media types whose projection contract is unchanged plus markdown heading lines [5, 10] with a fenced '#' excluded and an unclosed fence reported, CSV 5 rows / 4 columns / ragged_rows 2, TSV tab delimiter, JSON key_count 4 and depth 4 with a depth_unit field, broken JSON reporting parsed false with 'line 2' while the text is still projected in full, XML root/elements/children/depth, and one test that drives the transport's own route to prove the declared media type reaches the worker. The worker and transport suites are 37 passed with 47 subtests; the full workspace exits 0 with 68 groups, 152 passed, 0 failed and no errors; the format matrix gate passes with F01's row updated in the same commit; the architecture, path and language boundary gates exit 0 and the convention check still reports only the two frozen-package files.",
      "SIXTH STEP SELF-CORRECTIONS, all caught by running the code: an embed ![[diagram.png]] was counted twice, once as a wiki-link and once as an embed, so the link pattern gained a negative lookbehind; the JSON depth counted the scalars and reported one level too many, so it is now 'nested containers, the outermost counting as one' and carries depth_unit; and Element.iterancestors() does not exist on CPython's C-accelerated Element, so the XML depth is computed by recursion. F01 remains partial: code symbols, tables and local-model annotation are still missing.",
      "SEVENTH STEP RAW (R15, commit 54c7c21): the PDF route now reports the structure the engine really exposes. The projection stays canonical - text plus page/line anchors, coverage over lines - and the facts ride in loss_receipt.params.structure: per-page character and engine-block counts, image reference counts, engine text blocks with bounding boxes, character counts and first lines, and the tables the engine's own finder detects with row and column counts. Every field is labelled as a measurement ('a text block is an engine block, not a linguistic paragraph'; 'a detection is not a guarantee that the region is a table'), caps carry their own capped facts, and for scanned PDFs pages_without_text is now joined by per-page character counts.",
      "SEVENTH STEP PROTOCOL DEFECT AND FIX: PyMuPDF's table finder prints a layout hint to stdout on first use, and the sidecar protocol is one JSON envelope per stdout line, so the Core's existing end-to-end PDF job failed with 'unexpected trailing worker output'. Engine chatter is now captured during the call, recorded into the receipt as engine_messages with a policy note, and listed as a loss, so the protocol stream stays clean and nothing is silently dropped. The defect was found by the existing e2e test, not by the new unit tests.",
      "SEVENTH STEP MEASURED: pytest tests/test_worker_pdf_structure.py 8 passed - a two-block PDF reports text_block_count 2 with the original first lines and bboxes, page_facts[0].chars 76 and image_reference_count 1 after an image is inserted; the projection contract is unchanged (page/line anchors, covered == total == 2, coverage 1.0, no accuracy anywhere); a ruled grid is detected as a 2x2 table with table_support true; an image-only page reports empty text, pages_without_text [1], chars 0 and a loss naming OCR; a corrupt PDF still raises; and a subprocess test asserts stdout is exactly one JSON envelope. The PDF suites are 14 passed, cargo test -p archeaxis-application exits 0 with the previously failing pdf_job_end_to_end now green, the full workspace exits 0 with 68 groups, 152 passed, 0 failed and no errors, and the format matrix passes with the F05 and F06 rows updated in the same commit.",
      "SEVENTH STEP SELF-INFLICTED INCIDENTS (recorded so they are not repeated): a multi-line replacement done with a PowerShell single-quoted string wrote literal backtick-r-backtick-n into worker_pdf.py, and the Python command used to repair it had its backticks consumed by PowerShell first, replacing 13,385 sequences and leaving a 13,076-line broken file. git checkout restored it and the work was redone with the edit tool, one verified step at a time. Multi-line edits belong to the edit tool. F05 and F06 remain partial: blocks are not linguistic paragraphs, a detection is not a guarantee, pages are still not an addressing level, and the PDF job still does not chain into OCR by itself.",
      "EIGHTH STEP RAW (R15, commit a068a01): F12 was the last custody-only group with a plainly reachable increment and it is now partial. A .canvas file is resolved as the JSON document it is, so the text route reports nodes and edges as an integrity measurement: node and edge counts, node types, duplicate node ids, and edges naming a node that does not exist. SRT and VTT are resolved as the textual documents they are and the route reports cue count, first and last cue times and whether the document is SRT or WebVTT, labelled 'detected by pattern, not by media type' with cue times explicitly 'facts, not anchors'. The Rust derivation gained canvas -> application/json and srt/vtt -> text/plain, pinned by a test.",
      "EIGHTH STEP REFUSALS KEPT: a malformed canvas reports parsed false with no canvas section; numbered text without a time arrow stays plain text; and a name the route cannot read (a .canvas offered to the image route, a .srt to the pdf route) is still refused by the Core. No time anchor and no canvas node or edge is stored as a structure or a relation, so nothing yet links a cue or a node to knowledge and navigation stays on line anchors.",
      "EIGHTH STEP MEASURED: pytest tests/test_text_format_facts.py 17 passed - canvas node_count 4, edge_count 2, node_types {file: 3, text: 1}, duplicate_node_ids ['n-atomic'], dangling_edges [{edge: e-2, missing: n-missing}]; plain JSON is not called a canvas; broken canvas JSON reports parsed false; SRT counts 2 cues with first 00:00:01,000 and last 00:00:07,000; WebVTT is recognised by its header; an empty WebVTT says it has no cues; numbered text without a time arrow stays plain. The three structure suites together are 32 passed; the application crate is green; the full workspace exits 0 with 68 groups, 153 passed, 0 failed and no errors.",
      "EIGHTH STEP GUARD CAUGHT MY OWN EDIT: moving F12 from custody_only to partial left the matrix coverage_summary stale, and scripts/check_format_matrix.py refused it (partial 4 vs 5 rows, custody_only 12 vs 11). It now reads 0 complete / 5 partial / 11 custody-only and the gate exits 0, as do the architecture, path and language boundary gates, while the convention check still reports only the two frozen-package files. This is the second time this session that a gate of mine caught my own incomplete edit, which is the point of gates that read the artefacts.",
      "NINTH STEP RAW (R15, commit f88d0b9): F06's loop is closed. The PDF worker renders the pages it could not read to PNG and declares each one with its digest and size in loss_receipt.params.structure.ocr_candidates; the new Core module crates/archeaxis-application/src/ocr.rs reads that declaration, verifies every digest and size against the bytes actually in the transfer area, imports each page as its own source named <stem>-page-N.png (so the media derivation reads it as an image), and enqueues one image job per page, idempotently. The split is unchanged: the worker renders because only it holds the PDF engine, and the Core enqueues because only it writes, so no worker ever holds a database handle.",
      "NINTH STEP REFUSALS: a missing render, a digest mismatch, a size mismatch, or a declared name that tries to leave the transfer area is refused with a reason and leaves no queued job behind, because a job whose input cannot be verified would be a fake success waiting to happen.",
      "NINTH STEP TWO REAL DEFECTS FIXED WHILE BUILDING IT: (1) renders were written into the per-attempt temporary directory, which is deleted when the attempt ends, so the Core could not read them later - the executor now passes an explicit --artifact-root pointing at the durable transfer area; (2) that flag was first passed to every worker, which broke the text and OCR worker CLIs with 'unrecognized arguments' because unknown flags are correctly an error - it is now passed only to the routes that declare they need it (ARTIFACT_ROOT_CAPABILITIES = ['pdf.extract']), so every other launch shape is unchanged.",
      "NINTH STEP MEASURED: cargo test -p archeaxis-application exits 0 with the new pdf_ocr_chain.rs end-to-end tests green - a scanned PDF built with PyMuPDF and PIL declares one candidate (page 1, image/png, 64-character digest), enqueue_pages returns ['job-pdf-page-1'] and returns empty on a second call, that job is kind image with source name scan-page-1.png, and executing it stores OCR text containing 6371 through the real Tesseract engine; a PDF that already has text declares no candidate and enqueues nothing; tampering with the rendered file is refused with 'cannot enqueue work' and leaves zero job-tamper-page-* jobs; an escaping file name is refused too. pytest tests/test_worker_pdf_structure.py is 11 passed, including candidate declarations whose file really exists with matching bytes and digest and a PNG signature, ocr_render.dpi 150, no candidates and no files for a text PDF, and nothing written when no artifact directory is given. The worker and transport suites are 37 passed; the full workspace exits 0 with 69 groups, 155 passed, 0 failed and no errors; the format matrix passes with the F06 row updated in the same commit; the architecture, path and language boundary gates exit 0, and the convention check still reports only the two frozen-package files. F06 remains partial because the chain is a Core call a caller makes after the PDF job rather than something the executor triggers by itself, and low-confidence regions are still not marked.",
      "TENTH STEP RAW (R15, commit 3937f2b): the PDF to OCR chain is now automatic. A new attempts::CHAINED_AFTER_SUCCESS const names the routes that get follow-up work, and execute_owned calls ocr::enqueue_pages inside the same commit closure that finishes the job, so there is no window in which a job reports success while the pages it declared were never queued.",
      "TENTH STEP FAILURE HANDLING: a chaining failure does not fail the PDF job, because the extraction and the declaration really did happen; it is recorded as a machine receipt with outcome failed and the reason (method_version ocr.enqueue_pages/v1, tool_version pymupdf, model_version 'not-a-model: deterministic core chaining'), so a reader can find it instead of inferring it from a silence.",
      "TENTH STEP MEASURED: cargo test -p archeaxis-application exits 0 with the three pdf_ocr_chain.rs end-to-end tests green - a scanned PDF is queued with no manual call (job-pdf-page-1, kind image, source scan-page-1.png), a second enqueue_pages call returns empty and exactly one page job exists, executing that job stores OCR text containing 6371 through the real Tesseract engine, a text PDF declares no candidate and enqueues nothing, a tampered render is refused while the automatic page job remains, and a blocked render leaves the PDF job succeeded with a failed machine receipt naming the page and stating that no model was involved. The full workspace exits 0 with 69 groups, 156 passed, 0 failed and no errors, the format matrix passes with the F06 row updated in the same commit (its gap is now unmarked low-confidence regions and a one-step chain), and the architecture, path and language boundary gates exit 0 while the convention check still reports only the two frozen-package files.",
      "TENTH STEP SELF-CORRECTION: once enqueue_pages became automatic, the test assertion that a manual call returns the job id failed immediately because the automatic chain had already queued it - the test found the semantic change before I did. The assertion now requires the second call to return empty and the page job count to be exactly one, and the failure message carries the chain receipts so a future breakage explains itself.",
      "ELEVENTH STEP RAW (R15, commit f2b25fe): the OCR receipt already carried per-word boxes and the engine's own score; what was missing was the honest form of 'low-confidence regions'. The worker now reports params.review: a named review threshold (60.0), the list of regions scoring below it with their boxes, character spans and own scores, scored and unscored region counts kept separate (an unscored region is counted, never treated as fine), the lowest score, a capped fact when the list is truncated, and a loss line naming how many regions fell below. The threshold is described as a review aid and never as a correctness measure, and the word that would turn a score into a claim appears nowhere in a receipt.",
      "ELEVENTH STEP LESSON PINNED IN A TEST: a degraded render is recognised as garbage ('nT' instead of 'ArcheAxis 6371 km') and still scores 68.0, above the threshold. That is the honest limit of the feature: the score orders review work and cannot stand in for a verdict, so the receipt reports where to look rather than how right it is. The test asserts the review arithmetic, that every listed region is really below the threshold with a full bbox and an in-range character span, that lowest_score is the minimum of the region scores, and that no receipt contains the word that would make the score a claim.",
      "ELEVENTH STEP MEASURED: the OCR suites are 19 passed; a real engine reading of a clean synthetic screenshot reports three regions scoring 21.4, 16.6 and 92.2 with two below the threshold, lowest_score 16.6 and a loss line naming the count; the full workspace exits 0 with 69 groups, 156 passed, 0 failed and no errors; the format matrix passes with the F04 and F06 rows updated in the same commit (F04's gap is now reading order, figure description, unnamed extensions and the fact that a wrong reading can score above the threshold; F06's gap is the one-step chain); the architecture, path and language boundary gates exit 0 and the convention check still reports only the two frozen-package files.",
      "ELEVENTH STEP TWO SELF-CORRECTIONS: my first wording said 'it is not an accuracy figure' and the route's own assertion rejected it, because a receipt is not allowed to contain that word at all - the final text avoids the word entirely, which is the stronger guarantee. And my first test assumed a noisier image would produce more low-scoring regions; the measurement showed the opposite (the degraded image produced one region scoring 68.0), so that unfounded expectation was removed and replaced with the measured fact above."
    ]
  },
  "R16": {
    "status": "TODO",
    "evidence": []
  }
}
