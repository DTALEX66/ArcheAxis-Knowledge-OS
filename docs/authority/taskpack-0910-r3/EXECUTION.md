# 执行台账 — ARCHEAXIS-NEXT-TASKPACK-2026-09-10 (R3.1)

包已按 `使用方式` 单层安装到本目录，保留 `reference-r2/` 快照。
`verify_package.py` 以 Python UTF-8 模式运行 exit 0（hashes、17 项任务依赖、
原 23 项任务全部保留）；本机 locale 为 GBK，若直接用 `python verify_package.py`
会因 `read_text()` 默认编码在 UTF-8 的 `TASKS.json` 上失败，故使用
`python -X utf8 ...`，未修改冻结的包内文件。

进度写入本文件与 `STATE.json`；`TASKS.json` 保持计划定义不变。

|切片|实际SHA|run id|命令/环境|结果|证据路径/hash|限制/回滚|
|---|---|---|---|---|---|---|
|R00|985a219（登记前基线）|—|`python -X utf8 docs/authority/taskpack-0910-r3/verify_package.py` exit 0；`cargo test --workspace --offline` 52 组 exit 0；`pwsh -File scripts/ci/run_tests.ps1 --full` 2347 passed/7 skipped exit 0；conventions 门与架构守卫通过|IMPLEMENTED_PENDING_AUDIT|`R00-BASELINE-REVIEW.md`、`PACKAGE-STATUS.md`、`.project-local/runs/r00-python-full-0910.log`、`.project-local/runs/cargo-archive-01-full.log`|仅文档与治理指针；回滚＝revert 本切片提交；未改产品实现|
|R01|2dd7d6d（起始）|—|`inventory_project.py`（只读元数据）→ 23.423 GiB/130,120 文件/20 观测错误/60 排除/10 reparse；`.hermes` 单列 42.853 GiB/718,077 文件 newest 2026-09-06；D: free 231.15 GiB；`cargo build -p archeaxis-api` 经 `.project-local/runs/cargo-build-api.bat` exit 0，根 `target/` 6,151,810,038 B/19,949 文件前后一致，`.project-local/build/cargo` +52.6 MiB|IMPLEMENTED_PENDING_AUDIT（部分范围见 §6）|`R01-CAPACITY-BASELINE.md`、`.project-local/runs/r01-census.json`|未删任何缓存；打包侧路径差分与 `__pycache__` 重定向未做；回滚＝revert 本切片文档提交|
|R02|288991a + cbe253b|—|`cargo test -p archeaxis-domain` exit 0 / 9 组全绿：accept_with_new_body_is_rejected_and_body_stays_immutable、accept_commits_status_and_one_event_atomically、failed_review_leaves_no_partial_state、modified_creates_traceable_successor、modified_creates_candidate_and_records_event_on_original|IMPLEMENTED_PENDING_AUDIT|`review_transaction.rs`、`.project-local/runs/r02-domain.log`|接受/拒绝/撤销不得改正文；仅 `modified` 建新版本+supersedes，同一事务；回滚＝revert 288991a/cbe253b|
|R03|985a219 + 本轮|—|旧实现真实样本：在 v3 时代 scratch worktree（`968c479`/`a2dbef5`/`60b355a`/`3d65609` 的 store+archive）用历史代码导出四个**非空** v3 归档（10/11/12/13 表，sources=1 knowledge=2 objects=1），作为 fixture 提交；`cargo test -p archeaxis-archive` exit 0 / 7 组全绿（含 `genuine_v3_era_archives_restore_with_content` 逐个恢复四布局并断言升级到 v4、正文逐字节保留）；全 workspace `cargo test --workspace --offline` exit 0 / 52 组|IMPLEMENTED_PENDING_AUDIT|`crates/archeaxis-archive/tests/fixtures/v3-{ten,eleven,twelve,thirteen}-tables/`（含 PROVENANCE.txt）、`.project-local/runs/r03-archive-v2.log`、`.project-local/runs/r03-full-workspace.log`|v3 实际发布过 10/11/12/13 四种布局，全部接受；v1（7 表、无 workspace_meta）明确不支持；未知布局仍拒绝；回滚＝revert 本切片提交|
|R04|本轮|—|修复 fail-open 漏洞：`Launch::from_stdin` 现只接受 `actor ∈ {human, machine}`，其余值（`alien`、`Human`、空串、`"machine "` 尾空格等）一律拒绝启动且不建 workspace；机器/人类凭据仍由启动会话声明，客户端 `x-archeaxis-actor` 一律被中间件覆盖|IMPLEMENTED_PENDING_AUDIT（凭据托管范围待补）|`.project-local/runs/r04-api.log`（launch_auth 6 测试，含 `unknown_launch_actor_is_rejected_and_never_grants_human_authority`）；既有真实进程 harness 断言 stdout 不含 token|真实宿主签发并保管的机器/人类凭据范围仍未实现；合法用户动作成功、非法启动失败已回归；回滚＝revert 本切片|
|R05|本轮|—|Core 侧适配器落地：`crates/archeaxis-application/src/scheduler.rs`（`SchedulerClient`：ARCHEAXIS_PYTHON + worker 路径，子进程 JSON 契约，校验 `authority == "fsrs"`；不可用/被拒时返回 `Unavailable`/`Rejected`，**绝不回退阶梯**）；集成测试 5 项全过：真实 Rust→Python→FSRS donor，成熟卡间隔 >14 天（证明不是阶梯）、同请求可复现、缺 rating 被拒、缺解释器/缺 worker 脚本报 Unavailable|IN_PROGRESS（API 默认路径未接）|`.project-local/runs/r07-scheduler-adapter.log`（application 套件 exit 0；`adapter_returns_fsrs_scheduling_not_the_ladder` 等 5 项）；全仓 Python `run_tests.ps1 --full` **2355 passed / 7 skipped exit 0**（`.project-local/runs/r07-python-full.log`，较此前 2347 恰为新增 8 项 worker 测试，证明 donor 增量参数无回归）|剩余：学习事件 API 仍未默认调用适配器，`learning::suggest_next_interval` 仍是 Rust 内部默认；回滚＝revert 本切片|
|R05b|本轮|—|keyed 审校路径**参数化**（不复制去重逻辑）：`ScheduleSource::{Ladder, Explicit(Option<i64>)}` + 私有 `record_review_keyed_impl`；新增公开 `record_review_scheduled(..., next_review_days: Option<i64>)`，`None`＝调度不可用 → 事件照记但 **next_review 为 NULL**、payload 带 `schedule: unavailable` 标记、返回哨兵 `SCHEDULE_UNAVAILABLE=-2`（绝不回退阶梯）；`record_review_keyed` 行为不变（Ladder 模式）；EVENT-01 语义（重放返回原收据 `-1`、跨对象/跨 payload 冲突、缺键拒绝）保持|IN_PROGRESS|`.project-local/runs/r13-r05-domain.log`（domain 10 组全绿，新增 `learning_schedule_authority` 4 测试）；`.project-local/runs/r13-r05-api.log`（api 14 组全绿含 learning_events_api、machine_correction_loop，无回归）|回滚＝revert 本切片|
|R05c|本轮|—|API 处理器接线：`LearningEventBody` 新增可选 `schedule_state`+`now`；处理器先解析权威再入库——(a) 带卡状态 → 走 `SchedulerClient`（复用 FSRS worker）；(b) 调度不可用 → `record_review_scheduled(None)` 记 **unscheduled**；(c) 无卡状态 → 显式走占位阶梯；响应新增 `schedule_authority`（`fsrs`/`unavailable`/`placeholder_ladder`），调用方无法再把阶梯误认为真实 FSRS|IN_PROGRESS（剩余见右）|`.project-local/runs/r15-r05-api.log`（api 15 组全绿；新增 `learning_schedule_api` 3 测试：卡状态→fsrs 且 >14 天、无卡状态→placeholder_ladder、不可用→unavailable/-2 且历史 `next_review` 为 null）|剩余：产品侧调用方需始终提交卡状态（属 R10 宿主接线）；scheduled 事件的**重启一致性**尚未单独断言；全 workspace 复跑待下一工作单元；回滚＝revert 本切片|
|R05d|本轮|—|补 R05 收尾两项：① 新增 schedule_survives_a_workspace_reopen（domain）——已排程事件重开库后 due 仍在、unscheduled 事件仍无 due、重启后同键重试仍判为重放（收据一致）；② 全 workspace cargo test --workspace --offline **exit 0 / 55 组全绿**（新增测试文件使组数由 52 增至 55）|IMPLEMENTED_PENDING_AUDIT（唯一遗留见右）|.project-local/runs/r16-full-workspace.log（55 组 ok，含 schedule_survives_a_workspace_reopen）|唯一遗留：产品侧调用方需始终提交 schedule_state（属 R10 宿主接线，届时阶梯不再被任何产品路径触达）；回滚＝revert 本切片|
|R06|本轮|—|公开核查的**离线可交付部分**补回归：`tests/test_x07_public_check_probe.py`（13 项）锁定必需区分——真实支持须同句含对象+单位+数字；**直径命中记为 `diameter_only`（弱证据，非支持）**；无关同数字（如「6371 年」）→ `none` 非支持；模型答「不支持」「无法判断」、空文本或报错一律**不得**成为支持；抓取失败**抛异常**而非静默变成「无证据」|IN_PROGRESS（云端部分 BLOCKED_EXTERNAL）|pytest `tests/test_x07_public_check_probe.py` → **13 passed**；probe 本体 `scripts/probes/x07_public_check_probe.py`|云端交叉核查需**用户已配置且获准**的接口与凭据（本项目默认 0 付费预算）→ 记为 `BLOCKED_EXTERNAL`，不声称云端核查完成；活体「本地检索 + 模型裁决」需外网（此前间歇）与 ollama（当前 :11434 在线），列入下一工作单元；回滚＝revert 本切片|
|R06b|本轮|—|**活体运行成功**（真实公开检索 + 真实本地模型裁决）：Wikipedia Earth 抽取命中并在同句定位证据；判定 `evidence_kind=diameter_only`（该句给的是直径 12,742 km，**不算半径支持**）；本地 qwen3:8b 明确回答「无法判断：来源未提及平均半径」→ `verdict_kind=undeterminable`、`supported=false`；probe 退出码 1（=「不支持/未判定」的语义返回值，非崩溃）|IMPLEMENTED_PENDING_AUDIT（云端子项 BLOCKED_EXTERNAL）|`.project-local/runs/r18-x07-live.log`（含脱敏结构化结果：source_url、extract_head、evidence_kind、evidence_quote、local_model_verdict、verdict_kind、supported）；离线区分回归 `tests/test_x07_public_check_probe.py` 13 passed|云端交叉核查仍需**你已配置且获准**的接口/凭据 → `BLOCKED_EXTERNAL`，未声称完成；本次仅 1 次活体取样，不代表规模化结论；回滚＝revert 本切片（仅记账）|
|R07|本轮|—|**非空旧资产迁移路径打通**（合成合法样本，非私密真库）：`stage_legacy_learning_history` 迁移旧学习历史——旧库**正**间隔被保留、无间隔与旧 0（due-immediately，vNext 历史无对应表示）一律记 unscheduled 且计数与实存一致、重跑按持久事件键**重放收据不重复累积**；配合既有 `stage_demo_semantic_import` 一次性完成 notes(3) + docs/attachments/links 显式损失 + 学习历史(3)；旧库**前后字节相同**（无双写）；migration crate 新增 `archeaxis-domain` 依赖（学习写入仍走 Core 唯一权威）|IMPLEMENTED_PENDING_AUDIT（真库资格未完成）|`.project-local/runs/r19-r07-migration.log`（migration 5 组全绿，含 `non_empty_legacy_library_stages_notes_and_learning_history_without_touching_the_source`）；`.project-local/runs/r19-full-workspace.log`（全 workspace 56 组全绿，含新增依赖后的 lockfile）|**未声称**：用户真实私密库未提供 → 真库迁移资格仍未完成（按包规则用合法合成/公开材料证明软件路径）；附件/链接目前仍为显式损失行（非搬运）；旧时间戳未保留（键仅保存 legacy 表+行号）；回滚＝revert 本切片（含 Cargo.lock）|
|R08|本轮（第一片）|—|**原生 PDF 路由落地**：新增 `services/python-workers/document/worker_pdf.py`，与 text worker **同一契约**（`engine/engine_version/text/structure/loss_receipt`）；锚点为**页+行**（`path=["page-N","line-M"]` + 指向合并文本的字符偏移）；**降级显式**——损坏 PDF、以及被引擎当作"零页"接受的垃圾字节一律**报错**（避免静默空成功），页内无可提取文本（扫描/截图情形）返回真实空文本 + `losses` 明示需走 OCR；回执只记 engine/版本/页覆盖/损失，**不含任何"准确率"或模型置信度**|IN_PROGRESS|pytest `tests/test_worker_pdf_native.py` → **5 passed**（原生抽取含数字与单位、每页可寻址、无文本页降级为 OCR 提示、损坏文件显式失败、CLI 失败时 exit 1 且不输出 text）|剩余：扫描/截图经 **OCR 路由**接入同一契约；Rust 侧**统一 job 契约**（executor 目前只认 text worker）；质量展示；三类样本"导入→可检索知识"端到端；回滚＝revert 本切片（新增文件）|
|R08b|本轮|—|**OCR 路由接入同一契约**：`services/python-workers/vision/worker_ocr.py` 新增 `structure`（`kind="region"` + `path=["region-N"]` + 指向 `text` 的字符偏移 + `bbox{ x,y,w,h }`）与 `anchor_summary{covered,total}`，与 text/PDF 路由同形；每词置信度仅作**复核用字段**保留，回执仍不含"准确率"；空白样本不编造文本、structure 为空且回执有诚实损失说明|IN_PROGRESS|pytest `tests/test_worker_ocr_route.py` → **2 passed**（真实 Tesseract 识别合成截图：文本含 6371；region 锚点带 bbox 且偏移落在 `text` 内；覆盖计数已上报；空白图不声称识别成功）|环境事实（供后人）：本 shell 的 `TESSDATA_PREFIX` 指向**不存在**的目录，而仓库自带 `tools/tesseract/tessdata/eng.traineddata` 可用——测试显式传 `tessdata_dir` 即通过；既有 `tests/workers/test_bulk_ocr.py` 因同因而跳过。剩余：Rust 侧统一 job 契约、质量展示、三类样本端到端；回滚＝revert 本切片|
|R08c|本轮（gap 分析，无代码变更）|—|**统一 job 契约的真实缺口已定位**（三条证据）：① `crates/archeaxis-application/src/executor.rs:158` 硬编码 worker 身份 `python-worker-text-ndjson` v1；② `services/python-workers/transport/text_ndjson.py:137` 只接受 `capability == "text.extract"`（v1、单输入、空 parameters）；③ 本轮新增的 `worker_pdf.py`/`worker_ocr.py` 是**纯 CLI**（单次 stdin→stdout），不带 sidecar NDJSON 握手信封。因此 PDF/OCR 目前**进不了**同一作业/重试/错误模型|IN_PROGRESS|本轮为只读勘察（executor.rs、text_ndjson.py、两个新 worker 的返回契约）；无文件改动|**下一步（最小可行）**：(a) 传输层把能力名从单一常量泛化为受控集合（`text.extract` / `pdf.extract` / `image.ocr`），`execute()` 按能力分派到各路由的提取函数，保持同一 request/response 信封与同样的拒绝语义；(b) 给 pdf/ocr 两个 worker 加 NDJSON `main()`（复用同一 transport、各自声明 capability/identity）；(c) `executor.rs` 的 hello 校验改为接受受控身份集合（或由 profile 声明身份），并保持 attempt/错误/取消语义不变；(d) 端到端回归：三类样本经同一 job 类型产出 text+structure+loss_receipt 并可入库检索。回滚＝本行为纯记账|
|R08d|本轮|—|**统一作业契约落地（传输层泛化，步骤 a）**：`transport/text_ndjson.py` 由硬编码 `text.extract` 改为受控路由表 `ROUTES`（`text.extract` / `pdf.extract` / `image.ocr`），三条路由共用**同一 request/response 信封、同一拒绝语义、同一产物三元组**（text + document_structure + loss_report）；能力版本、可接受 media type、worker 模块均按路由声明，未知能力/错版本/错 media 均拒绝。集成中发现的真实问题并修掉：staging 输入是 **content-addressed（无扩展名）**，而 OCR worker 校验扩展名 → 传输层按 media type 生成路由局部视图（`<digest>.png` 等）后交给 OCR，原输入不动、哈希仍单独校验|IN_PROGRESS|pytest 四文件 **21 passed**：`tests/test_unified_job_contract.py`（三路由同契约 + 未知能力/错版本/错 media 拒绝）、`tests/test_worker_pdf_native.py` 5、`tests/test_worker_ocr_route.py` 2、`tests/test_worker_schedule.py` 8；另有全仓 Python 复跑（见 STATE 记录）|剩余：步骤 (b) 为 pdf/ocr 加 NDJSON `main()` 并各自声明 capability/identity；步骤 (c) `executor.rs` hello 身份由硬编码改为受控集合/profile 声明；步骤 (d) 三类样本经同一 job 类型端到端入库；回滚＝revert 本切片（含传输层与测试）|
|R08e|本轮|—|**PDF 路由接入 sidecar 作业循环（步骤 b 之 PDF 半）**：`transport/text_ndjson.py` 抽出通用 `serve_stdio(worker_name, capabilities, staging_root)`（握手/请求校验/响应信封/错误词表完全共用），text worker 改为调用它（行为不变）；`worker_pdf.py` 新增 `--staging-root` 模式并声明自身身份 `python-worker-pdf-ndjson` 与能力 `pdf.extract`。**又发现并修掉一个自造缺口**：路由表是全局的，若不校验就会让某 worker 服务它未声明的能力 → `serve_stdio` 现在强制「只能服务自己握手时声明的能力」，否则拒绝|IN_PROGRESS|pytest 五套件 **23 passed**：`tests/test_pdf_sidecar_job.py`（真实子进程握手 identity/capabilities、请求→响应 `succeeded`、产物三元组、text 产物含 6371；错能力被拒且 `retryable=false`）、`tests/test_unified_job_contract.py` 6、`tests/test_worker_pdf_native.py` 5、`tests/test_worker_ocr_route.py` 2、`tests/test_worker_schedule.py` 8|剩余：OCR worker 的 NDJSON 入口（同法）；步骤 (c) `executor.rs` hello 身份由硬编码改为受控集合/profile 声明；步骤 (d) 三类样本同一 job 端到端入库；回滚＝revert 本切片|
|R08f|本轮|—|**OCR 路由接入 sidecar 作业循环（步骤 b 完成）**：`worker_ocr.py` 新增 `--staging-root` 模式，经同一 `serve_stdio` 声明身份 `python-worker-ocr-ndjson` 与能力 `image.ocr`。至此三条路由**全部**走同一作业循环与信封：`python-worker-text-ndjson`/`text.extract`、`python-worker-pdf-ndjson`/`pdf.extract`、`python-worker-ocr-ndjson`/`image.ocr`；CLI 直调路径保持不变|IN_PROGRESS|pytest **10 passed**（`tests/test_ocr_sidecar_job.py` 真实子进程：握手身份/能力、`image.ocr` 作业 `succeeded`、产物三元组齐全、text 产物为真实识别文本、structure 首条为 `region`；未声明能力被拒 `retryable=false`。另 `tests/test_pdf_sidecar_job.py` 2 + `tests/test_unified_job_contract.py` 6）；CLI 路径回归 `tests/test_worker_ocr_route.py` 2 + `tests/test_worker_pdf_native.py` 5 全绿|剩余：步骤 (c) `executor.rs` hello 身份硬编码 → 受控集合/由 profile 声明；步骤 (d) 三类样本同一 job 端到端入库为可检索知识；回滚＝revert 本切片|
|R08g|本轮|—|**执行器身份闸门放宽（步骤 c 之半）**：`executor.rs` 由硬编码单身份改为受控集合 `KNOWN_WORKER_IDENTITIES`（text/pdf/ocr 三条），版本仍须 "1"；**新增真实执行器测试**证明 PDF worker 身份已被接受（错误信息不再含 `unexpected worker identity`）。**同时发现第二处硬编码（下一单元的关键）**：`crates/archeaxis-sidecar-protocol/src/worker.rs` 在请求构造（~L86）与 hello 兼容性检查（~L97）中仍**硬编码 `text.extract`**，因此 PDF/OCR 目前止步于 `incompatible worker hello`|IN_PROGRESS|`.project-local/runs/r27-executor.log`（application 套件 **7 组全绿**，含 `non_text_worker_identity_is_accepted_by_the_core`；既有 executor 真实作业/重启/取消测试不回归）|剩余：把 sidecar 协议 hello 校验改为「worker 必须**声明**请求所需能力」而非固定 text.extract，并让请求构造从作业/路由取能力（这两处即 R08 步骤 c 的收尾）；随后步骤 (d) 三类样本同一 job 端到端；回滚＝revert 本切片|
|R08h|本轮|—|**握手与请求能力去硬编码（步骤 c 完成）**：① `Request` 新增通用构造 `job(..., capability, ...)`（校验能力名合法性），`Request::text` 改为其薄封装 → 能力来自路由而非常量；② `decode_hello` 改为**能力无关**：只校验协议形状、身份非空、能力/模式列表**唯一且非空**，不再要求声明 `text.extract`；③ 执行器在**知道请求**的位置校验「worker 必须声明本作业所需能力」，不符即报 `worker does not advertise the requested capability <cap>`（身份闸门 + 能力匹配各司其职）|IN_PROGRESS|`.project-local/runs/r28-full-workspace.log`：全 workspace **exit 0 / 56 组全绿**，含新测试 `non_text_worker_is_accepted_and_a_foreign_capability_is_refused_with_a_reason`（PDF worker 收文本作业 → 不再报 `unexpected worker identity`、也不再报 `incompatible worker hello`，而是明确的能力不匹配原因）与协议既有 `handshake_requires_supported_versions_and_actual_text_capability` 不回归|剩余：步骤 (d) 端到端——作业 kind→能力/worker 的映射（目前执行器持有单一 worker 路径）、三类样本经同一 job 产出并可入库检索、质量展示；回滚＝revert 本切片|
|R08i|本轮|—|**作业 kind → 能力/媒体类型路由表（步骤 d 之 1）**：`attempts.rs` 新增 `ROUTES`（`text`/`text.extract`→`text.extract`+`text/plain`；`pdf`→`pdf.extract`+`application/pdf`；`image`→`image.ocr`+`image/png`）与 `route_for_kind()`；`claim()` 不再用「只允许 text」的白名单，也不再写死 `text/plain`，而是按 kind 取能力与媒体类型构造请求；**未声明的 kind 在 claim 即被拒且作业仍留在 queued**（不会被误标 running）|IN_PROGRESS|`.project-local/runs/r29-app.log`（application 套件 **8 组全绿**，含 `every_declared_route_selects_its_capability_and_media_type`、`an_unknown_job_kind_is_refused_at_claim_time`、`route_lookup_is_explicit_about_unknown_kinds`）；全 workspace 复跑见 STATE 记录|剩余：执行器按 kind 选择对应 worker 路径（目前仍只持有一条 worker 路径），随后三类样本端到端入库检索与质量展示；回滚＝revert 本切片|
|R08j|本轮|—|**执行器路由注册表（步骤 d 之 2 已验证部分）**：`Executor` 新增 `open_routes(db, staging, python, default_worker, extra)`（`open()` 保持兼容，默认只服务 `text.extract`）；**按 claim 出的能力派发**到对应 worker，未注册能力 → 明确报 `no worker registered for capability <cap>`（不再把作业发给错误的进程）。附带启动策略修复：stdlib-only 的 text 路由仍用硬化的 `-B -S`，**引擎型路由（PDF/OCR）允许解释器 site-packages**（`-S` 会剥掉 PyMuPDF 等引擎，实测报“no native PDF engine available”）|IN_PROGRESS|`.project-local/runs/r30-full-workspace.log`：全 workspace **exit 0 / 58 组全绿**（含 `a_job_without_a_registered_route_worker_fails_explicitly`、`registering_a_route_makes_its_capability_dispatchable`）；PDF 路由自身 5 项与统一契约 6 项 pytest 全绿|**发现并如实记录的剩余缺口（PDF 作业端到端仍被 Core 拒绝，拒得对）**：① 回执必须同时给 `covered/total/coverage`（PDF 回执缺 coverage）；② **structure/coverage 必须与投影文本一致** —— 锚点必须由**最终文本**推导（页分隔符会把按页累加的字符偏移与行数弄歪）。两条都是 worker 侧修正，修好后与端到端测试一并提交；本轮尝试该重写时因耦合改动过多且预算见底，**已回滚 worker_pdf 到已验证状态**（不提交未验证代码）；回滚本切片＝revert 执行器与测试改动|
|R08k|本轮（勘察+回滚，无代码变更）|—|**PDF 作业端到端：Core 第二条校验规则尚未满足，已精确定位到报错但未破解**。进展与结论：① 试修 worker 后，**第一条错误已消除**（"covered, total and coverage must be supplied together" → 我在回执里按**行**为单位给出 covered/total/coverage，并把锚点改为由**最终文本**推导）；② 仍被 **`invalid receipt: structure or coverage does not match projected text`** 拒绝——说明 Core 的该校验规则比"锚点覆盖最终文本 + coverage=covered/total"更强，具体判据我**尚未读到源码**（应在 `attempts::finish` 或 `jobs` 的回执校验处），因此**不再猜测**。③ 本轮两次尝试的改动**全部回滚**，工作树回到上一已验证提交 `3a0662d`（0 脏文件），PDF worker 5 项 + 统一契约 6 项 + OCR 路由 2 项 pytest 全绿、application 套件全绿|IN_PROGRESS（PDF 端到端仍缺）|回滚后复核：pytest 13 passed；application 套件各组 ok；`.project-local/runs/r31-app.log`|**下一单元第一步（明确）**：先读 Core 里抛 `structure or coverage does not match projected text` 的校验实现，按其**确切判据**改 worker（可能要求 coverage 与结构计数/字符跨度满足特定关系，例如 covered == len(structure) 且末锚点 char_end == len(text)），然后再提交端到端测试；本轮无代码变更，无需回滚|
|R08l|本轮（读校验源码，无代码变更）|—|**Core 回执校验的确切判据已找到**（`crates/archeaxis-application/src/attempts.rs:104-106`）——它当前是**文本路由专用**的，这正是 PDF 作业被拒的原因：① `loss.engine=="python-worker-text" && loss.engine_version=="0.1.0"`（PDF worker 报 `pymupdf-native-pdf`/`pymupdf` → 第一条即不满足）；② `structure == expected`，其中 expected 由 Core 自己从文本**重算**（`expected_lines(text)`，即规范的 `kind="line"`、`path=["line-N"]` 锚点），而 PDF worker 给的是 `path=["page-N","line-M"]` → 结构不等；③ `loss.covered == structure.len()` 且 `loss.total == expected 的总数`|IN_PROGRESS|只读勘察：`attempts.rs:88-107`（另含 `expected_lines`、`Line`、`LossReceipt::validate`）|**下一单元的确切改法（二选一，需你或下一会话定）**：(A) **Core 泛化**——把引擎身份改为受控路由集合（text/pdf/ocr 各自的 engine+version），并把结构比较改为「按字符跨度 + kind 比对，允许 path 带页前缀」（如 `path.last()==canonical`）；(B) **worker 迁就现状**——让 PDF/OCR 回执伪装成文本 worker 身份与 `["line-N"]` 锚点（**不推荐**：等于丢掉页锚点语义，且伪造引擎身份违背保真原则）。选 (A) 才符合 R08「原件、页/区域锚点统一」的目标；本轮无代码变更，无需回滚|
|R08m|本轮|—|**Core 回执引擎闸门改为路由感知（步骤 d 泛化 (A) 之第一半）**：`attempts.rs` 新增受控路由引擎集合 `ENGINE_PROFILES`（`python-worker-text`/`0.1.0`、`pymupdf-native-pdf`/`pymupdf`、`python-worker-ocr`/`0.1.0`），校验由「必须是文本引擎」改为「必须属于已声明路由引擎且版本匹配」——文本路径行为不变，PDF/OCR 的引擎身份**不再被无条件拒绝**|IN_PROGRESS|`.project-local/runs/r34-app.log`（application 套件 **9 组全绿**）；`.project-local/runs/r34-full-workspace.log`（全 workspace **exit 0 / 58 组全绿**）|**仍缺（(A) 第二半）**：结构比较仍要求 `structure == expected_lines(text)`（规范 `["line-N"]`），因此**页前缀锚点 `["page-N","line-M"]` 仍会被拒**——需把比较改为「按 `kind` + 字符跨度比对，允许 path 末段匹配规范 path（或按跨度等价）」；完成前**不提交 PDF 端到端测试、也不声称 PDF 作业可用**；回滚＝revert 本切片（两处小改）|

## R00 记录（2026-09-11）

- 基线锁定：分支 `codex/full-loop-0906`，HEAD `985a219`（晚于包审计基线 `cbe253b`），
  与 origin 同步，工作区仅 `.zcode/` 未跟踪（GLM 会话本地）。`main` 仍 `4ca46ea`。
- 继承缺陷重查（不假定、不回退旧实现）：R02 已修（`288991a` REVISION-01）；
  R05 已修（`288991a` EVENT-01）；R03 本会话已修（`985a219` ARCHIVE-01）；
  R06 部分（`288991a` VERIFY-01，实跑受 HTTPS/凭据 gated）。
- 入口登记：`AGENTS.md` §6、`docs/DOCUMENTATION_AUTHORITY_INDEX.md`、
  `docs/CONFIGURATION_AUTHORITY_INDEX.md`、`DECISION_SUPERSESSION_LEDGER.yaml`
  SUP-018、intake `workspace/intake/2026-09-10-next-taskpack-0910.md`。
- 乱码检查：扫描 `docs/`、`workspace/`、`config/` 及根级 md/yaml 共 451 个文件，
  活动入口文档无乱码；唯一 U+FFFD 出现在
  `docs/authority/legacy/T17-semantic-review-samples-2026-09-05.json` 的
  `head_snippet`（二进制 PNG 头片段），属合法数据而非文档损坏，历史材料不改。
- 待办：R01/R02/R03… 的验收仍以各切片独立证据为准；Q00/Q01 只由独立 GPT 判定。
