# 执行台账 — ARCHEAXIS-NEXT-TASKPACK-2026-09-10 (R3.1)

包已按 `使用方式` 单层安装到本目录，保留 `reference-r2/` 快照。
`verify_package.py` 以 Python UTF-8 模式运行 exit 0（hashes、17 项任务依赖、
原 23 项任务全部保留）；本机 locale 为 GBK，若直接用 `python verify_package.py`
会因 `read_text()` 默认编码在 UTF-8 的 `TASKS.json` 上失败，故使用
`python -X utf8 ...`，未修改冻结的包内文件。

进度写入本文件与 `STATE.json`；`TASKS.json` 保持计划定义不变。

|切片|实际SHA|run id|命令/环境|结果|证据路径/hash|限制/回滚|
|---|---|---|---|---|---|---|
|R00|985a219（登记前基线）|—|`python -X utf8 docs/authority/taskpack-0910-r3/verify_package.py` exit 0；`cargo test --workspace --offline` 52 组 exit 0；`pwsh -File scripts/ci/run_tests.ps1 --full` 2347 passed/7 skipped exit 0；conventions 门与架构守卫通过|IMPLEMENTED_PENDING_AUDIT|`R00-BASELINE-REVIEW.md`、`PACKAGE-STATUS.md`、`.project-local/runs/r00-python-full-0910.log`、`.project-local/runs/cargo-archive-01-full.log`|仅文档与治理指针；回滚＝revert 本切片提交；未改产品实现|
|R01|2dd7d6d（起始）|—|`inventory_project.py`（只读元数据）→ 23.423 GiB/130,120 文件/20 观测错误/60 排除/10 reparse；`.hermes` 单列 42.853 GiB/718,077 文件 newest 2026-09-06；D: free 231.15 GiB；`cargo build -p archeaxis-api` 经 `.project-local/runs/cargo-build-api.bat` exit 0，根 `target/` 6,151,810,038 B/19,949 文件前后一致，`.project-local/build/cargo` +52.6 MiB|IMPLEMENTED_PENDING_AUDIT（部分范围见 §6）|`R01-CAPACITY-BASELINE.md`、`.project-local/runs/r01-census.json`|未删任何缓存；打包侧路径差分与 `__pycache__` 重定向未做；回滚＝revert 本切片文档提交|
|R02|288991a + cbe253b|—|`cargo test -p archeaxis-domain` exit 0 / 9 组全绿：accept_with_new_body_is_rejected_and_body_stays_immutable、accept_commits_status_and_one_event_atomically、failed_review_leaves_no_partial_state、modified_creates_traceable_successor、modified_creates_candidate_and_records_event_on_original|IMPLEMENTED_PENDING_AUDIT|`review_transaction.rs`、`.project-local/runs/r02-domain.log`|接受/拒绝/撤销不得改正文；仅 `modified` 建新版本+supersedes，同一事务；回滚＝revert 288991a/cbe253b|
|R03|985a219 + 本轮|—|旧实现真实样本：在 v3 时代 scratch worktree（`968c479`/`a2dbef5`/`60b355a`/`3d65609` 的 store+archive）用历史代码导出四个**非空** v3 归档（10/11/12/13 表，sources=1 knowledge=2 objects=1），作为 fixture 提交；`cargo test -p archeaxis-archive` exit 0 / 7 组全绿（含 `genuine_v3_era_archives_restore_with_content` 逐个恢复四布局并断言升级到 v4、正文逐字节保留）；全 workspace `cargo test --workspace --offline` exit 0 / 52 组|IMPLEMENTED_PENDING_AUDIT|`crates/archeaxis-archive/tests/fixtures/v3-{ten,eleven,twelve,thirteen}-tables/`（含 PROVENANCE.txt）、`.project-local/runs/r03-archive-v2.log`、`.project-local/runs/r03-full-workspace.log`|v3 实际发布过 10/11/12/13 四种布局，全部接受；v1（7 表、无 workspace_meta）明确不支持；未知布局仍拒绝；回滚＝revert 本切片提交|
|R04|本轮|—|修复 fail-open 漏洞：`Launch::from_stdin` 现只接受 `actor ∈ {human, machine}`，其余值（`alien`、`Human`、空串、`"machine "` 尾空格等）一律拒绝启动且不建 workspace；机器/人类凭据仍由启动会话声明，客户端 `x-archeaxis-actor` 一律被中间件覆盖|IMPLEMENTED_PENDING_AUDIT（凭据托管范围待补）|`.project-local/runs/r04-api.log`（launch_auth 6 测试，含 `unknown_launch_actor_is_rejected_and_never_grants_human_authority`）；既有真实进程 harness 断言 stdout 不含 token|真实宿主签发并保管的机器/人类凭据范围仍未实现；合法用户动作成功、非法启动失败已回归；回滚＝revert 本切片|
|R05|本轮|—|Core 侧适配器落地：`crates/archeaxis-application/src/scheduler.rs`（`SchedulerClient`：ARCHEAXIS_PYTHON + worker 路径，子进程 JSON 契约，校验 `authority == "fsrs"`；不可用/被拒时返回 `Unavailable`/`Rejected`，**绝不回退阶梯**）；集成测试 5 项全过：真实 Rust→Python→FSRS donor，成熟卡间隔 >14 天（证明不是阶梯）、同请求可复现、缺 rating 被拒、缺解释器/缺 worker 脚本报 Unavailable|IN_PROGRESS（API 默认路径未接）|`.project-local/runs/r07-scheduler-adapter.log`（application 套件 exit 0；`adapter_returns_fsrs_scheduling_not_the_ladder` 等 5 项）；全仓 Python `run_tests.ps1 --full` **2355 passed / 7 skipped exit 0**（`.project-local/runs/r07-python-full.log`，较此前 2347 恰为新增 8 项 worker 测试，证明 donor 增量参数无回归）|剩余：学习事件 API 仍未默认调用适配器，`learning::suggest_next_interval` 仍是 Rust 内部默认；回滚＝revert 本切片|
|R05b|本轮|—|keyed 审校路径**参数化**（不复制去重逻辑）：`ScheduleSource::{Ladder, Explicit(Option<i64>)}` + 私有 `record_review_keyed_impl`；新增公开 `record_review_scheduled(..., next_review_days: Option<i64>)`，`None`＝调度不可用 → 事件照记但 **next_review 为 NULL**、payload 带 `schedule: unavailable` 标记、返回哨兵 `SCHEDULE_UNAVAILABLE=-2`（绝不回退阶梯）；`record_review_keyed` 行为不变（Ladder 模式）；EVENT-01 语义（重放返回原收据 `-1`、跨对象/跨 payload 冲突、缺键拒绝）保持|IN_PROGRESS|`.project-local/runs/r13-r05-domain.log`（domain 10 组全绿，新增 `learning_schedule_authority` 4 测试）；`.project-local/runs/r13-r05-api.log`（api 14 组全绿含 learning_events_api、machine_correction_loop，无回归）|回滚＝revert 本切片|
|R05c|本轮|—|API 处理器接线：`LearningEventBody` 新增可选 `schedule_state`+`now`；处理器先解析权威再入库——(a) 带卡状态 → 走 `SchedulerClient`（复用 FSRS worker）；(b) 调度不可用 → `record_review_scheduled(None)` 记 **unscheduled**；(c) 无卡状态 → 显式走占位阶梯；响应新增 `schedule_authority`（`fsrs`/`unavailable`/`placeholder_ladder`），调用方无法再把阶梯误认为真实 FSRS|IN_PROGRESS（剩余见右）|`.project-local/runs/r15-r05-api.log`（api 15 组全绿；新增 `learning_schedule_api` 3 测试：卡状态→fsrs 且 >14 天、无卡状态→placeholder_ladder、不可用→unavailable/-2 且历史 `next_review` 为 null）|剩余：产品侧调用方需始终提交卡状态（属 R10 宿主接线）；scheduled 事件的**重启一致性**尚未单独断言；全 workspace 复跑待下一工作单元；回滚＝revert 本切片|
|R05d|本轮|—|补 R05 收尾两项：① 新增 schedule_survives_a_workspace_reopen（domain）——已排程事件重开库后 due 仍在、unscheduled 事件仍无 due、重启后同键重试仍判为重放（收据一致）；② 全 workspace cargo test --workspace --offline **exit 0 / 55 组全绿**（新增测试文件使组数由 52 增至 55）|IMPLEMENTED_PENDING_AUDIT（唯一遗留见右）|.project-local/runs/r16-full-workspace.log（55 组 ok，含 schedule_survives_a_workspace_reopen）|唯一遗留：产品侧调用方需始终提交 schedule_state（属 R10 宿主接线，届时阶梯不再被任何产品路径触达）；回滚＝revert 本切片|
|R06|本轮|—|公开核查的**离线可交付部分**补回归：`tests/test_x07_public_check_probe.py`（13 项）锁定必需区分——真实支持须同句含对象+单位+数字；**直径命中记为 `diameter_only`（弱证据，非支持）**；无关同数字（如「6371 年」）→ `none` 非支持；模型答「不支持」「无法判断」、空文本或报错一律**不得**成为支持；抓取失败**抛异常**而非静默变成「无证据」|IN_PROGRESS（云端部分 BLOCKED_EXTERNAL）|pytest `tests/test_x07_public_check_probe.py` → **13 passed**；probe 本体 `scripts/probes/x07_public_check_probe.py`|云端交叉核查需**用户已配置且获准**的接口与凭据（本项目默认 0 付费预算）→ 记为 `BLOCKED_EXTERNAL`，不声称云端核查完成；活体「本地检索 + 模型裁决」需外网（此前间歇）与 ollama（当前 :11434 在线），列入下一工作单元；回滚＝revert 本切片|
|R06b|本轮|—|**活体运行成功**（真实公开检索 + 真实本地模型裁决）：Wikipedia Earth 抽取命中并在同句定位证据；判定 `evidence_kind=diameter_only`（该句给的是直径 12,742 km，**不算半径支持**）；本地 qwen3:8b 明确回答「无法判断：来源未提及平均半径」→ `verdict_kind=undeterminable`、`supported=false`；probe 退出码 1（=「不支持/未判定」的语义返回值，非崩溃）|IMPLEMENTED_PENDING_AUDIT（云端子项 BLOCKED_EXTERNAL）|`.project-local/runs/r18-x07-live.log`（含脱敏结构化结果：source_url、extract_head、evidence_kind、evidence_quote、local_model_verdict、verdict_kind、supported）；离线区分回归 `tests/test_x07_public_check_probe.py` 13 passed|云端交叉核查仍需**你已配置且获准**的接口/凭据 → `BLOCKED_EXTERNAL`，未声称完成；本次仅 1 次活体取样，不代表规模化结论；回滚＝revert 本切片（仅记账）|
|R07|本轮|—|**非空旧资产迁移路径打通**（合成合法样本，非私密真库）：`stage_legacy_learning_history` 迁移旧学习历史——旧库**正**间隔被保留、无间隔与旧 0（due-immediately，vNext 历史无对应表示）一律记 unscheduled 且计数与实存一致、重跑按持久事件键**重放收据不重复累积**；配合既有 `stage_demo_semantic_import` 一次性完成 notes(3) + docs/attachments/links 显式损失 + 学习历史(3)；旧库**前后字节相同**（无双写）；migration crate 新增 `archeaxis-domain` 依赖（学习写入仍走 Core 唯一权威）|IMPLEMENTED_PENDING_AUDIT（真库资格未完成）|`.project-local/runs/r19-r07-migration.log`（migration 5 组全绿，含 `non_empty_legacy_library_stages_notes_and_learning_history_without_touching_the_source`）；`.project-local/runs/r19-full-workspace.log`（全 workspace 56 组全绿，含新增依赖后的 lockfile）|**未声称**：用户真实私密库未提供 → 真库迁移资格仍未完成（按包规则用合法合成/公开材料证明软件路径）；附件/链接目前仍为显式损失行（非搬运）；旧时间戳未保留（键仅保存 legacy 表+行号）；回滚＝revert 本切片（含 Cargo.lock）|
|R08|本轮（第一片）|—|**原生 PDF 路由落地**：新增 `services/python-workers/document/worker_pdf.py`，与 text worker **同一契约**（`engine/engine_version/text/structure/loss_receipt`）；锚点为**页+行**（`path=["page-N","line-M"]` + 指向合并文本的字符偏移）；**降级显式**——损坏 PDF、以及被引擎当作"零页"接受的垃圾字节一律**报错**（避免静默空成功），页内无可提取文本（扫描/截图情形）返回真实空文本 + `losses` 明示需走 OCR；回执只记 engine/版本/页覆盖/损失，**不含任何"准确率"或模型置信度**|IN_PROGRESS|pytest `tests/test_worker_pdf_native.py` → **5 passed**（原生抽取含数字与单位、每页可寻址、无文本页降级为 OCR 提示、损坏文件显式失败、CLI 失败时 exit 1 且不输出 text）|剩余：扫描/截图经 **OCR 路由**接入同一契约；Rust 侧**统一 job 契约**（executor 目前只认 text worker）；质量展示；三类样本"导入→可检索知识"端到端；回滚＝revert 本切片（新增文件）|
|R08b|本轮|—|**OCR 路由接入同一契约**：`services/python-workers/vision/worker_ocr.py` 新增 `structure`（`kind="region"` + `path=["region-N"]` + 指向 `text` 的字符偏移 + `bbox{ x,y,w,h }`）与 `anchor_summary{covered,total}`，与 text/PDF 路由同形；每词置信度仅作**复核用字段**保留，回执仍不含"准确率"；空白样本不编造文本、structure 为空且回执有诚实损失说明|IN_PROGRESS|pytest `tests/test_worker_ocr_route.py` → **2 passed**（真实 Tesseract 识别合成截图：文本含 6371；region 锚点带 bbox 且偏移落在 `text` 内；覆盖计数已上报；空白图不声称识别成功）|环境事实（供后人）：本 shell 的 `TESSDATA_PREFIX` 指向**不存在**的目录，而仓库自带 `tools/tesseract/tessdata/eng.traineddata` 可用——测试显式传 `tessdata_dir` 即通过；既有 `tests/workers/test_bulk_ocr.py` 因同因而跳过。剩余：Rust 侧统一 job 契约、质量展示、三类样本端到端；回滚＝revert 本切片|
|R08c|本轮（gap 分析，无代码变更）|—|**统一 job 契约的真实缺口已定位**（三条证据）：① `crates/archeaxis-application/src/executor.rs:158` 硬编码 worker 身份 `python-worker-text-ndjson` v1；② `services/python-workers/transport/text_ndjson.py:137` 只接受 `capability == "text.extract"`（v1、单输入、空 parameters）；③ 本轮新增的 `worker_pdf.py`/`worker_ocr.py` 是**纯 CLI**（单次 stdin→stdout），不带 sidecar NDJSON 握手信封。因此 PDF/OCR 目前**进不了**同一作业/重试/错误模型|IN_PROGRESS|本轮为只读勘察（executor.rs、text_ndjson.py、两个新 worker 的返回契约）；无文件改动|**下一步（最小可行）**：(a) 传输层把能力名从单一常量泛化为受控集合（`text.extract` / `pdf.extract` / `image.ocr`），`execute()` 按能力分派到各路由的提取函数，保持同一 request/response 信封与同样的拒绝语义；(b) 给 pdf/ocr 两个 worker 加 NDJSON `main()`（复用同一 transport、各自声明 capability/identity）；(c) `executor.rs` 的 hello 校验改为接受受控身份集合（或由 profile 声明身份），并保持 attempt/错误/取消语义不变；(d) 端到端回归：三类样本经同一 job 类型产出 text+structure+loss_receipt 并可入库检索。回滚＝本行为纯记账|
|R08d|本轮|—|**统一作业契约落地（传输层泛化，步骤 a）**：`transport/text_ndjson.py` 由硬编码 `text.extract` 改为受控路由表 `ROUTES`（`text.extract` / `pdf.extract` / `image.ocr`），三条路由共用**同一 request/response 信封、同一拒绝语义、同一产物三元组**（text + document_structure + loss_report）；能力版本、可接受 media type、worker 模块均按路由声明，未知能力/错版本/错 media 均拒绝。集成中发现的真实问题并修掉：staging 输入是 **content-addressed（无扩展名）**，而 OCR worker 校验扩展名 → 传输层按 media type 生成路由局部视图（`<digest>.png` 等）后交给 OCR，原输入不动、哈希仍单独校验|IN_PROGRESS|pytest 四文件 **21 passed**：`tests/test_unified_job_contract.py`（三路由同契约 + 未知能力/错版本/错 media 拒绝）、`tests/test_worker_pdf_native.py` 5、`tests/test_worker_ocr_route.py` 2、`tests/test_worker_schedule.py` 8；另有全仓 Python 复跑（见 STATE 记录）|剩余：步骤 (b) 为 pdf/ocr 加 NDJSON `main()` 并各自声明 capability/identity；步骤 (c) `executor.rs` hello 身份由硬编码改为受控集合/profile 声明；步骤 (d) 三类样本经同一 job 类型端到端入库；回滚＝revert 本切片（含传输层与测试）|
|R08e|本轮|—|**PDF 路由接入 sidecar 作业循环（步骤 b 之 PDF 半）**：`transport/text_ndjson.py` 抽出通用 `serve_stdio(worker_name, capabilities, staging_root)`（握手/请求校验/响应信封/错误词表完全共用），text worker 改为调用它（行为不变）；`worker_pdf.py` 新增 `--staging-root` 模式并声明自身身份 `python-worker-pdf-ndjson` 与能力 `pdf.extract`。**又发现并修掉一个自造缺口**：路由表是全局的，若不校验就会让某 worker 服务它未声明的能力 → `serve_stdio` 现在强制「只能服务自己握手时声明的能力」，否则拒绝|IN_PROGRESS|pytest 五套件 **23 passed**：`tests/test_pdf_sidecar_job.py`（真实子进程握手 identity/capabilities、请求→响应 `succeeded`、产物三元组、text 产物含 6371；错能力被拒且 `retryable=false`）、`tests/test_unified_job_contract.py` 6、`tests/test_worker_pdf_native.py` 5、`tests/test_worker_ocr_route.py` 2、`tests/test_worker_schedule.py` 8|剩余：OCR worker 的 NDJSON 入口（同法）；步骤 (c) `executor.rs` hello 身份由硬编码改为受控集合/profile 声明；步骤 (d) 三类样本同一 job 端到端入库；回滚＝revert 本切片|
|R08f|本轮|—|**OCR 路由接入 sidecar 作业循环（步骤 b 完成）**：`worker_ocr.py` 新增 `--staging-root` 模式，经同一 `serve_stdio` 声明身份 `python-worker-ocr-ndjson` 与能力 `image.ocr`。至此三条路由**全部**走同一作业循环与信封：`python-worker-text-ndjson`/`text.extract`、`python-worker-pdf-ndjson`/`pdf.extract`、`python-worker-ocr-ndjson`/`image.ocr`；CLI 直调路径保持不变|IN_PROGRESS|pytest **10 passed**（`tests/test_ocr_sidecar_job.py` 真实子进程：握手身份/能力、`image.ocr` 作业 `succeeded`、产物三元组齐全、text 产物为真实识别文本、structure 首条为 `region`；未声明能力被拒 `retryable=false`。另 `tests/test_pdf_sidecar_job.py` 2 + `tests/test_unified_job_contract.py` 6）；CLI 路径回归 `tests/test_worker_ocr_route.py` 2 + `tests/test_worker_pdf_native.py` 5 全绿|剩余：步骤 (c) `executor.rs` hello 身份硬编码 → 受控集合/由 profile 声明；步骤 (d) 三类样本同一 job 端到端入库为可检索知识；回滚＝revert 本切片|
|R08g|本轮|—|**执行器身份闸门放宽（步骤 c 之半）**：`executor.rs` 由硬编码单身份改为受控集合 `KNOWN_WORKER_IDENTITIES`（text/pdf/ocr 三条），版本仍须 "1"；**新增真实执行器测试**证明 PDF worker 身份已被接受（错误信息不再含 `unexpected worker identity`）。**同时发现第二处硬编码（下一单元的关键）**：`crates/archeaxis-sidecar-protocol/src/worker.rs` 在请求构造（~L86）与 hello 兼容性检查（~L97）中仍**硬编码 `text.extract`**，因此 PDF/OCR 目前止步于 `incompatible worker hello`|IN_PROGRESS|`.project-local/runs/r27-executor.log`（application 套件 **7 组全绿**，含 `non_text_worker_identity_is_accepted_by_the_core`；既有 executor 真实作业/重启/取消测试不回归）|剩余：把 sidecar 协议 hello 校验改为「worker 必须**声明**请求所需能力」而非固定 text.extract，并让请求构造从作业/路由取能力（这两处即 R08 步骤 c 的收尾）；随后步骤 (d) 三类样本同一 job 端到端；回滚＝revert 本切片|
|R08h|本轮|—|**握手与请求能力去硬编码（步骤 c 完成）**：① `Request` 新增通用构造 `job(..., capability, ...)`（校验能力名合法性），`Request::text` 改为其薄封装 → 能力来自路由而非常量；② `decode_hello` 改为**能力无关**：只校验协议形状、身份非空、能力/模式列表**唯一且非空**，不再要求声明 `text.extract`；③ 执行器在**知道请求**的位置校验「worker 必须声明本作业所需能力」，不符即报 `worker does not advertise the requested capability <cap>`（身份闸门 + 能力匹配各司其职）|IN_PROGRESS|`.project-local/runs/r28-full-workspace.log`：全 workspace **exit 0 / 56 组全绿**，含新测试 `non_text_worker_is_accepted_and_a_foreign_capability_is_refused_with_a_reason`（PDF worker 收文本作业 → 不再报 `unexpected worker identity`、也不再报 `incompatible worker hello`，而是明确的能力不匹配原因）与协议既有 `handshake_requires_supported_versions_and_actual_text_capability` 不回归|剩余：步骤 (d) 端到端——作业 kind→能力/worker 的映射（目前执行器持有单一 worker 路径）、三类样本经同一 job 产出并可入库检索、质量展示；回滚＝revert 本切片|
|R08i|本轮|—|**作业 kind → 能力/媒体类型路由表（步骤 d 之 1）**：`attempts.rs` 新增 `ROUTES`（`text`/`text.extract`→`text.extract`+`text/plain`；`pdf`→`pdf.extract`+`application/pdf`；`image`→`image.ocr`+`image/png`）与 `route_for_kind()`；`claim()` 不再用「只允许 text」的白名单，也不再写死 `text/plain`，而是按 kind 取能力与媒体类型构造请求；**未声明的 kind 在 claim 即被拒且作业仍留在 queued**（不会被误标 running）|IN_PROGRESS|`.project-local/runs/r29-app.log`（application 套件 **8 组全绿**，含 `every_declared_route_selects_its_capability_and_media_type`、`an_unknown_job_kind_is_refused_at_claim_time`、`route_lookup_is_explicit_about_unknown_kinds`）；全 workspace 复跑见 STATE 记录|剩余：执行器按 kind 选择对应 worker 路径（目前仍只持有一条 worker 路径），随后三类样本端到端入库检索与质量展示；回滚＝revert 本切片|
|R08j|本轮|—|**执行器路由注册表（步骤 d 之 2 已验证部分）**：`Executor` 新增 `open_routes(db, staging, python, default_worker, extra)`（`open()` 保持兼容，默认只服务 `text.extract`）；**按 claim 出的能力派发**到对应 worker，未注册能力 → 明确报 `no worker registered for capability <cap>`（不再把作业发给错误的进程）。附带启动策略修复：stdlib-only 的 text 路由仍用硬化的 `-B -S`，**引擎型路由（PDF/OCR）允许解释器 site-packages**（`-S` 会剥掉 PyMuPDF 等引擎，实测报“no native PDF engine available”）|IN_PROGRESS|`.project-local/runs/r30-full-workspace.log`：全 workspace **exit 0 / 58 组全绿**（含 `a_job_without_a_registered_route_worker_fails_explicitly`、`registering_a_route_makes_its_capability_dispatchable`）；PDF 路由自身 5 项与统一契约 6 项 pytest 全绿|**发现并如实记录的剩余缺口（PDF 作业端到端仍被 Core 拒绝，拒得对）**：① 回执必须同时给 `covered/total/coverage`（PDF 回执缺 coverage）；② **structure/coverage 必须与投影文本一致** —— 锚点必须由**最终文本**推导（页分隔符会把按页累加的字符偏移与行数弄歪）。两条都是 worker 侧修正，修好后与端到端测试一并提交；本轮尝试该重写时因耦合改动过多且预算见底，**已回滚 worker_pdf 到已验证状态**（不提交未验证代码）；回滚本切片＝revert 执行器与测试改动|
|R08k|本轮（勘察+回滚，无代码变更）|—|**PDF 作业端到端：Core 第二条校验规则尚未满足，已精确定位到报错但未破解**。进展与结论：① 试修 worker 后，**第一条错误已消除**（"covered, total and coverage must be supplied together" → 我在回执里按**行**为单位给出 covered/total/coverage，并把锚点改为由**最终文本**推导）；② 仍被 **`invalid receipt: structure or coverage does not match projected text`** 拒绝——说明 Core 的该校验规则比"锚点覆盖最终文本 + coverage=covered/total"更强，具体判据我**尚未读到源码**（应在 `attempts::finish` 或 `jobs` 的回执校验处），因此**不再猜测**。③ 本轮两次尝试的改动**全部回滚**，工作树回到上一已验证提交 `3a0662d`（0 脏文件），PDF worker 5 项 + 统一契约 6 项 + OCR 路由 2 项 pytest 全绿、application 套件全绿|IN_PROGRESS（PDF 端到端仍缺）|回滚后复核：pytest 13 passed；application 套件各组 ok；`.project-local/runs/r31-app.log`|**下一单元第一步（明确）**：先读 Core 里抛 `structure or coverage does not match projected text` 的校验实现，按其**确切判据**改 worker（可能要求 coverage 与结构计数/字符跨度满足特定关系，例如 covered == len(structure) 且末锚点 char_end == len(text)），然后再提交端到端测试；本轮无代码变更，无需回滚|
|R08l|本轮（读校验源码，无代码变更）|—|**Core 回执校验的确切判据已找到**（`crates/archeaxis-application/src/attempts.rs:104-106`）——它当前是**文本路由专用**的，这正是 PDF 作业被拒的原因：① `loss.engine=="python-worker-text" && loss.engine_version=="0.1.0"`（PDF worker 报 `pymupdf-native-pdf`/`pymupdf` → 第一条即不满足）；② `structure == expected`，其中 expected 由 Core 自己从文本**重算**（`expected_lines(text)`，即规范的 `kind="line"`、`path=["line-N"]` 锚点），而 PDF worker 给的是 `path=["page-N","line-M"]` → 结构不等；③ `loss.covered == structure.len()` 且 `loss.total == expected 的总数`|IN_PROGRESS|只读勘察：`attempts.rs:88-107`（另含 `expected_lines`、`Line`、`LossReceipt::validate`）|**下一单元的确切改法（二选一，需你或下一会话定）**：(A) **Core 泛化**——把引擎身份改为受控路由集合（text/pdf/ocr 各自的 engine+version），并把结构比较改为「按字符跨度 + kind 比对，允许 path 带页前缀」（如 `path.last()==canonical`）；(B) **worker 迁就现状**——让 PDF/OCR 回执伪装成文本 worker 身份与 `["line-N"]` 锚点（**不推荐**：等于丢掉页锚点语义，且伪造引擎身份违背保真原则）。选 (A) 才符合 R08「原件、页/区域锚点统一」的目标；本轮无代码变更，无需回滚|
|R08m|本轮|—|**Core 回执引擎闸门改为路由感知（步骤 d 泛化 (A) 之第一半）**：`attempts.rs` 新增受控路由引擎集合 `ENGINE_PROFILES`（`python-worker-text`/`0.1.0`、`pymupdf-native-pdf`/`pymupdf`、`python-worker-ocr`/`0.1.0`），校验由「必须是文本引擎」改为「必须属于已声明路由引擎且版本匹配」——文本路径行为不变，PDF/OCR 的引擎身份**不再被无条件拒绝**|IN_PROGRESS|`.project-local/runs/r34-app.log`（application 套件 **9 组全绿**）；`.project-local/runs/r34-full-workspace.log`（全 workspace **exit 0 / 58 组全绿**）|**仍缺（(A) 第二半）**：结构比较仍要求 `structure == expected_lines(text)`（规范 `["line-N"]`），因此**页前缀锚点 `["page-N","line-M"]` 仍会被拒**——需把比较改为「按 `kind` + 字符跨度比对，允许 path 末段匹配规范 path（或按跨度等价）」；完成前**不提交 PDF 端到端测试、也不声称 PDF 作业可用**；回滚＝revert 本切片（两处小改）|
|R08n|本轮|—|**结构比较改为跨度感知（步骤 d 泛化 (A) 完成）**：`attempts.rs` 新增 `same_spans(found, expected)`——按 `kind` + `char_start/char_end` + **path 末段**比较，因此页限定锚点 `["page-3","line-12"]` 与规范 `["line-1"]` 结构等价被接受，而**错行/错跨度仍拒绝**（不是放松校验）。至此 Core 侧三条件全部路由化：引擎身份（受控路由集合）、结构（跨度+末段）、covered/total（按行）|IN_PROGRESS|`.project-local/runs/r35-app.log`（application 套件 **10 组全绿**，含新 `receipt_spans.rs` 三项：页限定锚点同跨度被接受且记录真实引擎 `pymupdf-native-pdf`、末段错行被拒、未声明引擎被拒）；`.project-local/runs/r35-full-workspace.log`（全 workspace **exit 0 / 59 组全绿**）|**仍差最后一步（worker 侧）**：committed 的 PDF worker 回执仍缺 `coverage` 且按**页**计数（Core 要求按行并三者同给），且锚点需由**最终文本**推导——这正是第 30/31 轮写过又回滚的那两处 worker 修正；现 Core 已放行，下一单元应重新落地 worker 修正并**同提交** PDF 端到端作业测试；回滚＝revert 本切片|
|R08o|本轮|—|**PDF 作业端到端打通（步骤 d 关键目标达成）**：重新落地上一轮已就绪、现被 Core 接受的两处 worker 修正——① 回执**按行**给 `covered/total/coverage`（三者同给）；② 锚点由**最终文本**推导（页分隔符不再弄歪偏移/行数）。结果：`kind="pdf"` 作业 → `pdf.extract` 能力 → 派发到 PDF worker（`-B` 且保留 site-packages）→ Core 校验通过 → **文本入库 `job_outputs`（3 条产物，text 含 6371）**、`jobs.engine` 记录真实引擎 **`pymupdf-native-pdf`**（而非文本引擎）|IN_PROGRESS（R08 余：OCR 端到端、可检索知识、质量展示）|`.project-local/runs/r36-app.log`（application 10 组含 `pdf_job_is_dispatched_to_the_pdf_worker_and_its_text_is_stored`）；`.project-local/runs/r36-full-workspace.log`（**全 workspace exit 0 / 59 组**）；pytest 13 passed（PDF 原生 5 + 统一契约 6 + OCR 路由 2）|仍属 IN_PROGRESS：OCR 路由端到端（同一机制，按对称性应可通）与「导入→可检索知识」、质量展示尚未完成，均已在 STATE 列明；回滚＝revert 本切片（worker 两处 + 测试断言 + 端到端测试）|
|R08p|本轮（勘察+决策，无代码变更）|—|**OCR 端到端的真实冲突已定位**（两条证据）：① OCR worker 的 `structure` 是**词/区域**锚点（`kind="region"`，另有 `anchor_summary`），而回执本体**没有** `covered/total/coverage`；② Core 要求 `loss.covered==structure.len()`、`loss.total==期望行数`，且 `same_spans(structure, 规范行锚点)` —— 因此**词区域锚点在语义上不可能等于规范行锚点**，OCR 作业必被拒（且 Core 只接受恰好 3 个产物，区域框无处另开产物）|IN_PROGRESS（OCR 端到端未做）|只读勘察：`services/python-workers/vision/worker_ocr.py:208,217` 与 `crates/archeaxis-application/src/attempts.rs:120,127,128`|**决策建议 (A)（推荐）**：统一 Core 的**投影契约仍以「行」为准**——OCR worker 的 `structure` 改为**规范行锚点**（与 text/PDF 同形，满足 `same_spans`），把**词框/区域**降级为**复核元数据**放进 `loss_report.params.regions`（合法：仍是 3 个产物），并补 `covered/total/coverage`（行单位）。这样既满足 Core，又保住"页/区域锚点"信息，且不放松校验。**备选 (B)**：Core 再泛化以接受区域粒度（校验更弱、Core 面更大）——不推荐。定下 (A) 后即可照 PDF 的路子加 OCR 端到端作业测试；本轮无代码变更，无需回滚|
|R08q|本轮|—|**OCR 作业端到端打通（三路由全部走通同一 job 契约）**：按上一轮决策 (A) 实施——① `worker_ocr.py` 的 `structure` 改为**规范行锚点**（与 text/PDF 同形，满足 `same_spans`），并补 `covered/total/coverage`（行单位）；② OCR 自己的**词框/区域**降级为复核元数据放进 `loss_receipt.params.regions`（仍是 3 个产物，不放松 Core 校验、不丢区域信息）。③ 途中修掉一个真实缺陷：Rust 侧 `canonicalize()` 产生的 **Windows 扩展路径 `\\?\`** 传进 transport 后，tesseract 无法打开 tessdata（`Error opening data file \\?\...\tools\tesseract\tessdata/eng.traineddata`）→ transport 现剥离该前缀后再交给 OCR|IN_PROGRESS（R08 余：抽取结果→可检索知识、质量展示）|`.project-local/runs/r38-app.log`（application **11 组全绿**，含 `ocr_job_is_dispatched_to_the_ocr_worker_and_its_text_is_stored`）；`.project-local/runs/r38-full-workspace.log`（**全 workspace exit 0 / 60 组**）；pytest **17 passed**（OCR 路由 2 + OCR sidecar 2 + 统一契约 6 + PDF 原生 5 + PDF sidecar 2）|三路由现状：`text.extract`（既有）、`pdf.extract`（R08o 打通）、`image.ocr`（本轮打通）；**未声称**：知识检索入库与质量展示；回滚＝revert 本切片|
|R08r|本轮（勘察+决策，无代码变更）|—|**"抽取结果→可检索知识"的现状与决策点已定**：Core 在作业完成时已经写入 **`transforms(source_id, engine, text, loss_note)`** 并把 `jobs.transform_id` 指向它（`jobs.rs` 的 `complete_tx`）——即**抽取文本已按原件归属持久化**，且带引擎与损失说明；但**检索出口**（`GET /api/v1/search`）目前只覆盖 `knowledge`，因此"抽取结果可被检索"这一步**尚未接通**|IN_PROGRESS（R08 余：可检索 + 质量展示）|只读勘察：`crates/archeaxis-application/src/jobs.rs`（`complete_tx` 的 `INSERT INTO transforms(...)` + `UPDATE jobs ... transform_id`）；`crates/archeaxis-domain/src/search.rs`（仅 knowledge）|**决策建议 (a)（推荐，最小且不越权）**：把**检索出口扩到 `transforms.text`**，命中结果标注**来源原件 + 引擎 + 损失**，但**不**自动把抽取文本当成"知识"（类型/证据/资格是 R09 之后的语义问题）；**备选 (b)**：完成作业即自动建 knowledge 候选——需要知识类型/资格/审校语义，语义负担大，建议晚于 R09。质量展示随之给出**覆盖率、损失条数、引擎、区域数**等事实字段，**绝不含"准确率"或置信度**；本轮无代码变更，无需回滚|
|R08s|本轮|—|**抽取文本可检索（按决策 (a) 实施，R08 收敛）**：`search.rs` 新增 `transform_fts`（与既有 `knowledge_fts` 同款按需创建，**不升 schema 版本**）+ `reindex_transforms` + `search_transforms`（返回 `transform_id / source_id / engine / head`）；`GET /api/v1/search` 响应新增 **`transforms` 与 `transform_count`**（原有 `items` 不变，非破坏性）。**明确语义边界**：这是**抽取文本的检索**，命中标注**原件来源 + 引擎**，但**不把抽取文本当知识**（knowledge 检索结果不受影响，有测试断言）|IMPLEMENTED_PENDING_AUDIT（质量展示另列）|`.project-local/runs/r40-domain.log`（domain **11 组全绿**，含 `search_transforms.rs`：命中含 6371、标注 source 与引擎、缺失词为空、knowledge 检索不掺入抽取文本）；`.project-local/runs/r40-api.log`（api **15 组全绿**）；`.project-local/runs/r40-full-workspace.log`（**全 workspace exit 0 / 61 组**）|R08 剩余：**质量展示**（只给事实字段：覆盖率/损失条数/引擎/区域数，绝不含准确率）；「完成作业即自动建 knowledge 候选」按决策晚于 R09；回滚＝revert 本切片|
|R08t|本轮|—|**质量展示上线（R08 收尾项）**：新增 `GET /api/v1/jobs/:job_id/quality` —— 只给**事实字段**：`state / engine / engine_version / coverage / covered / total / loss_count / region_count / pages`，并带 `note:"facts only; model or recogniser confidence is not accuracy"`；**响应中不存在 `accuracy` 键**，也未把置信度当准确率。未知作业 → 404；已入队未完成 → 各计数为 0、`coverage` 为 null（**不编造覆盖率**）|IMPLEMENTED_PENDING_AUDIT|`.project-local/runs/r41-api.log`（api **16 组全绿**，含新 `job_quality.rs`：未知作业 404、排队作业只报事实且**无 accuracy 键**、note 明示置信度非准确率）；`.project-local/runs/r41-full-workspace.log`（**全 workspace exit 0 / 62 组**）；架构守卫通过；conventions 仅 2 条冻结包文件行|R08 至此：三路由端到端 ✅、抽取文本可检索（带来源/引擎归因）✅、质量展示（纯事实）✅；「完成作业自动建 knowledge 候选」按既定决策留待 R09 之后；回滚＝revert 本切片|
|R09|本轮（勘察+决策，无代码变更）|—|**R09 的根缺口已定位：学习记录与知识修订之间没有任何关联**。证据：① `learning_events` 只有 `(item_key, kind, outcome, next_review, event_id)`，**无 `knowledge_id`/revision/version**；`learning_event_keys` 同样只有 `event_id`；② `archeaxis-domain/src/learning.rs` 全文**不出现 `knowledge`**。因此"题目/学习记录引用某个修订、撤销后消费入口检测有效版本"目前**无法表达**——所以 R09 的"题目、缓存、机器上下文、结果回写均检测版本"缺少载体。**已具备的部分**（R2/R08 期间落地）：检索的 `active_only` 过滤、`qualification` 出口、superseded-not-current 与修订链、抽取文本按原件归属可检索|IN_PROGRESS|只读勘察：`crates/archeaxis-store-sqlite/src/lib.rs`（两张学习表的列）、`crates/archeaxis-domain/src/learning.rs`（无 knowledge 引用）|**决策建议 (A)（推荐，加性、保留历史）**：新增**加性表** `card_references(item_key, knowledge_id, created_event_id)`（或给 `learning_events` 加可空 `knowledge_id`），在**创建题目/学习项时记录当时引用的修订**；**读取端**（历史/题目/机器上下文/结果回写）按当前资格**标注** `knowledge_active / superseded / revision_changed`，**旧学习历史一律保留并标注**，绝不回写篡改历史，也不声称"收回"外部已发出的旧回答；需要 schema 版本提升（v4→v5）并沿用 EVENT-01 的守卫式迁移模式。**备选 (B)**：只从知识侧标注（较弱，无法回答"哪些题目引用了被撤销的修订"）——不推荐。本轮无代码变更，无需回滚|
|R09b|本轮|—|**R09 载体落地（决策 (A) 第一片，且刻意不升 schema 版本）**：`learning.rs` 新增 `card_references(item_key, knowledge_id, created_event_id, referenced_at)`（**按需创建**，沿用 FTS 索引的既有模式 → **不动 `SCHEMA_VERSION`**，因此不触发归档布局/测试级联）；API：`record_card_reference`（幂等 `INSERT OR IGNORE`、拒绝空键、拒绝不存在的修订）+ `references_for_card`（返回 `(knowledge_id, active)`，用 `knowledge::is_knowledge_active` 标注**当前是否仍有效**）。**语义**：引用**永不改写**——被修订超越后历史引用保留，仅标注"不再当前"|IN_PROGRESS（读取端接线与写回检查待做）|`.project-local/runs/r43-domain.log`（domain **12 组全绿**，含 `card_references.rs` 2 项：超越后引用保留且 `active=false`、同引用重复记录幂等、拒绝不存在修订与空键、**重开库后引用与有效性一致**）；`.project-local/runs/r43-full-workspace.log`（**全 workspace exit 0 / 63 组**）|已记录的局限：该表按需创建、**不在 `EXPORT_TABLES` 中，故归档暂不携带 card 引用**；剩余：把标注接到消费入口（历史/题目/机器上下文读取、结果写回检测版本）；回滚＝revert 本切片|
|R09c|本轮|—|**消费入口接线（R09 语义可在真实接口上验证）**：① 新增 `POST /api/v1/learning/items/:item_key/references`（记录该学习项所依据的知识修订；引用一旦记录**不可改写**、未知修订返回 400）；② `GET /api/v1/learning/events/:item_key` 现在返回 **`references: [{knowledge_id, active}]`**，用 `is_knowledge_active` 标注**该修订当前是否仍有效**。HTTP 级端到端验证：记录引用 → 历史显示 `active=true` → 用 `modified` 修正并接受后继 → **再次读取仍指向原修订且 `active=false`**（历史未改写、未"收回"任何旧回答）|IN_PROGRESS（剩余：机器上下文读取与**结果写回**的版本检测、异步索引滞后时的过滤）|`.project-local/runs/r44-api.log`（api **17 组全绿**，含 `card_reference_api.rs`：引用记录、`active=true`、超越后仍指原修订且 `active=false`、未知修订 400）；`.project-local/runs/r44-full-workspace.log`（**全 workspace exit 0 / 64 组**）|已知小瑕疵：测试里有一处未使用的 `mut` 触发编译器警告（不影响结果）；回滚＝revert 本切片|
|R09d|本轮（结论性核对，无代码变更）|—|**R09 剩余两项经核对为"按设计已满足"或"依赖尚未存在的语义"**：① **异步索引滞后过滤**——vNext 的检索是**每查询重建**（`search::reindex` / `reindex_transforms`）+ 逐行用 `is_knowledge_active` 标注 `active`，`active_only` 在**服务端**剔除失效项，因此**不存在"索引滞后导致沿用旧有效资格"的窗口**；若将来引入异步/增量索引，必须保留同一逐行校验（已作为约束记录）。② **结果写回的版本检测**——当前作业产物是**抽取文本（`transforms`）**，**不产生 knowledge**，故"写回"环节**没有知识修订维度可检**；该项真正生效的前提是"完成作业即建 knowledge 候选"，而该动作已按 R08r 决策 (a) 明确**推迟到 R09 之后**。③ 机器上下文读取：vNext 目前无独立 `context` 出口，检索出口已带 `active` 标注，届时新出口须沿用同一校验|IN_PROGRESS（剩余实质项＝推迟中的 knowledge 候选化）|核对依据：`crates/archeaxis-domain/src/search.rs`（每查询重建 + API 逐行 `is_knowledge_active`）、`crates/archeaxis-application/src/jobs.rs`（完成写 `transforms`，不建 knowledge）、既有 `active_only` 回归测试|**建议**：把 R09 视为 **IMPLEMENTED_PENDING_AUDIT**，并将"候选化 + 写回版本检测"作为**依赖项**挂到 R09 之后的切片（与 R08r 决策一致）；本轮无代码变更，无需回滚|
|R10|本轮（宿主恢复 + 现状核对）|—|**DeepTutor 本地宿主已重新拉起并可用**：受管后台任务启动 `deeptutor start`（HOME/XDG_* 均指向 `.project-local/deeptutor-val`），**backend :8001 / frontend :3782 / ollama :11434 全部 HTTP 200**，前端日志显示 `Next.js 16.2.3` 与 `Ready in 0ms`（宿主日志 `.project-local/deeptutor-val/start-round46.log`）。**Core 侧"真实旅程"已存在**：`crates/archeaxis-api/tests/v01_journey.rs::v01_twelve_step_journey`（十二步闭环，全 workspace 内持续通过）。**R10 的实际缺口**：① **宿主 ↔ Core 适配**（DeepTutor 界面/能力经受控接口读写 Core，而非各写各的库）；② 人类学习界面走完整非空案例（导入→阅读定位→解释→练习/讲回→纠错→复习→保存继续）；③ 用户学习状态与 AI 能力状态**分开显示**；④ 活动成绩不可回流时**明确显示并支持人工记录**|IN_PROGRESS|端口探测 3/3 HTTP 200；前端就绪日志；Core 侧 `v01_twelve_step_journey` 既有回归；本轮为启动与核对，**未声称**界面旅程完成|下一单元：先做**适配层最小切片**（一个受控进程/接口把宿主侧输入映射为 Core API 调用：导入 + 检索 + 学习事件 + 纠错），再做界面旅程与两侧状态分离展示；宿主保持运行供下一步使用；回滚＝本行仅记账（宿主进程可随时停止）|
|R10b|本轮|—|**宿主↔Core 适配层第一片：`shared/core_client.py`** —— 宿主侧**只经 Core 自己的 API**（绝不碰 Core 数据库），提供纯请求构造 + 调用：导入（base64）、检索（`active_only` 可选、查询串 URL 编码）、学习事件（**强制持久 `client_event_id`**、`schedule_state` 作为数据交给 Core 的 FSRS worker）、学习项→修订引用、人工审校（`modified` 带 `new_body`，**不覆写旧正文**）。**边界写死并有测试**：鉴权只用 `x-archeaxis-launch-token` 头；**任何请求体都不含 actor/token**（身份是服务端声明）；**Core 不可达返回 0 与显式错误**而不抛异常（本轮实测发现并修正：原先只捕 `HTTPError`，连接失败会崩）。**引用**：`import_request` / `search_path` / `learning_event_request` / `reference_request` / `review_request` / `call`|IN_PROGRESS|pytest `tests/test_core_client.py` → **8 passed**（令牌只在头、体无 actor/token、导入 base64 与空源拒绝、检索 `active_only` 与编码、事件缺持久 id 拒绝、`modified` 带新正文且不覆写、不可达返回非 200 而非崩溃）；架构守卫通过|剩余：把该客户端接到真实宿主进程/界面（下一步），以及"用户学习状态与 AI 能力状态分离显示""成绩不可回流时人工记录"；回滚＝revert 本切片|
|R10c|本轮|—|**最小真实旅程编排落地**：`core_client.run_journey(...)` 把宿主侧旅程顺序**单点化**——可达性 → 导入样本 → 检索（`active_only=true`）→ 记学习事件 → 记录该学习项所依据的修订；**任一步失败即停止并回报 `failed_step`**（不静默跳过）；**旅程自身绝不执行人类审校**（返回 `note` 明确"review stays a human action"）。为便于无 Core 时验证，`call` 由调用方注入（依赖注入）|IN_PROGRESS|pytest `tests/test_core_client.py` → **11 passed**（新增 3 项：旅程顺序与不含审校调用、失败步停止后续、检索带 `active_only=true`）|剩余：用真实运行的 Core 做一次冒烟（需按 launch JSON 拉起 `archeaxis-api` 并读其就绪端口）；随后接真实宿主界面、两侧状态分离显示、成绩不可回流时人工记录；回滚＝revert 本切片|
|R10d|本轮|—|**真实 Core 冒烟通过（R10 从"可测"到"实测"）**：新增 `scripts/probes/r10_core_journey_smoke.py` —— 以 launch JSON 拉起**真实** `archeaxis-api <db> 0`（stdin 喂启动声明、读就绪行的端口），再用 `shared/core_client.run_journey` 走真实 HTTP。**收据（exit 0）**：`reachability GET /system/version → 200` → `import POST /imports → 202`（`source_id=src_3f83059f699322666aa2fd3d`）→ `search GET /search?q=6371&active_only=true → 200`（count 1）→ `learning_event POST /learning/events → 201` → `reference POST /learning/items/card-smoke/references → 201`（引用修订 `k_8f390d95872e39a2e2bb110b`）；事先以人工身份创建 1 条 accepted 事实（201）。**本轮发现并修掉一个真实协议细节**：Core 的 `Launch::from_stdin` 需读到 **EOF** 才继续，脚本原先未关 stdin → 进程挂起、永不报就绪；关闭 stdin 后立即就绪|IN_PROGRESS|冒烟脚本输出 JSON 收据（未打印任何 token）；宿主 DeepTutor 同时在线（8001/3782/11434 全 200）|剩余：把该适配接到**真实宿主界面**（人类学习旅程的 UI 侧），做"**用户学习状态 vs AI 能力状态**分离显示"，以及"**成绩不可回流时明确提示 + 人工记录**"；回滚＝revert 本切片（探针脚本）|
|R10e|本轮|—|**两侧状态分离 + 不可回流时的人工记录（R10 验收项 ②③）**：新增 `GET /api/v1/learning/items/:item_key/state`，返回**互不混淆**两侧——`learner`：`event_count / correct_streak / scheduled_events / unscheduled_events / references[{knowledge_id,active}]` 及 `recording` 说明（人类经学习事件端点记录；**调度不可用即存为 unscheduled 而非编造**）；`machine`：`status:"not_recorded"` + 说明「机器能力收据由机器环路写入；**绝不把学习者进度当作机器能力**」。测试同时覆盖两条路径：无卡状态→占位阶梯（计入 scheduled），调度不可用→**unscheduled**（这正是"成绩不可回流时人工记录"的落点）|IN_PROGRESS（R10 余：真实宿主界面接线）|`.project-local/runs/r50-api.log`（api **18 组全绿**，含 `item_state_api.rs`：两侧分离、机器侧显式 not_recorded、两种记录路径计数正确）；`.project-local/runs/r50-full-workspace.log`（**全 workspace exit 0 / 65 组**）|剩余：把该状态视图与旅程接到**真实宿主（DeepTutor）界面**；回滚＝revert 本切片|
|R11|本轮（勘察+决策，无代码变更）|—|**R11 现状与缺口（有据）**：**已有** —— 资格出口 `GET /knowledge-items/:id/qualification`、`is_knowledge_active`/`knowledge_status`（消费者复用前检查）、`search?active_only=true`、机器身份边界（机器只能**提候选**、不能自接受/自审校）、以及机器纠错闭环（`cbe253b`：候选→人类复核→新版本→再调用，含重启一致）；**缺** —— **全仓 Rust crates 中不存在任何 MCP/工具调用面**（`mcp|tool_call` 匹配为 0），即没有"真实外部客户端/受控工具调用"路径，也没有机器**任务收据**（任务条件、模型/知识/方法/工具版本、调用范围、失败、重测）的落点|IN_PROGRESS|只读勘察：`crates/archeaxis-api/src/lib.rs`（资格路由、机器 actor 守卫 L319/511-513、候选-only 规则 L468）、`crates/archeaxis-domain/src/knowledge.rs`（L169-208 资格/状态）、`crates/*` 无 mcp/tool_call 命中|**最小诚实切片建议**：① 加**机器任务收据**（加性表，按需创建、免 schema 升版，与 `card_references` 同法）：机器身份才能写、语义为**候选/测量**（**绝不自动接受**）；字段含任务条件、知识/方法/工具/模型版本、调用范围、结果、失败与重测；② 用 R10 的适配模式接一个**真实外部客户端**（受控工具/MCP 等价物）跑一次可观察任务；③ 评测**隔离练习与未见测试**，未测标 UNMEASURED，**不把知识更新或"取到文档/答对一次"当作训练了权重**。本轮无代码变更，无需回滚|
|R11b|本轮|—|**机器任务收据落地**：新增 `archeaxis-domain/src/machine.rs` + `machine_tasks` 表（**按需创建、免 schema 升版**，与 `card_references` 同法）；`record_machine_task` 记录任务条件、知识/方法/工具/**模型版本**、调用范围、结果、失败与 `retest_of`。**刻意设置的拒绝**：① **仅机器身份可写**（人类结果属于人类学习记录）；② 结果只能是 `succeeded/failed/unmeasured`，**`unmeasured` 是一等结果**（不把"没测"四舍五入成成功）；③ 失败必须写明原因、成功不得带失败说明；④ **收据不可改写**（同 task_id 二次写入被拒，事后不能改账）。`machine_task_counts` 返回 (总数, 未测数)，便于调用方**报告覆盖而不是暗示全部已验证**|IN_PROGRESS（余：真实外部客户端跑一次可观察任务）|`.project-local/runs/r53-domain.log`（domain **13 组全绿**，含 `machine_tasks.rs` 4 项：版本/范围入库、unmeasured 保持可见、失败须给因、非机器身份/未知结果/成功带失败/重复写入均被拒且不落库）；`.project-local/runs/r53-full-workspace.log`（**全 workspace exit 0 / 66 组**）|已知局限：按需创建 ⇒ 不在 `EXPORT_TABLES`，归档暂不携带机器收据；**未声称**：机器权重训练（收据只是任务/测量事实）；回滚＝revert 本切片|
|R11c|本轮|—|**机器收据接入 Core API（两侧可区分，接口级验证）**：新增 `POST /api/v1/machine/tasks`（**仅机器主体**可写；人类主体 → **403** 且**不落库**）与 `GET /api/v1/machine/tasks/:task_id`（**任何主体可读**——收据是关于机器侧的**证据**，不授予权限）；读回显式带 `note:"a task receipt is a measurement fact, not a claim that weights were trained"`。拒绝语义在接口层复核：失败无原因 → **400**；同 task_id 重复写入 → **400**（**收据不可改写**）；`unmeasured` 原样保留、不被汇总成成功|IN_PROGRESS（余：真实外部客户端跑一次可观察任务）|`.project-local/runs/r55-api.log`（api **19 组全绿**，含 `machine_task_api.rs` 3 项：机器写入+任意方读回+未知 404、人类写入 403 且不落库、unmeasured 保留/失败须给因/不可改写）；`.project-local/runs/r55-full-workspace.log`（**全 workspace exit 0 / 67 组**）|本轮另修：api crate 缺 `machine` 模块导入（编译期 E0433，已补 `use archeaxis_domain::{..., machine, ...}`）；回滚＝revert 本切片|
|R11d|本轮|—|**真实进程收据闭环（R11 收口）**：新增 `scripts/probes/r11_machine_receipt_smoke.py` —— 以 **`actor:"machine"` 的 launch 声明**拉起**真实** `archeaxis-api`（声明即身份；客户端头无法篡改），再经 HTTP **写入收据 `POST /machine/tasks` → 201**、**读回 `GET /machine/tasks/<id>` → 200**。收据含**任务条件、知识/方法/工具/模型版本、范围、结果**，读回带 `note:"a task receipt is a measurement fact, not a claim that weights were trained"`。**R11 验收映射**：真实进程请求+版本+结果 ✅（本轮实测）、失败例 ✅（失败须给因，接口与域层均有拒绝测试）、重测字段 ✅（`retest_of`）、**AI 不能自授人工接受** ✅（403 守卫 + 测试）、**区分知识/策略更新与权重训练** ✅（收据为测量事实并显式声明非训练）|IMPLEMENTED_PENDING_AUDIT（余：真实外部客户端/独立未见例评测）|探针输出 JSON 收据（`write_status=201`、`read_status=200`、`outcome=succeeded`、`model_version=qwen3:8b`、`scope`、note 齐全；未打印 token）|**未声称**：独立未见例上的能力改善评测（属后续）；真实 MCP 客户端（当前用受控 HTTP 客户端代表）；回滚＝revert 本切片（探针脚本）|
|R12|本轮（只读终态容量证明，未删除任何内容）|—|**同工具同口径终态盘点 + 增量归因**（`inventory_project.py`，只读元数据）：观测逻辑总量 **27.469 GiB / 152,230 文件**（26 观测错误、76 个 opaque 排除项）。最大残留：**`.project-local` 20.641 GiB / 101,176 文件**、`target` 5.729 GiB / 19,949、`.venv` 0.858 GiB、`frontend` 0.099 GiB、`data` 0.094 GiB。**`.hermes`（单列指纹）42.853 GiB / 718,077 文件，最新写入仍 2026-09-06 11:29 → 零增长**。**卷状态**：D: 空闲 **227.29 GiB**。**与 R01 口径对比（同工具）**：总量 23.423→27.469 GiB、文件 130,120→152,230、`.project-local` 16.595→20.641 GiB（+4.05 GiB）——**全部为本会话真实构建/测试/证据输出，且都落在受治理位置**（cargo 输出经封印进 `.project-local/build/cargo`、运行收据进 `.project-local/runs`）；**根 `target/` 保持 5.729 GiB 不变（止增仍有效）**；D: 空闲 231.15→227.29 GiB（−3.86 GiB，与上述构建/证据增长一致）|IN_PROGRESS（未执行删除）|收据 `.project-local/runs/r12-census.json`（UTF-16，同工具输出）；本行数据即最新一次同口径测量|**未删除任何内容**：删除获准可重建缓存仍需**逐路径授权**（本轮只列清单）；分类：可重建候选＝`.project-local/build`（cargo 输出）、遗留根 `target` 5.729 GiB、`.venv` 0.858 GiB、`.project-local/cache`；保留＝`.project-local/runs` 收据、`docs`、taskpack、`.hermes`（opaque，**未知即未知**）；**`.hermes` 未读写内容、未整体删除**；回滚＝本行仅记账|
|R13|本轮（一键启动/诊断种子，无删除）|—|**R13 第一步落地**：新增 `scripts/launch/core_launch.py` —— ① `--check`：**依赖诊断**（core 二进制、Python 解释器、三个 worker、PDF 引擎 pymupdf、**tesseract 可执行**、**仓库 tessdata eng.traineddata**、cargo 包装器）＋**端口诊断**（8001/3782/11434 是否在用）；② `--probe`：**真实拉起 Core**（随机生成 launch claim、写 stdin 后关闭以触发 EOF）、读就绪端口、随后停止；③ 无参数：前台常驻启动并打印监听地址。**依赖缺失/未构建一律具名失败并给退出码**（绝不空成功）；所有路径相对**仓库根**解析，**不从安装目录推断**|IN_PROGRESS（余：候选包本身与哈希绑定）|`--check` **exit 0**：9/9 依赖 ok，且 8001/3782/11434 均报"在用"（宿主在线）；`--probe` **exit 0**：`requested_port=64436` → `ready_port=64436`、`ok=true`（真实进程启动并报就绪）|**未声称**：Windows 候选安装包、同源交付物哈希绑定、一键停止/恢复/卸载说明（R13 后续）；**R12 删除仍未获逐路径授权，本轮未删除任何内容**；回滚＝revert 本切片（脚本）|
|R13b|本轮（`c77bff5` + `100978c`）|—|**停止 / 保存恢复 / 哈希↔被测提交绑定落地**：`core_launch.py` 新增 ① `--stop`——只终止**自己记录过**的 pid，pid 被无关进程复用（镜像名非 `archeaxis-api`）时**拒绝终止**（exit 4），无记录/进程已消失＝幂等成功；② `--backup`——`VACUUM INTO` + **以 `mode=ro` 打开源库**，记录前后 sha256 与 WAL 事实，`source_unchanged` 是**测量**而非假设；会话仍在运行 exit 5、库不存在 exit 6；③ `--restore`——把被替换的库**连同其 `-wal`/`-shm` 一并移开**（**不删除**），sidecar 不符/非 SQLite 一律 exit 7，放回后**断言哈希等于备份**；④ `--manifest`——每个交付物的 `bytes`/`sha256` 绑定 `tested_source_sha`，**被跟踪工作树脏时拒绝生成**（exit 8）|IN_PROGRESS（余：安装包/签名/卸载器；宿主界面旅程属 R10）|**真实收据（`tested_source_sha=100978c`）**：`--backup` 于**真实 Core 库**（118784 B + **8272 B 未 checkpoint 的 WAL**）→ `opened_read_only=true`、`source_unchanged=true`、`wal_before==wal_after`、备份 sha256 `f0c00376c7423f87babd93e2a8d561e6096f7d7de9765540cacab80a3a881298`；`--restore --from` 该备份 → `restored=true`、`sidecar_matched=true`、`restored_sha256==backup_sha256`、被替换库及其 `-wal/-shm` 移至 `core.sqlite.replaced-20260910T165851Z*`；随后 `--probe` **ok=true**（被恢复的库仍能真实启动服务）；`--manifest` → `written=true`（2 个交付物哈希），改脏 1 个被跟踪文件后 **exit 8 且不产出文件**（实测）；`--check` 9/9 依赖 ok；pytest `tests/test_core_launch.py` **18 passed**（与该客户端测试合计 30 passed）；架构守卫通过；conventions 仍仅 2 条冻结包文件行|**本轮修掉两个真实缺陷**：① 备份原以读写方式打开源库 → 会 checkpoint 其 WAL（实测 `source_unchanged=false`），改 `mode=ro` 后不再触碰源库与日志；② 恢复原只替换主文件、把旧 `-wal`/`-shm` 留在原地（会让 SQLite 把**旧库日志重放进新库**），现随被替换库一起移开且不删除。**未声称**：MSI/EXE 安装包、代码签名、卸载器；无工具链的干净环境启动/重启恢复；**R12 删除仍未获逐路径授权，本轮未删除任何内容**；回滚＝revert `c77bff5`/`100978c`|
|R15a|本轮（`7d353fb`）|—|**语言边界真实契约检查落地**（R15/X13"语言边界有真实契约检查"）：新增 `scripts/check_language_boundaries.py`——① **归属边界**：SQL 驱动只允许 `crates/` 声明（crates 之外的 `Cargo.toml` 声明 rusqlite/sqlx/diesel 即失败）；`services/python-workers/**` 不得 import 数据库驱动、不得引用 rusqlite；`apps/ArcheAxis.Desktop/**` 不得引用 SQL 客户端包、不得 `using …Sqlite`；② **跨语言契约**：Rust/Python/JSON schema 中所有 `archeaxis.worker-*/vN` 字面量必须同版本，且等于 sidecar 协议 crate 的 `PROTOCOL_VERSION`、等于 `packages/contracts/v<N>` 目录名、等于 Python hello 的 `protocol.major`、等于 schema 中 hello 的 `major.const`（期望版本**由交付物推导**，脚本内无魔数）。**接线**：`scripts/ci/check_vnext_contracts.py`（CI `contracts-vnext` 作业实际运行的脚本）现调用它——该脚本 docstring 原先把跨语言一致性"留给后续冻结"，现改为**在此强制**|IN_PROGRESS（余：Obsidian 往返样本、16 组格式矩阵、全仓路径规范与旧资产去向）|**真实收据**（`7d353fb`）：真实仓库 `exit 0`，`protocol_version=1`、rust/python/schemas majors 均为 `[1]`、`python_hello_major=1`、`schema_hello_major=1`、envelope 名 `hello/request/response`；**真实树负例（非仅合成）**：① 放入 `services/python-workers/document/tmp_boundary_violation.py`（`import sqlite3`）→ `exit 1` 且**具名该文件**（随后删除、复跑 exit 0）；② 仅把 Rust `PROTOCOL_VERSION` 改为 2 → `exit 1`，报 `packages/contracts/v1 disagrees with PROTOCOL_VERSION=2` 与 `PROTOCOL_VERSION=2 but the envelope literals are [1]`（随后 `git checkout` 还原、复跑 exit 0）；组合门 `scripts/ci/check_vnext_contracts.py` 同样 exit 0／1；pytest `tests/test_language_boundaries.py` **16 passed**（含 12 个合成树负例）；`tests/contract` 经 `dev.py --pytest` **64 passed**；架构守卫通过；conventions 仍仅 2 条冻结包文件行|**本轮修掉自身两处"静默通过"缺陷**：① 忽略目录按**绝对路径**匹配 → pytest 临时树（位于 `.project-local/` 下）被整体跳过、负例形同虚设；改为按**树内相对路径**判定；② 桌面 SQL 规则把文本降为小写但比较串未降（永不命中），且读取未去 BOM（PowerShell 写入的 BOM 会让所有 `^` 锚定规则失效——实测真实树负例**第一次没有触发**）。**现存事实**：边界扫描 132 个文件、BOM 前缀 **0** 个（故该漏洞当前未掩盖真实违规）。**未声称**：R15 形式依赖 R14（独立 GPT 门）尚未通过，本轮属自足实现片；Obsidian 附件/链接/锚点往返样本、16 组格式矩阵、旧资产去向与路径规范未做；回滚＝revert `7d353fb`|
|R15b|本轮（`c9d1072`，无产品代码变更）|—|**Obsidian 往返真实样本落地**（R15/X12"Obsidian附件/链接/锚点往返有样本"）：提交**五文件真实保险库 fixture** `crates/archeaxis-archive/tests/fixtures/obsidian-vault/`（含 `PROVENANCE.txt` 声明为仓库内自撰样本、无任何私密材料）：`notes/index.md`（wiki 链接、别名链接、标题锚点、块引用、嵌入、指向 canvas 的 markdown 链接、外链、callout、任务列表）、`notes/atomic.md`（标题 + 块 id `^why-block`，含反向引用）、`attachments/diagram.png`（**69 B 真 PNG，字节非合法 UTF-8**，因此字节级往返才有意义）、`vault.canvas`（JSON Canvas 1.0，4 节点 2 边）。测试 `obsidian_vault_roundtrip.rs`：**先自证 fixture 是真保险库**（逐条解析 `[[…]]`/`](…)`：按路径→文件名→词干解析，标题锚点必须在目标文件里存在同名标题、块引用必须在目标文件里存在该 `^block`；内部链接 ≥8 条、**恰好 1 条外链**用于证明外链不被要求解析），再**真实导入 5 个源 → 导出归档 → 恢复到全新库**，按 digest 经 raw object store **逐文件断言字节完全相同**（名称集合亦一致），并断言 canvas 恢复后仍是可解析的 4 节点/2 边文档、索引笔记文本仍含 4 个链接样本原文|IN_PROGRESS（余：16 组格式矩阵、全仓路径规范、旧资产去向）|**真实收据**（`c9d1072`）：`cargo test -p archeaxis-archive --offline` **exit 0**，该包 **16 项测试全绿、0 失败**（6 个测试二进制：lib 7、archive_rejection 4、archive_roundtrip 1、executor_roundtrip 1、**obsidian_vault_roundtrip 1**、raw_recovery 1，另 doc-tests 0；日志 `.project-local/runs/r62-archive-test.log`）；fixture 5 文件共 4381 B，sha256 前 16 位：`diagram.png a74be423c3b9df93`、`atomic.md 91e3adc4ecc8ecc9`、`index.md 76fc557e49d76823`、`PROVENANCE.txt 139e8519e67e3d6f`、`vault.canvas 7c28a78de60bf618`；架构守卫通过；conventions 仍仅 2 条冻结包文件行|**诚实半边（以断言而非沉默表达"尚未吸收"）**：导入保险库**不写任何 anchor 行**（`anchors` 计数为 0，且注释写明"不得凭空发明未解析的锚点"）；`sqlite_master` 中**不存在**任何 link/embed 表（断言同时写明"若将来出现该表，须替换为真实的链接图检查而不是删掉此断言"）；`route_for_kind("markdown")==None`（markdown 无专用路由，走 text 路由）、`route_for_kind("canvas")==None`（**canvas 仅保管，无摄入路由**）。**未声称**：链接图/嵌入/锚点的**语义**解析与重建（本轮只证明"字节+名称+文本按原文保留"）；R15 形式依赖 R14 未运行；回滚＝revert `c9d1072`|
|R15c|本轮（`791c909`）|—|**16 组格式状态矩阵 + 可拒绝的校验器**（R15/X12"承接原16组格式，各项标完整/部分/仅保管及具体缺口"）：新增 `docs/authority/taskpack-0910-r3/R15-FORMAT-STATUS.json`——从 2026-09-07 的 `FORMAT-COVERAGE.json`（schema `archeaxis.format-retention/v1`）**逐字承接全部 16 组**（F01–F12 core、F13–F15 expansion、F16 explicit_boundary 的 `release_scope`/`formats`/`required_output` 均原样保留，**无删项**），逐项给出 `status`＋`implemented_now`＋`gap`＋`evidence`（core route 三元组 / worker route / code / tests / legacy_reference）。**结果（对当前实现）**：**complete 0**、**partial 4**（F01 文本类、F04 图片、F05 文本 PDF、F06 扫描/混合 PDF）、**custody_only 12**。配套 `scripts/check_format_matrix.py` 对**真实代码**校验：id 必须恰为 F01–F16 且顺序不变、`release_scope`/`formats`/`required_output` 必须与承接记录**逐字一致**（需求不得被悄悄改写以迁就实现）、status 词表与汇总计数必须自洽、**每一条 core route 三元组必须真实存在于 `attempts.rs` 的 `ROUTES`**、每条 worker route 的 capability→模块路径必须与 `text_ndjson.py` 的 `ROUTES` 一致、evidence 路径必须存在、`custody_only` 行**不得**声明抽取路由、`partial/complete` 行**必须**至少一条 core route＋一条测试路径、`partial/custody_only` 必须写明 gap|IN_PROGRESS（余：全仓路径规范、旧资产去向；本矩阵本身即"逐项标状态及缺口"交付）|**真实收据**（`791c909`）：`check_format_matrix.py` **exit 0**——"16 groups carried, 0 complete, 4 partial, 12 custody only; every claimed route exists in the Core and transport tables (4 core routes, 3 worker routes parsed)"（日志 `.project-local/runs/r63-matrix.log`）；pytest `tests/test_format_matrix.py` **12 passed**（1 正例＋`tests/test_language_boundaries.py` 合计 28 passed，日志 `.project-local/runs/r63-tests.log`）；`check_language_boundaries.py` 与 `check_architecture.py` 均 exit 0；ci.yml 经 YAML 解析确认新步骤落在**与架构/约定守卫同一作业 `lint`**（`static+lint+py-primary` 在未知路径时一并运行）|**校验器可拒绝性以 8 个负例证明**（每个负例都在真实矩阵副本上改一处、并对**真实路由表**校验）：删组、改写需求、**编造不存在的路由**、路由配错 worker、`custody_only` 行声明抽取、`partial` 行无路由、evidence 路径不存在、汇总计数不符、非法 status 词。**未声称**：GitHub Actions 未在本机运行（仅本地脚本与 YAML 解析通过），故不声称 CI 通过；矩阵中每一行的**充分性**判断仍属独立审计（R14），本校验器只保证机械性声明不假；R15 形式依赖 R14 未运行；回滚＝revert `791c909`|
|R15d|本轮（`25a3000`）|—|**全仓路径规范实测 + 旧资产去向登记**（R15/X13"跟踪文件100%分类…剩余旧资产逐能力登记"）：`DIRECTORY_AUTHORITY.yaml` 自带匹配语义与优先级（deny → exact-path → 最高字面段数 → 最长字面前缀，平手即 ambiguous/reject），但此前**没有任何东西**拿它对照真实被跟踪树；新增 `scripts/check_path_conventions.py` 依该声明逐条判定 `git ls-files` 全量路径。**实测（诚实结论，不四舍五入成"已完成"）**：owned **1672/1741（96.04%）**、被跟踪的 deny-commit 路径 **0**、ambiguous **0**——即仓库**并未**100% 分类：**69 条路径不匹配任何 authority 规则**，在默认 write-denied 下**没有任何写通道**，其中 `shared-contracts/**` **38**（整棵树，虽已在 LEGACY_MANIFEST 登记却未被授权文件拥有）、根文件 **20**、`.worklab` 2、`.codex.example` 2、`apps` 2（`apps/.gitignore`、`apps/desktop/README.md` 遗留）、`contracts` 2、`.cargo/config.toml` 1、`crates/README.md` 1、`archeaxis/__init__.py` 1。记录 `R15-PATH-DISPOSITION.json` **逐条列出这 69 条**，对**全部 65 个顶层条目**给出 class/disposition/owner_doc/说明（33 目录 + 32 根文件分 4 组），并对声明中的**六个 legacy root** 记录 `migration_status` 与清点实数：**LEGACY_MANIFEST 共 1246 条资产、全部仍为 INVENTORIED_NOT_SEMANTICALLY_REVIEWED**（app 183、shared 91、frontend 53、knowledge_base 38、desktop 32、src-tauri 13），因此**任何 root 都不得声称已吸收**|IN_PROGRESS（余：69 条未拥有路径交 owner 决策；旧资产**逐能力语义吸收**仍待后续切片）|**真实收据**（`25a3000`）：`check_path_conventions.py` exit 0——"1672/1741 tracked paths owned (96.04%), 69 unowned and all recorded, 0 deny-commit paths tracked, 0 ambiguous, 65 top-level entries classified, 1246 legacy assets inventoried across 6 legacy roots"（日志 `.project-local/runs/r64-path.log`）；pytest `tests/test_path_conventions.py` **17 passed**（三个门合计 44 passed）；架构／语言边界／格式矩阵三门 exit 0；conventions 回到仅 2 条冻结包文件行|**设计取舍（写明以免后人误改）**：快照总量（tracked/owned/coverage）**仅作 informational** 并附 `measured_at_commit`——否则每次普通提交都会让门变红（实测：本切片落地后 1744/1675 仍 96.04% 不触发）；**被强制**的是：未拥有集合必须与记录**逐一相等**（新增未拥有路径立即失败，直到有人分类或登记）、deny／ambiguous 恒为 0、每个顶层条目**恰好一条**规则且 `owner_doc` 必须存在、六个 legacy root 的清点数与 `LEGACY_MANIFEST.yaml` 实际值相等（清点数字不会静默过期）。**本轮修掉自己一个真实缺陷**：生成器以 Windows 文本模式写记录 → 工作副本 CRLF，被 conventions 门抓出 `crlf` 问题（提交内容本已被 git 归一为 LF），已改为 LF 写入。**未声称**：`DIRECTORY_AUTHORITY.yaml` 是**受保护治理文件，本轮未修改它**（69 条缺口属 owner 决策）；旧资产的**语义吸收**未做（1246 条全部仍为未复核）；CI 未在本机运行；回滚＝revert `25a3000`|
|R00b|本轮（`d482c8`；验证片，无产品代码变更）|—|**① 全 workspace 回归复跑**（上一次整体复跑为 R11c 的 67 组，其后新增了 R08/R09/R11/R15 的 Rust 测试与 fixture）：`cargo test --workspace --offline` **exit 0**。**② 任务包完整性复核**：出厂 `verify_package.py` 对 MANIFEST 全部 23 项逐一比对哈希，其中 **`EXECUTION.md` 与 `STATE.json` 正是本包自己指定的"进度写入"活文件**（见其开头"进度写入本文件与 STATE.json"），因此只要记录过任何进展它就必然 `AssertionError: EXECUTION.md`——**看起来像包被破坏，实际是台账在正常工作**。新增 `scripts/check_taskpack_integrity.py` 把两件事拆开：冻结项必须逐字节等于记录值；两个活文件必须等于**安装快照**（`.project-local/runs/taskpack-0910-shipped/`）的哈希，从而"变化"可归因于增长而非来历不明的编辑；并继续复核计划结构（id 唯一、依赖图无环且可全序、**23 项前序任务全部仍有归属**）|IMPLEMENTED_PENDING_AUDIT（验证片）|**真实收据**（`d482c8`，日志 `.project-local/runs/r65-full-workspace.log`、`r65-pack.log`）：全 workspace **exit 0**——**68 个 test-result 组全部 `ok`**、**149 passed / 0 failed / 0 ignored**、`error[`／`panicked` 行 **0**、doc-tests 已运行（组数由 R11c 的 67 增至 68，新增即 R15b 的 `obsidian_vault_roundtrip`）；包完整性 exit 0——"21 frozen pack files match their recorded hashes；the live progress files match the install snapshot and grew as expected（EXECUTION.md +71037 bytes、STATE.json +71010 bytes）；plan intact（52 ledger rows, 17 slices）"；`frozen_changed: []`（**冻结内容 0 处变化**）；pytest `tests/test_taskpack_integrity.py` **8 passed**；conventions 仍仅 2 条冻结包文件行；路径门 exit 0|**过程纠正（我自己的）**：第一次统计用 PowerShell `-match`（**大小写不敏感**）判失败组，把 "0 failed" 也匹配成失败，得出"68 组全部失败"的荒谬结论；改用 Python 精确解析后为 **0 失败**。**未声称**：`check_taskpack_integrity.py` **刻意不接入 CI**——它依赖 `.project-local/runs/taskpack-0910-shipped/`（被 gitignore、全新检出中不存在），在 CI 中它会正确地拒绝归因，故仅作本地审计工具（已写入脚本 docstring）；本轮未改任何产品实现；回滚＝revert `d482c8`|
|R10f|本轮（`e442f56`）|—|**宿主界面接线的项目侧落地（R10 最后一项）**：先核实边界——`docs/integrations/DEEPTUTOR_PRODUCT_BASE.md` 记录 DeepTutor **v1.5.17 为不可改的上游依赖**（外部库路径、未提交、**当前未打任何上游补丁**），并写明"branding／route mounting／authority-adapter injection 属**项目侧**集成工作"；因此**不去改 DeepTutor 界面**，而是交付**可挂载的项目侧投影面**：新增 `scripts/host/journey_panel.py`（仅监听 127.0.0.1）——`GET /` 一页两栏、`GET /api/state?item_key=`、`GET /api/health`；数据经 `shared/core_client.py` 读取 Core（**前缀取自适配器的 `BASE`，不再手抄一份**）。**诚实规则**：Core 不可达或返回非 200 时一律 **503 且 `learner`/`machine` 为 `null`**＋页面横幅，**绝不渲染成"事件数 0"这种像进度又像数据的假象**；两栏是同一 Core 的**两个独立读数**，学习者进度**永不**呈现为机器能力；启动令牌只从**环境变量**读取（`--token-env`），**不进页面、不受 query 参数影响**，访问日志**丢弃 `?` 之后全部内容**，绝不打印令牌|IMPLEMENTED_PENDING_AUDIT（R10 至此：适配器＋旅程＋两态分离＋可挂载投影面齐备；**DeepTutor 屏幕本身未修改**）|**真实收据**（`e442f56`，`scripts/probes/r10_host_panel_smoke.py` **exit 0**）：真实 Core 启动并就绪（`core_ready_port` 实测）→ 真实旅程 `steps=[200,202,200,200]`（reachability/import/search/learning_event；因当时无 accepted 知识，`references` 为空并被如实报告）→ 面板 `page_status=200`、`page_has_both_panels=true`、`health_status=200`（`reachable=true`）、`state_status=200`（`learner.event_count=1`、`machine.status="not_recorded"`）；**负例**：把面板指向一个已关闭端口 → `http_status=503`、`learner=null`、`machine=null`、`core.reason` 为真实连接被拒原文；pytest `tests/test_host_journey_panel.py` **9 passed**；架构／路径／语言边界／格式矩阵**四门 exit 0**；conventions 仍仅 2 条冻结包文件行|**冒烟与测试各抓到我一个真实缺陷**：① 面板最初调用 `/system/version` **漏了 `/api/v1` 前缀** → Core 返回 404，面板**照设计拒渲染**（503＋null）而非显示 0；② 测试发现 `urllib.parse.quote()` 默认**不转义 `/`**，`item_key` 含斜杠会**多加路径段** → 改为 `quote(item_key, safe="")`。**未声称**：DeepTutor 界面/品牌/路由挂载未改（上游不可改，仅提供项目侧 URL）；未在本机跑 GitHub Actions；回滚＝revert `e442f56`|
|R15e|本轮（`b9df0ed`）|—|**F04 真能力推进：媒体类型按来源文件名推导，不再按 kind 写死**（闭掉格式矩阵 F04 的一条具体缺口）：Core 原先对**每个 image 作业**都向 OCR worker 声明 `image/png`，即使 worker 接受 png/jpeg/tiff/webp/bmp——即 JPEG 被**错误标注**为 PNG。现在 `claim()` 读取来源的 `original_name`，经 `attempts::media_type_for_name` 依扩展名推导媒体类型，并由新增 `ROUTE_MEDIA_TYPES`（**镜像传输层自己声明的接受集**：text 7 种、pdf 1 种、image 5 种）校验；**推导不出或路由不接受时一律拒绝**（新错误 `JobError::MediaTypeNotAccepted`，列出可接受集合，API 映射 **400**），且**作业保持 queued**——绝不带错误媒体类型下发|IN_PROGRESS（F04 仍为 partial：余 reading order 与图示描述；16 组矩阵本次由校验器复核通过）|**真实收据**（`b9df0ed`）：`cargo test -p archeaxis-application` **exit 0 全绿**（`route_capabilities.rs` 扩到覆盖**全部 10 组 kind/name/media**、推导表、PDF 作业配 `.png` 名的**具名拒绝**、`input.bin`/`clipboard`/`scan.dat` 的**未命名扩展名拒绝**，且被拒后作业仍 `queued`）；全 workspace **exit 0 / 68 组 / 152 passed / 0 failed / 0 error**（较第 65 轮的 149 增加 3 项即本轮 Rust 测试）；`scripts/runtime/dev.py --pytest tests/test_image_media_types.py` **7 passed**——用 Pillow 渲染同一张合成截图，分别以 **png/jpg/tiff/webp/bmp** 五格式送进**真实 Tesseract**，均返回文本且含 `6371`、行锚点齐、覆盖计数一致、保留词框、无 accuracy 字样；格式矩阵门 **exit 0**（16 组、0 complete、4 partial、12 custody_only，所有声明路由仍在 Core/传输表中）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**诚实边界（写进矩阵与评审面）**：**`image/gif` 等不在声明集内的格式不会被静默接受**；**扩展名无法命名时拒绝而不是猜测**（例如无扩展名的剪贴板截图必须由调用方给出表明类型的名字）。**本轮一次自纠**：我先把 `receipt_spans.rs` 的 fixture 名从 `sample.pdf` 改成 `sample.txt`，随即被测试以 `MediaTypeNotAccepted{kind:"pdf",name:"sample.txt"}` 拒绝——该测试本就是 **pdf 作业**，已改回 `.pdf`（**新的推导规则当场抓出了我改错的名字**）。**未声称**：F04 未达 complete（无阅读顺序/图示描述）；未跑 GitHub Actions；回滚＝revert `b9df0ed`|
|R15f|本轮（`9ac1db9`）|—|**F01 真能力推进：文本路由报告它能真推导出的结构**：投影仍保持**规范形**（text＋行锚点、按行覆盖），格式事实放入 `loss_receipt.params.format`——markdown：标题（层级＋**行号**）、wiki 链接／嵌入／markdown 链接／代码围栏／列表项计数；CSV/TSV：分隔符、行数、列数、表头、**ragged 行数**；JSON：解析结果＋顶层类型／键数／嵌套深度；XML：解析结果＋根标签／元素数／深度。**解析失败不伪装**：文档仍按文本投影，事实标 `parsed:false`＋原因，并把原因**同时并入 losses**；`![[embed]]` **不再被重复计入 wiki 链接**（测试当场抓出该双计）。传输层新增 `media_type_arg`：**把 Core 声明的媒体类型交给需要的 worker**，不再让 worker 猜文件名|IN_PROGRESS（F01 仍 partial：余代码符号/表格结构与本地模型标注）|**真实收据**（`9ac1db9`）：pytest `tests/test_text_format_facts.py` **10 passed**（6 种媒体类型下**投影契约不变**：text 逐字节等于输入、行锚点、`covered==total==len(structure)`、`coverage==1.0`、`params.media_type` 回显；markdown 标题行号 `[5,10]` 且**围栏内 `#` 不算标题**、未闭合围栏有具名 note；CSV 5 行/4 列/`ragged_rows==2`；TSV 制表符分隔；JSON `key_count==4`、深度**按"容器层数、最外层为 1"**并带 `depth_unit`；坏 JSON → `parsed:false`＋"line 2"＋文本仍完整投影＋losses 含该项；XML 根/元素/子元素/深度；**直驱传输层路由**验证声明媒体类型确实到达 worker）；worker/传输相关套件 **37 passed + 47 subtests**；全 workspace **exit 0 / 68 组 / 152 passed / 0 failed / 0 error**；格式矩阵门 exit 0（F01 行同期更新，计数不变）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮三处自纠（全部由测试/运行抓出）**：① `![[diagram.png]]` 被 **wiki 链接与嵌入各计一次**（双计）→ 链接正则加负向后顾；② JSON 深度定义含标量而虚高 → 明确为**容器层数**并加 `depth_unit` 说明；③ `Element.iterancestors()` 在 CPython 的 C 加速实现上**不存在**（AttributeError）→ 改自写递归深度。**未声称**：F01 未达 complete；格式事实**不是第二套寻址**（锚点仍为行）；未跑 GitHub Actions；回滚＝revert `9ac1db9`|
|R15g|本轮（`54c7c21`）|—|**F05/F06 真能力推进：PDF 路由报告引擎真正暴露的页结构**：投影仍为规范形（text＋**页/行锚点**、按行覆盖），事实写入 `loss_receipt.params.structure`——逐页字符数与**引擎块数**、**图片引用数**、**引擎文本块**（bbox＋首行＋字符数）、**引擎自带表格检测器**检出的表（行数/列数）；每个字段都标明是**测量**而非语义（"a text block is an engine block, not a linguistic paragraph"、"a detection is not a guarantee"）；截断一律带 `capped` 事实。F06 侧：无文本页在 `pages_without_text` 之外**新增逐页字符数**，"哪些页需要 OCR"成为事实。**关键修复**：**引擎噪声被收进回执而不是打到 stdout**——PyMuPDF 的表格检测器会往 stdout 打印一行 layout 提示，导致 Core 端到端 PDF 作业以 **"unexpected trailing worker output"** 失败（**正是该 e2e 抓到了这个协议级缺陷**）|IN_PROGRESS（F05/F06 仍 partial：块≠语言段落、检测≠保证、页仍非寻址层级；PDF 仍不自动串 OCR）|**真实收据**（`54c7c21`）：pytest `tests/test_worker_pdf_structure.py` **8 passed**（两块 PDF：`text_block_count==2`、首行含原文、bbox 四元、`page_facts[0].chars==76`、`image_reference_count==1`；投影契约不变：页/行锚点、`covered==total==2`、`coverage==1.0`、无 accuracy；**规则网格 PDF：`table_support==true` 且检出 `rows=2, cols=2`**；纯图页：文本为空、`pages_without_text==[1]`、`page_facts[0].chars==0`、loss 点名 OCR；损坏 PDF 仍抛错；**子进程级断言 stdout 恰为一行 JSON**）；PDF 相关套件 14 passed；`cargo test -p archeaxis-application` **exit 0**（此前被引擎噪声打断的 `pdf_job_end_to_end` 现已通过）；全 workspace **exit 0 / 68 组 / 152 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**F05、F06 行同期更新**，计数不变）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮我自己的两次事故（都如实记下）**：① 用 PowerShell 单引号字符串做多行替换，把字面 `` `r`n `` 写进 worker（第 141 行语法错误）；随后我用 Python 修它时又被 PowerShell 的反引号转义吞掉，**把整个文件写坏（13,076 行）**——已 `git checkout` 还原，改用 **edit 工具**重做全部改动并逐次校验；**教训：多行改动用 edit 工具，绝不用 PowerShell 字符串替换**。② 引擎噪声缺陷由既有 e2e 抓出，证明"委托给真实端到端"比自查更可靠。**未声称**：F05/F06 未达 complete；未跑 GitHub Actions；回滚＝revert `54c7c21`|
|R15h|本轮（`a068a01`）|—|**F12 从"仅保管"升为"部分"：canvas 节点/边与字幕时间线索作为事实报告**：`.canvas` 现按它本来的样子解析为 **JSON 文档**，文本路由报告**节点/边计数、节点类型、重复节点 id、以及指向不存在节点的边（dangling）**——这是**完整性测量**而非图模型（"an edge naming a missing node is reported, not ignored"）；`.srt`/`.vtt` 按文本解析，报告 **cue 数、首/末 cue 时间**、SRT 或 WebVTT，并**明确标注"按模式检测、非媒体类型声明"**、cue 时间**是事实不是锚点**。字节保管不变|IN_PROGRESS（F12 仍 partial：没有任何时间锚或 canvas 节点/边被存为结构或关系，故 cue/节点仍**未与知识相连**，导航仍靠行锚点）|**真实收据**（`a068a01`）：pytest `tests/test_text_format_facts.py` **17 passed**（canvas：`node_count==4`、`edge_count==2`、`node_types=={file:3,text:1}`、`duplicate_node_ids==["n-atomic"]`、`dangling_edges==[{"edge":"e-2","missing":"n-missing"}]`；普通 JSON **不会被叫作 canvas**；坏 canvas JSON → `parsed:false` 且无 canvas 段；SRT 2 cues、`first_cue_start==00:00:01,000`、`last_cue_end==00:00:07,000`；WEBVTT 由头识别；空 WEBVTT 明说"no cues"；**只有编号没有时间箭头的文本仍是 plain**）；三套结构测试合计 **32 passed**；`cargo test -p archeaxis-application` exit 0（新增 Rust 测试固定 `.canvas→application/json`、`.srt/.vtt→text/plain` 及其被错路由拒绝）；全 workspace **exit 0 / 68 组 / 153 passed / 0 failed / 0 error**；**格式矩阵门抓到我自己漏改的 `coverage_summary`**（F12 改判后计数应为 5 partial/11 custody）并已修正，门现 exit 0；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**未声称**：F12 未达 complete（时间锚/图关系仍未落为结构）；cue/节点事实**不是第二套寻址**；未跑 GitHub Actions；回滚＝revert `a068a01`|
|R15i|本轮（`f88d0b9`）|—|**F06 闭环：扫描 PDF 真的串进 OCR**（不是"人力再排一个作业"）：PDF worker 把**读不出文本的页渲染成 PNG** 并在回执里**逐页声明 digest 与字节数**；新增 Core 侧 `crates/archeaxis-application/src/ocr.rs`——`candidates()` 读回声明、`enqueue_pages()` **逐页校验 digest 与字节数**、把每页**导入为独立源**（命名 `<stem>-page-N.png`，使媒体类型推导认得它）、**每页入队一个 image 作业**且**幂等**（重复调用不重复入队）。**职责切分不变**：worker 渲染（只有它持有 PDF 引擎）、Core 入队（只有它写库）——**worker 永不持数据库句柄**。**不可验证的输入一律拒绝**（缺文件／digest 或字节数不符／试图越出传输区的名字）并**不留下任何排队作业**|IN_PROGRESS（F06 仍 partial：串接是作业完成后的 Core 调用，**执行器不会自动触发**；低置信区域仍未标注）|**真实收据**（`f88d0b9`）：`cargo test -p archeaxis-application` **exit 0**，新增 `pdf_ocr_chain.rs` 两个 e2e 全绿：① 真实 PyMuPDF+PIL 造出**只有图片、无文本层**的扫描页 → PDF 作业声明 1 个候选（`page 1`、`image/png`、64 位 digest）→ `enqueue_pages` 返回 `["job-pdf-page-1"]`，**再次调用返回空**（幂等）→ 该作业 `kind=="image"`、源名 `scan-page-1.png` → **执行后经真实 Tesseract 存下文本且含 `6371`**；② 有文本的 PDF **声明 0 个候选**、不入队任何作业；③ **篡改渲染文件后拒绝**（`cannot enqueue work`）且 `job-tamper-page-%` 作业数为 **0**；④ 越界文件名同样拒绝。pytest `tests/test_worker_pdf_structure.py` **11 passed**（含候选声明：文件真实存在、字节数与 sha256 与声明一致、确为 PNG、`ocr_render.dpi==150`、无文本 PDF **一个候选都不声明且不留文件**、无 artifact 目录时**不写任何东西**）；生产套件 37 passed；全 workspace **exit 0 / 69 组 / 155 passed / 0 failed / 0 error**；格式矩阵门 exit 0（F06 行同期更新）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**构建中我遇到并修掉两个真实缺陷**：① 渲染文件原本写在**每次尝试的临时目录**里，尝试结束即被删除 → Core 校验时"unreadable"；改为由执行器显式传 **`--artifact-root`**（把外层 staging 作为持久传输区）；② 该标志最初**无条件传给所有 worker**，导致 text/OCR worker 的 CLI 以"unrecognized arguments"拒绝启动 → 改为**只传给声明需要的路由**（`ARTIFACT_ROOT_CAPABILITIES = ["pdf.extract"]`），其余启动形态保持不变、未知参数仍是错误。**未声称**：F06 未达 complete；未跑 GitHub Actions；回滚＝revert `f88d0b9`|
|R15j|本轮（`3937f2b`）|—|**F06 串接改为自动：执行器在 PDF 作业完成的同一提交内完成入队**：新增 `attempts::CHAINED_AFTER_SUCCESS = ["pdf.extract"]`；`execute_owned` 在 `attempts::finish` **成功之后、同一事务闭包内**调用 `ocr::enqueue_pages`——因此**不存在"作业已报成功但声明页从未入队"的窗口**。**串接失败不失败作业**（提取与声明确实发生了），而是写一条**机器收据**（`machine_tasks`，`outcome=failed`＋原因，`method_version=ocr.enqueue_pages/v1`、`tool_version=pymupdf`、`model_version="not-a-model: deterministic core chaining"`），使其成为**读者查得到的事实而非沉默**|IN_PROGRESS（F06 仍 partial：**低置信区域仍未标注**；串接只走一步——OCR 结果不回填到它来自的那一页）|**真实收据**（`3937f2b`）：`cargo test -p archeaxis-application` **exit 0**，`pdf_ocr_chain.rs` **3 个 e2e 全绿**——① 扫描页 PDF 执行后**无需任何手动调用**即存在 `job-pdf-page-1`（`kind=image`、源名 `scan-page-1.png`）；② **再次调用 `enqueue_pages` 返回空**且 `job-pdf-page-%` 作业数恰为 **1**（幂等，不多不少）；③ 执行该作业经**真实 Tesseract** 存下含 `6371` 的文本；④ 有文本的 PDF 声明 0 候选、不入队；⑤ **篡改渲染后被拒**且已入队的仍是自动链的那 1 个作业；⑥ **阻断渲染**（把 `ocr/page-1.png` 预建为目录）→ **PDF 作业保持成功**、`job-blocked-ocr-chain` 收据 `outcome=failed`、原因**点名页**、`model_version` 明示**不是模型调用**；全 workspace **exit 0 / 69 组 / 156 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**F06 行同期更新**：缺口改为"低置信区域未标注 + 串接仅一步"）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮一次自纠**：`enqueue_pages` 改为自动后，测试里"手动调用应返回作业 id"的旧断言立即失败（实际返回空，因为自动链已经入队）——**测试比我先发现语义已变**，已把断言改为"第二次调用必须为空 + 作业数恰为 1"，并把诊断信息（链收据）一并放进断言消息。**未声称**：F06 未达 complete；未跑 GitHub Actions；回滚＝revert `3937f2b`|
|R15k|本轮（`f2b25fe`）|—|**F04/F06 的"低置信区"以诚实形式落地**：OCR 回执原本已有逐词框与引擎分数，缺的是"低置信区"的**诚实形态**。现报告 `params.review`：**具名复核阈值 60.0**、**低于阈值的区域清单**（框＋字符跨度＋该区自己的分数）、`scored_region_count`／`unscored_region_count`（**未给分的区域单独计数，不当作"没问题"**）、`lowest_score`、超限带 `capped` 事实，并在 losses 中**点名有多少区域低于阈值**。措辞是刻意的：阈值说明写为 **"a review threshold … it is not a correctness measure"**——**"accuracy" 一词在回执中完全不出现**|IN_PROGRESS（F04 仍 partial：无阅读顺序模型与图示描述；分数只用于**排序复核工作**，不作为判定）|**真实收据**（`f2b25fe`）：pytest OCR 相关套件 **19 passed**；真实引擎取样：常规截图 3 区域、分数 `[21.4, 16.6, 92.2]`、**2 个低于 60**、`lowest_score=16.6`、losses 含"2 region(s) scored below the review threshold"；**测试固化了本轮最有价值的事实**：加噪降质的图被识别成 **`"nT"`（错的）却拿到 68.0（高于阈值）**——所以"高分 ≠ 正确"，这也正是回执只给"复核阈值"而不给"正确率"的原因；断言同时覆盖计数自洽、清单内每项确实低于阈值、`bbox` 四元齐备、框内字符跨度落在文本范围内、**回执不含 accuracy 字样**；全 workspace **exit 0 / 69 组 / 156 passed / 0 failed / 0 error**；格式矩阵门 exit 0（F04／F06 行同期更新）；架构／路径／语言边界门 exit 0；conventions 仍仅 2 条冻结包文件行|**措辞自纠**：我第一版写 "it is not an accuracy figure"，**被该路由自己的断言挡下**（回执中不得出现 accuracy 字样）——最终改为**完全不使用该词**（比"加限定词"更强）；另我最初假设"图越噪低分区越多"，实测**相反**（降质图只识别出 1 个区域且分数 68.0，高于阈值），已把该错误假设从测试中删除，改为固化实测事实。**未声称**：F04/F06 未达 complete；未跑 GitHub Actions；回滚＝revert `f2b25fe`|
|R14prep|本轮（`48a6f9e`；为独立门准备证据，**不是**任何审计通过）|—|**① 可校验的证据索引**：原 markdown 索引停在 R05，而台账已到 61 行 → 权威索引改为 `R14-EVIDENCE-INDEX.json`（17 切片，状态**从 STATE.json 复制**而非重打），配套 `scripts/check_evidence_index.py` 拒绝以下情形：缺切片、状态与 STATE.json 不一致、**证据只依赖被忽略的本地收据**（全新检出无法复核）、被引用的受跟踪路径不存在、切片未写限制或未给可复跑命令、命令指向不存在的脚本、**任一独立门（R14/R16）被本仓库记为通过**。**② 顺带补上一个真实可复现性缺口**：此前**没有任何受跟踪的方式跑 Rust 测试**（只有 `.project-local/runs/` 下被忽略的按次生成包装器）→ 新增 `scripts/ci/cargo_test.bat`：读 `ARCHEAXIS_MSVC_VCVARS`/`ARCHEAXIS_RUST_TOOLCHAINS`/`ARCHEAXIS_PYTHON`，把 `CARGO_TARGET_DIR` 钉到 `.project-local/build/cargo`（避免 pitfall 3 的工作树窜台），未给子命令时自动补 `test`，**找不到工具链时具名失败 exit 2**|IMPLEMENTED_PENDING_AUDIT（**索引本身已交付并通过其自身门**；R14/R16 仍为 TODO，须由独立 GPT 裁决）|**真实收据**（`48a6f9e`）：`check_evidence_index.py` exit 0——"17 slices, 49 tracked evidence pointers, 3 receipts, every status agrees with STATE.json, no independent gate is self-signed and every slice states its limits"；pytest `tests/test_evidence_index.py` **14 passed**（11 个负例：缺切片、状态冲突、**自签 R14 为 PASS**、未指名独立审计者、只靠本地收据、收据不在 `.project-local/` 下、受跟踪路径不存在、无命令/无限制、命令指向不存在脚本、无 `not_claimed`）；`scripts/ci/cargo_test.bat` 实测：带环境变量 `-p archeaxis-contracts --offline` **exit 0**，不带环境变量**具名失败 exit 2**；其余四门 exit 0；conventions 仍仅 2 条冻结包文件行|**未声称**：**这不是审计通过**——R14/R16 仍为 `TODO`，索引只提供独立审计所需的指针，且**永不**由本仓库自签；索引里的状态快照与 STATE.json 的一致性由门强制，但**证据的分量由独立审计判断**；回滚＝revert `48a6f9e`|
|R15l|本轮（`f3d613c`）|—|**F13 的邮件部分由"仅保管"升为"部分"**：`.eml` 按其本来面目解析为**文本**，文本路由报告其 **RFC 822 结构事实**——识别出的头字段（from/to/cc/subject/date/message-id）、头字段计数、MIME **part 数**、是否存在 text 与 html 正文、以及**附件清单（名称＋字节数，明确"仅列出、不抽取"）**；消息**逐字节原样投影**，因此原始文件仍是记录之源。检测本身**标明是检测**（"RFC 822 header shape, not by media type"）。**无法读取的二进制名一律拒绝**（`.msg`/`.zip`/`.epub`/`.ods`）而不是解码成噪声（新增 Rust 测试固化）|IN_PROGRESS（F13 仍 partial：RTF/ODT/ODS/ODP/EPUB 的章节/表/页结构未做；邮件附件未抽取；二进制 `.msg` 需先有 reader）|**真实收据**（`f3d613c`）：pytest `tests/test_text_format_facts.py` **20 passed**——真实 multipart 邮件：`subject`／`message-id` 正确、`header_count==5`、`part_count==4`、`text_body` 与 `html_body` 均为真、**附件 `data.csv` 带字节数**、note 含 "NOT extracted"、**投影文本与文件逐字节相同**且 `covered==total`；**检测负例**：`"From: hi\n"` **不再**被当成邮件（这是首轮探针抓到的真实弱点：原规则过松，现要求"空行分隔的头块 ＋ 结构化头（地址/message-id/日期）或 ≥2 个识别头"）；坏邮件仍被投影且回执自洽；Rust 侧固定 `.eml→text/plain` 与四个二进制扩展名的具名拒绝；`cargo test -p archeaxis-application` exit 0；全 workspace **exit 0 / 69 组 / 157 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**F13 行同期改判，计数更新为 0 complete / 6 partial / 10 custody_only**）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**未声称**：F13 未达 complete；附件**未被抽取**（仅列出）；`.msg`/`.zip`/`.epub`/`.ods` 仍无 reader；未跑 GitHub Actions；回滚＝revert `f3d613c`|
|R15m|本轮（`f55ddf0`）|—|**F15 由"仅保管"升为"部分"：容器获得自己的路由，绝不解码成文本**（ZIP 是二进制，按文本解码只会产生"像内容"的噪声）：新增能力 **`archive.inventory`**（含独立 worker、engine profile `python-worker-archive`/`0.1.0`、媒体类型 `application/zip`）。**投影＝清单**：每行一个成员（`name<TAB>size`）＋规范行锚点；**回执明说**"这是容器的清单，**不是**成员内容"、"**没有任何成员被抽取、执行或解码**"、"容器自身字节仍是记录之源"。事实字段：成员/文件/目录计数、未压缩与压缩总字节、压缩方法集合、**嵌套容器**、**加密成员**。**坏容器 → 作业失败且不留任何 text 产物**；**空容器 → 拒绝**（不报告"空成功"）|IN_PROGRESS（F15 仍 partial：**容器→文件→知识的来源关系尚未存在**——清单只是回执事实，成员尚未各自成为源；"目录批量"仍未实现）|**真实收据**（`f55ddf0`）：pytest `tests/test_worker_archive_route.py` **7 passed**（清单逐行与锚点、`member_count==4`/`file_count==3`/`directory_count==1`/`compression_methods==["deflate"]`、成员大小与内容一致、**回执注明"NOT the members' contents"**、嵌套容器"listed, not opened"且内层成员名**不出现在投影中**、坏容器抛错、空容器拒绝、传输层声明归档能力且**文本路由不接受 `application/zip`**、worker stdout 恰一行信封）；Rust `archive_job_end_to_end.rs` **3 个 e2e 全绿**（`.zip` 名字选中归档路由且**文本路由拒绝它**、真实 ZIP 作业经执行器落库且**投影不含成员内容 `6371`**、回执含 engine `python-worker-archive`、坏容器作业 `failed` 且 `job_outputs` 中**无 text 产物**）；全 workspace **exit 0 / 70 组 / 160 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**F15 行同期改判，计数更新为 0 complete / 7 partial / 9 custody_only**，并解析到 **5 条 core 路由／4 条 worker 路由**）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**两个门先后抓到我的遗漏（正面证明门有效）**：① 执行器的**受控身份清单**拒绝新 worker 身份（`unexpected worker identity`）→ 声明 `python-worker-archive-ndjson` 后才通过；② 第 75 轮我写的"`.zip` 没有任何媒体类型"断言**以好的方式过期**——现在 `.zip` 有了自己的路由，断言改为"文本路由拒绝它、归档路由拥有它"。**未声称**：F15 未达 complete（成员未成为源、目录批量未做）；未跑 GitHub Actions；回滚＝revert `f55ddf0`|
|R15n|本轮（`dd89d77`）|—|**F15 的后半：容器成员各自成为源，并记录"来自哪个容器"**：归档 worker 把**可读成员抽取到传输区**并**逐成员声明 digest＋字节数**；新增 Core 模块 `crates/archeaxis-application/src/container.rs`——**逐成员校验 digest 与字节数** → **每个成员导入为独立源** → **记录来源**（`origin_kind="import"`，`origin_ref="<container source_id>#<member path>"`，因为 store 的来源词表是固定的）→ **对名字能解析到路由的成员各入队一个作业**；**名字解析不到路由的成员照样保留为源**并报告为 **custody-only**（"保留了但读不了"是事实，丢掉则是看不见的损失）。**篡改成员 → 拒绝且不留排队作业**。同时把路由的产物目录标志改为**路由中立**（`artifact_dir`/`artifact_kwarg`，不再复用 OCR 专用名），并把归档能力加入 `ARTIFACT_ROOT_CAPABILITIES`|IN_PROGRESS（F15 仍 partial：无"目录批量"摄入；成员关系是**来源引用**而非一等 container→file 边；超出声明上限的成员不展开）|**真实收据**（`dd89d77`）：`cargo test -p archeaxis-domain` exit 0（新增 `an_origin_kind_outside_the_vocabulary_is_refused_instead_of_ignored`）；`cargo test -p archeaxis-application` exit 0（`container_members.rs` 2 个 e2e 全绿：3 个成员→3 个源、**2 个入队**（text＋image）、`opaque/blob.bin` 归入 custody-only、**每条 `source_origins` 记录的 origin_ref 以容器 source_id 开头**、**二次展开不再入队**（幂等）、执行成员 text 作业后文本含 `6371`；篡改成员 → `cannot enqueue work` 且 `job-zip-member-%` 计数为 **0**）；全 workspace **exit 0 / 71 组 / 163 passed / 0 failed / 0 error**；格式矩阵门 exit 0（F15 行同期更新，计数不变 0 complete / 7 partial / 9 custody-only）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮抓到一个"静默丢失"陷阱（重要）**：`source_origins` 有 `CHECK(origin_kind IN ('path','url','import','manual'))`，而写入用 **`INSERT OR IGNORE`**——我最初用的 `"archive-member"` **违反约束却被静默丢弃**：代码以为记录了来源，实际什么都没写（**测试先发现：origin 查询返回 0 行**）。修法两层：① `expand_members` **导入后回读校验关系确实落库**，否则报错；② `record_origin` **校验词表并具名失败**（新增 domain 测试，且失败时**不留半个源**）。另：本切片还暴露"未声明就收不到 `--artifact-root`"——`declared_members` 起初返回 0，把 `archive.inventory` 加入 `ARTIFACT_ROOT_CAPABILITIES` 后才拿到成员（**这是该闸门按设计工作**）。**未声称**：F15 未达 complete；未跑 GitHub Actions；回滚＝revert `dd89d77`|
|R15o|本轮（`4df2158`）|—|**"容器→文件"关系变为可见（可查询出口）**：新增 `GET /api/v1/sources/:source_id/members`——回答**这个容器里有什么、哪些被读过了**：成员各自的 `source_id`／成员路径／digest／**`readable`（是否存在 transform）**／已入队的 `job_id`，并给出计数与**两句明写的限制**：① 这是**已导入的成员**、**不是容器的完整清单**（超上限、加密与不可读成员由归档作业自己的回执报告）；② **`readable` 只表示存在 transform，不表示内容被理解**。未知源 → **具名 404**。Core 侧 `container::members_of` **从 origin 引用还原成员名**，因此**不引入第二张关系表**|IN_PROGRESS（F15 仍 partial：无"目录批量"摄入；成员关系仍是**来源引用**而非一等 container→file 边；被读取的成员**仍未升级为知识**）|**真实收据**（`4df2158`）：`cargo test -p archeaxis-api` **exit 0**（`source_members_api.rs` 2 项：2 成员→`member_count==2`／`readable_count==1`／`custody_only_count==1`、`notes/index.md` 为 `readable` 且带 `job_id`、`opaque/blob.bin` 为**只读不可读且 `job_id` 为 null**、digest 64 位、note 同时含 "not its whole inventory" 与 "a transform exists"；未知源 → **404**；未展开的容器 → 全 0 且成员数组为空）；`cargo test -p archeaxis-application` **exit 0**（`container_members.rs` 扩展：展开后 `members_of` 返回 3 个成员，**已执行 text 作业的成员 `readable==true` 且 job_id 指向该作业**、image 成员有作业但未读、`opaque/blob.bin` **无作业且不可读**）；全 workspace **exit 0 / 72 组 / 165 passed / 0 failed / 0 error**；格式矩阵门 exit 0（F15 行同期更新，计数不变）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**未声称**：F15 未达 complete；该出口**只读**、不触发任何作业；未跑 GitHub Actions；回滚＝revert `4df2158`|
|R15p|本轮（`7463702`；Python 面，**Rust 无改动**）|—|**把"容器→文件"关系接到宿主投影面**：`scripts/host/journey_panel.py` 新增**容器成员区块**，由 `/api/members?source_id=` 驱动，读 Core 的 members 出口，并**沿用同一诚实规则**——**Core 不可达时整块不显示任何成员**；**未知容器按其 404 如实报告**而**不是画一张空表**（"容器里没有东西"与"我们不认识这个容器"必须不一样）；**缺少标识符 → 具名 400**；Core 自己的 note **透传到页面**，因此那两句限制（"只含已导入成员"、"readable 只表示存在 transform"）**无法被视图丢掉**|IMPLEMENTED_PENDING_AUDIT（R15/F15 仍 partial，见矩阵；R10 的宿主投影面扩展一项）|**真实收据**（`7463702`）：**真实 Core 冒烟**（`scripts/probes/r10_host_panel_smoke.py` **exit 0**）——`page 200`、`health 200`、`state 200`（`learner.event_count=1`、`machine=not_recorded`）、**`members` 未知源 → 404 且 `members=None`、`core.reachable=true`**、**缺标识符 → 400**；pytest `tests/test_host_journey_panel.py` **14 passed**（新增 5 项：成员可读性透传、未知容器**不得**呈现为空容器、不可达时**无成员列表**、`source_id` 的 `/` 与空格被正确转义、页面含容器区块与 readable 说明），与 `test_core_client.py` 合计 **25 passed**；架构／语言边界／路径／证据索引／格式矩阵**五门 exit 0**；conventions 仍仅 2 条冻结包文件行|**精确边界**：本轮**未改任何 Rust**（`git status -- crates/ Cargo.toml Cargo.lock` 为空），故**第 78 轮的全 workspace 收据（72 组／165 passed／0 failed）仍然适用**，本轮不重复跑 cargo 也不声称新的 workspace 结果。**未声称**：F15 未达 complete；面板只读、不触发作业；未跑 GitHub Actions；回滚＝revert `7463702`|
|R15q|本轮（`738314d`）|—|**F10/F11 由"仅保管"升为"部分"：媒体探测路由（只读头部，绝不声称已解码）**：音视频是二进制，故获得自己的能力 **`media.probe`**（独立 worker、engine profile `python-worker-media`/`0.1.0`、媒体类型 `audio/wav`＋`video/mp4`、独立宣告身份）。worker：**WAV** 走 stdlib `wave` 读头（声道、采样率、位宽、压缩、帧数及由此得出的时长）；**MP4/MOV** **逐 box 走查 ISO base media 文件**而**不解释任何 payload**（major/compatible brand、box 类型集合、movie timescale 与声明时长、**每条 track 的 handler/timescale/duration**）；`declared_by` **明写"这些是文件头的声明"**。**截断的 box 即停止走查并如实报告**（不猜）。投影为**逐行事实清单**＋行锚点；回执明说**没有任何样本被解码**，因此**不存在波形/转写/时间戳/关键帧/采样覆盖**事实|IN_PROGRESS（F10/F11 仍 partial：无转写与片段/词时间；无字幕/画面事件/采样覆盖；**MP3/M4A/FLAC/MKV/WebM 按名拒绝**（本仓库无 reader），故这几类在"部分覆盖"的组内仍属仅保管）|**真实收据**（`738314d`）：pytest `tests/test_worker_media_route.py` **7 passed**（真实 WAV：`channels==2`、`sample_rate_hz==8000`、`sample_width_bytes==2`、`frame_count==4000`、`duration_seconds==0.5`；**逐 box 构造的合法 MP4**：brand `isom`、兼容 brand 含 `avc1`、`timescale==1000`、`duration_units==2500`、`duration_seconds==2.5`、**两条 track handler 为 `vide`/`soun`**、事实行 `track-1\tvide\t1000\t2500`；零帧 WAV → `frame_count==0` 且 losses 点名；`song.mp3` → **具名拒绝**；回执含"no sample, frame or track was decoded"且**无 accuracy 字样**；传输层声明 `media.probe` 且**文本路由不接受 audio/wav 与 video/mp4**；stdout 恰一行信封）；Rust `media_job_end_to_end.rs` **3 个 e2e 全绿**（媒体名选中媒体路由而文本路由拒绝；`.mp3/.m4a/.flac/.mkv/.webm` **具名拒绝**；真实 WAV 作业落库且投影含 `sample_rate_hz 8000`、**不含 transcript**；坏媒体容器作业 `failed` 且 `job_outputs` **无 text 产物**）；全 workspace **exit 0 / 73 组 / 168 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**F10、F11 行同期改判，计数更新为 0 complete / 9 partial / 7 custody_only**，解析到 **6 条 core 路由／5 条 worker 路由**）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**未声称**：F10/F11 未达 complete；**头部事实是文件的声明而非对内容的测量**；未跑 GitHub Actions；回滚＝revert `738314d`|
|R15r|本轮（`400c9a9`）|—|**重大发现＋修复：仓库里早就存在 Office/Canvas/字幕 worker，但没有任何路由指向它们**。`worker_office.py`（DOCX 走 stdlib zip+XML、PPTX 走 python-pptx、XLSX 走 openpyxl、PDF 走 PyMuPDF，**引擎在本机全部可用**，自 2026-09-05 切片起就在库里且被跟踪）以及 `worker_canvas.py`、`worker_subtitles.py` **都没有路由**——能力**在作业契约上不可达**，而我的格式矩阵却把这些组记为 `custody_only`（**低估**）。**修法：不再写第二个同能力 worker**（AGENTS.md 禁止重复实现），而是**把既有 worker 接进契约**：侧车模式（声明自身身份 `python-worker-office-ndjson` 与能力 `office.structure`）＋传输层路由＋Core 路由＋engine profile＋docx/pptx/xlsx 媒体类型映射|IN_PROGRESS（F07/F08/F09 由 custody_only 升为 partial：worker 自身的结构作为 `params.worker_structure` **事实**保留，路由契约承载规范行锚点；**F12 的字幕/画布同样存在既有 worker 却未接线，且我第 70/75 轮在文本路由里的"事实"实现与之重复——如实记下，留待后续切片接线并处理重复**）|**真实收据**（`400c9a9`）：Rust `office_job_end_to_end.rs` **3 个 e2e 全绿**（office 名选中 office 路由而**文本路由拒绝** `.docx`；**真实 DOCX 作业端到端**：段落文本 `6371` 与 `second paragraph` **真的落库**、回执含 `python-worker-office`；坏包 → 作业 `failed` 且 **无 text 产物**；`doc/ppt/xls/rtf` 旧二进制格式**具名拒绝**）；pytest `tests/workers/test_bulk_office.py` ＋统一契约 ＋text 侧车 **25 passed + 47 subtests**；全 workspace **exit 0 / 74 组 / 171 passed / 0 failed / 0 error**；矩阵门 exit 0（**解析到 7 条 core 路由／6 条 worker 路由**，计数更新为 **0 complete / 12 partial / 4 custody_only**）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮修掉两个真实缺陷**：① worker 按**扩展名**分派，而 staging 输入是**内容寻址（无扩展名）** → 传输层按媒体类型**生成路由局部带后缀视图**（与 OCR 路由同法，抽成 `_materialise_view`），原输入不动、哈希仍单独校验；② 既有 worker **早于统一契约**（引擎形状锚点、无 coverage）→ 传输层做**边界适配** `_as_route_contract`：由投影文本推导规范行锚点与 covered/total/coverage，并把 worker 自身结构存为 `params.worker_structure` 事实，**loss 行写明"谁推导了什么"**。③ 矩阵校验器**自身**也有真缺陷：`ROUTES` 解析漏掉**多行条目**（office 路由因此不可见），且声明规则未反映"一 kind、一 capability、多接受媒体类型"的真实语义——两处均已修，其测试同步更新。**未声称**：F07/F08/F09 未达 complete（worker 结构仍是事实而非寻址层级）；`worker_canvas`/`worker_subtitles` **仍未接线**；未跑 GitHub Actions；回滚＝revert `400c9a9`|
|R15s|本轮（`0c7651b`）|—|**接完最后两个"存在但不可达"的 worker，并消除重复**：`canvas.structure`（读 `.canvas`，返回**带 node id 的节点锚点**＋边＋文件/链接引用）与 `subtitles.structure`（读 SRT/VTT，返回**带 `offset_ms`/`duration_ms` 的 cue 锚点**）接入契约（各自能力＋媒体类型＋engine profile＋worker 身份＋传输路由）。**给字幕独立媒体类型**（`application/x-subrip`、`text/vtt`）：`.srt` **不再被声明为纯文本**，因此**专门 worker 成为字幕文件的唯一答案**——我第 75 轮在文本路由里加的 cue 检测**降级为"仅当文档被声明为 text/plain 时适用"**，重复由此**收敛而非留下两个答案**。传输层适配器**泛化**：`suffix_by_media`（按扩展名分派的 worker）与 `contract_adapter`（早于统一契约的 worker）；并**新增保留 worker 的额外顶层字段**|IN_PROGRESS（F12 仍 partial：节点/cue 锚点是**事实**而非投影中的寻址层级；canvas 几何不投影；文本路由的 JSON 读法仍在但不再是结构路径）|**真实收据**（`0c7651b`）：Rust `canvas_subtitles_job_end_to_end.rs` **4 个 e2e 全绿**（canvas 名选中 canvas 路由；**真实 canvas 作业**：投影含文本节点原文、回执含 `python-worker-canvas`、**worker 自身结构含 node id `n-1` 与 `text_node`**、**边 `e-1` 被保留**；**真实 SRT 作业**：投影含两条 cue 文本、回执含 `python-worker-subtitles`、**`cue-` 锚点带 `offset_ms`/`duration_ms`**；坏字幕 → 作业 `failed`；`.srt` **不再**能被 text 路由接受、`.txt` **不再**能被字幕路由接受）；pytest `test_text_format_facts.py` ＋统一契约 ＋text 侧车 **39 passed + 47 subtests**；全 workspace **exit 0 / 75 组 / 175 passed / 0 failed / 0 error**；格式矩阵门 exit 0（**解析到 9 条 core 路由／8 条 worker 路由**，计数仍 0 complete / 12 partial / 4 custody_only）；架构／路径／语言边界／证据索引门 exit 0；conventions 仍仅 2 条冻结包文件行|**本轮又抓到一个"适配器引入的静默丢失"**：我第一版 `_as_route_contract` 只保留 worker 的 `structure`，**把 canvas 的 `edges`/`references` 丢掉了**——测试断言 `e-1` 时暴露；现改为**保留任何额外顶层字段**（`params.worker_output`＋说明），因为丢掉它们的是适配器而不是引擎。另**两处过期断言被正确修正而非删除**：第 62 轮"canvas 无摄入路由"（现为 `canvas.structure`）、第 75 轮"`.srt` 解析为 text/plain"。**未声称**：F12 未达 complete（无存储的图关系/时间锚）；未跑 GitHub Actions；回滚＝revert `0c7651b`|
|R15t|本轮（`b6656af`）|—|**"存在但不可达"第三次出现 → 本轮既接线又建闸门**：又发现一族未接线 worker——`web/worker_html.py`（标题＋正文块＋链接表）、`web/worker_webpage.py`（**抓 URL**）、`media/worker_transcribe.py`（ASR，**需模型**）、`media/worker_video.py`（抽帧，**需契约无法承载的参数**）、`vision/worker_caption.py`（**本地模型调用**）。**接线 HTML**：新增 `html.structure`（`text/html`＋`application/xhtml+xml`）＋engine profile＋worker 身份＋传输路由，同一适配器保留其块锚点与链接表为事实。F02/F03 **由 custody_only 升为 partial**（现 **0 complete / 14 partial / 2 custody-only**）。**新增闸门** `scripts/check_worker_reachability.py`：每个能力 worker 必须**被传输路由点名**或**在 `WORKER-REACHABILITY.json` 中带理由豁免**；**拒绝**"已接线却仍被豁免"的过期条目与"豁免了不存在的 worker"，并记录 transcribe/video **为何刻意不接线**|IN_PROGRESS（F02/F03 仍 partial：**无抓取**——只有已保存快照可读；捕获时间不能从文件得知（属导入来源的 `received_at`）；F03 无浏览器故**无分页/滚动覆盖**）|**真实收据**（`b6656af`）：Rust `html_job_end_to_end.rs` **3 个 e2e 全绿**（html 名选中 html 路由而**文本路由拒绝 `text/html`**；**真实快照作业**：正文含 `6371`、**脚本内容不出现在投影中**、回执含 `python-worker-html`、worker 块锚点 `block-1` 与**链接 `example.invalid/source` 均保留**；无标记的文件 → **拒绝**（不报空成功）；URL 名 → **无抓取路由，具名拒绝**）；pytest `tests/test_worker_reachability.py` **7 passed**（含负例：无路由且无理由、豁免无理由、**对已接线 worker 的过期豁免**、豁免了不存在的 worker）；矩阵校验器 12 passed；pytest 合计 **53 passed**；全 workspace **exit 0 / 76 组 / 178 passed / 0 failed / 0 error**；**七门**（约定／架构／语言边界／路径／证据索引／worker 可达性／格式矩阵）中六门 exit 0，conventions 仍仅 2 条冻结包文件行|**本轮两个缺陷都由测试而非阅读发现**：① 一个改成 `.html` 名字的 ZIP **产生"成功但正文为空"的作业** → worker 现**拒绝毫无标记的文件**（**有标记但确实空白**的页面仍如实报告"无文本"）；② 矩阵测试里**硬编码的覆盖率数字第三次过期** → 改为**从被测矩阵本身推导期望**（这类测试不该钉住当前覆盖率）。**未声称**：F02/F03 未达 complete；`worker_webpage`/`worker_caption`/`worker_transcribe`/`worker_video` **刻意未接线**（各自理由已入档）；未跑 GitHub Actions；回滚＝revert `b6656af`|
|R15u|本轮（`9fa191c`）|—|**F04 的"图示描述"以"标明候选"的方式落地**：接线 `vision/worker_caption.py` 为 **`image.caption`**（独立能力＋engine profile＋worker 身份＋传输路由）。**对"模型调用"的诚实处理**：① **先探测模型**——缺模型是**具名失败并列出已安装模型**（不是原始 HTTP 报错，更不是空描述）；② 描述虽作为 `text` 存入，但**回执明确标注它是模型输出**（`params.authority`："this text is a model description, not extracted content: it is a candidate"）并记录**模型 id 与 prompt 版本**；③ **质量不在此处度量**——需要人类真值配对而不是再叫一次模型。**kind 选路由**：同一个 `.png` 在 `image` 下走 OCR、在 `caption` 下走描述|IN_PROGRESS（F04 仍 partial：阅读顺序仍只是行序；描述质量需人类真值配对，本仓库不做质量结论）|**真实收据**（`9fa191c`）：**本机活体**——`qwen2.5vl:7b` 用 **2.92 s** 描述了合成图：*"The image contains the text 'measured 6371 km' in the top left corner…"*，回执标为 **candidate**、`prompt_version=archeaxis.vnext/v1 2026-09-05`、**无 accuracy 字样**；Rust `caption_job_end_to_end.rs` **2 个 e2e 全绿**（`caption` kind → `image.caption` 而 `image` kind → `image.ocr`；caption 路由拒绝非图片名；**真实 PNG 作业**要么成功且描述非空＋回执含 `python-worker-caption`／`qwen2.5vl`／"model output"／"candidate"／`prompt_version`，要么**具名失败且不留 text 产物**——两条路都不允许"空成功"）；pytest `tests/test_worker_caption_route.py` **5 passed**（缺模型 → 具名失败并列出已安装模型；端点不可达 → 具名失败；不支持的扩展名 → 拒绝；路由声明 5 种图片媒体类型与后缀映射；描述被标注为模型输出且回执无 accuracy）；全 workspace **exit 0 / 77 组 / 180 passed / 0 failed / 0 error**；**七门**（含新增的 worker 可达性门：**12 个能力 worker、10 已接线、2 带理由豁免**）中六门 exit 0，conventions 仍仅 2 条冻结包文件行|**可达性记录已更新**：caption 从"未接线"移入**已接线**，并保留**自该发现以来接线的 5 个 worker 的历史列表**（office／canvas／subtitles／html／caption），使"当时的豁免"仍可追溯。**本轮一次操作事故**：提交信息中的引号把 `git add` 命令弄坏（`fatal: '/' is outside repository`）导致**首次提交未发生**——已改用 `git commit -F <文件>` 重做并核对 HEAD 后才推送。**未声称**：F04 未达 complete；模型描述**不是**对图像的"事实"，只是候选；未跑 GitHub Actions；回滚＝revert `9fa191c`|
|R11e|本轮（`—`；探针与测试，**无产品代码变更**）|—|**未见例评测落地**：新增 `scripts/probes/r11_unseen_evaluation.py` —— **自撰 5 篇留出语料**，使用前**先证明"未见"**（5 个特征值 token 与每篇首句在**全部受跟踪文件文本**中都不存在，**检查而非假定**），随后跑完整闭环：导入 → 建候选 → **人类 `accepted`** → 每篇一个**闭式查询** → **人类 `modified` 纠正一处测量值** → 复问同样 5 问 → **纠正后才第一次问**的新查询。**两次测量 bug 都出在我这边，已在探针里修掉，不当作产品发现上报**：① 早先按"返回结果里的来源名"判定命中，而 `/api/v1/search` 的响应**根本没有来源名**（items 是 `{knowledge_id,status,head,active}`）；② 改按"特征 token 出现在 head 里"，而 **head 是 `substr(body,1,60)`**（`crates/archeaxis-domain/src/search.rs`），特征值常在 60 字之外。**现口径**：命中＝**返回的某个 head 是期望文档正文的前缀**；查询词**按整 token 比对（非子串）**，与 FTS5 的 MATCH 对齐|PROBE_COMPLETE（**本探针不给检索质量签 PASS**；`ok` 只表示闭环跑通且纠正已生效）|**真实收据**（本轮）：探针 **exit 0**，收据 `.project-local/runs/r11-unseen-receipt.json`——基线**5 问 3 中**、纠正后**3 中**、**2 个未中全部由查询词自身解释**（`dating`、`distance` 不是该文档的 token）、**无未解释未中**；**纠正确实进了索引**：同一个"词词都在"的诊断查询，head 由 `…retreated 640 metres…` 变为 `…retreated 705 metres…`，**被取代的旧值不再返回**；纠正后才问的那问**返回空**，因为 `revised` 不是该文档的 token。pytest `tests/test_unseen_evaluation.py` **10 passed**（钉住两条测量规则：head 前缀身份、token 级比对；并含"每个 token 都在却仍未中 ⇒ 未解释"的正例与"词缺 ⇒ 已解释"的负例）|**对 owner 的发现（本仓库不代为定性）**：检索是**字面 token 的 AND、无词干还原**，故**词形变化**的查询会把语料里明明存在的文档问空——是否可接受属 owner 决策，本记录只做测量。**未声称**：排名质量（只测"是否返回"，从不测"排第几"）、规模（5 篇文档测的是这条闭环，不是检索引擎）、真实 MCP 客户端（仍用受控 HTTP 客户端代表）；回滚＝revert 本行两个新文件|
|R15v|本轮（`—`；**产品代码 + 闸门**）|—|**全量 pytest 自 R00 基线以来首次重跑 ⇒ 2 failed，两个都是真缺陷**：① `tests/workers/test_bulk_html.py::test_non_utf8_snapshot_uses_replace_and_marks_encoding` —— **我上一轮加的"无标记即拒绝"判据过宽**：`_TAG_MARKER` 是**块级白名单**，于是 `<b>hi</b>` 这种**纯内联片段被判成"不是 HTML 文档"**；而同一次排查发现**内联片段没有块边界**，`_Extractor` **从未在 `close()` flush 残留文本** —— 即一篇真有文字的文档**投影为空**（正是我上一轮想堵的那类静默空正文）。**修**：标记判据改为**任意标签**；`close()` **flush 内联文本**；拒绝判据同时改为**只看"标记是否存在"**。② `tests/test_path_conventions.py::test_the_record_matches_the_real_tracked_tree` —— 该测试把**活树**计数钉死（1741/1672/96.04/69），而记录测的是 commit `3362af9`（当时确实 1741），活树已长到 **1775**；**根因其实在闸门**：`check_path_conventions.check()` **只对账 unowned 集合**，`owned`／`tracked_paths`／`coverage_percent` **永不复核** ⇒ **记录里公开的数字没有任何机制再测一遍**|IMPLEMENTED_PENDING_AUDIT（R15 整体仍 IN_PROGRESS）|**真实收据**（本轮）：全量 pytest 复跑 **2537 passed / 7 skipped / 0 failed / 124 subtests passed**（同一命令在修前是 2 failed）；`scripts/ci/cargo_test.bat -p archeaxis-application --test html_job_end_to_end --offline` **exit 0 / 3 passed**（**含"不是 HTML 必须失败而不是空成功"** —— 放宽标记后这条仍然成立）；`-p archeaxis-application --offline` **exit 0 / 19 组 / 71 passed / 0 failed**；pytest `test_bulk_html.py` + `test_path_conventions.py` **26 passed**；`tests/test_unseen_evaluation.py` **10 passed**；`check_path_conventions.py` **exit 0**——"1706/1775 tracked paths owned (96.11%), 69 unowned and all recorded"；`check_evidence_index.py` **exit 0**（受跟踪证据指针 **59 → 63**）；`check_taskpack_integrity.py` exit 0；conventions 仍仅 2 条冻结包文件行|**闸门修法**：新增 `tracked_paths_at(commit)`（`git ls-tree`），**在记录自称的 commit 上重算它的全部总数**，使"1741 条中有 69 条无归属"变成**后来者可复核的断言**；活树增长改为**报告** `drift_since_measurement`（不再要求它为零），而 **unowned 集合仍按活树对账**（那些路径根本没有写入车道，变动不得无声）。**随后把记录本身刷到本轮的 `240485a`**（同一时点语义）：`1777` 条 / `1708` 有归属 / **96.12%** / 69 无归属，`finding` 与 `measured_note` 一并改写；**旧测量（`3362af9` 时 69/1741）留在台账里**而不是留在记录里 —— **记录是某一次 commit 的测量，台账才是测量的历史**。测试相应改为**从记录与测量读数**而不是钉数字（这正是第三次同类教训：矩阵覆盖率、证据指针数、路径计数）。**新测试当场又抓到一个**：改成 `close()` flush 后，"纯文本文件"会因产生块而**绕过拒绝**——故拒绝判据必须只看标记存在。**未声称**：记录的总数是**它自己那个 commit 的**测量，不是当前树的；回滚＝revert 本行提交|
|R14prep2|本轮（`—`；整理与交接，**无产品代码变更**）|—|**收尾整理（对 R14/R16 门与下一会话的准备）**：① **HANDOFF 补齐第 71–84 轮**——此前交接文档只写到第 70 轮，71–84（启动器第二片、宿主面板与成员视图、媒体探测、邮件、容器路由与成员 API、PDF→OCR 自动串接、OCR 复核区、Office/Canvas/字幕/HTML/描述接线、可达性门）**只存在于台账与 STATE**，现已写成一份**统一叙述**（含"三个能力域总览表"、复发三次的"存在但不可达"发现、矩阵现状、以及第 16–21 条新pitfalls）；② **证据索引刷新**：`R14-EVIDENCE-INDEX.json` 的 R08/R14/R15 三行补入**新门与新 worker**（可达性门与记录、Office/HTML/描述 worker、受跟踪 cargo 运行器），受跟踪证据指针由 **49 增至 59**；③ 全部门与校验复跑|IMPLEMENTED_PENDING_AUDIT（整理片；**R14/R16 仍为 TODO，永不本仓库自签**）|**真实收据**（本轮）：`check_evidence_index.py` **exit 0**——"17 slices, **59 tracked evidence pointers**, 3 receipts, every status agrees with STATE.json, no independent gate is self-signed and every slice states its limits"；pytest `tests/test_evidence_index.py` **14 passed**；`check_taskpack_integrity.py` exit 0（21 冻结文件一致、**72 行台账**、17 切片）；架构／语言边界／路径／可达性／格式矩阵**五门 exit 0**；conventions 仍仅 2 条冻结包文件行|**本轮未改任何产品代码**（仅两份文档），因此**第 84 轮的全 workspace 收据（77 组／180 passed／0 failed）仍然适用**，本轮不重复跑 cargo 也不声称新的 workspace 结果。**未声称**：F14/F16 仍为仅保管（本机无 LibreOffice 等转换器）；R12 未删除任何内容；R10 挂载宿主界面仍属 owner 决策；R11 仍欠真实 MCP 客户端与未见例评测；回滚＝revert 本行两个文档提交|

## R00 记录（2026-09-11）

- 基线锁定：分支 `codex/full-loop-0906`，HEAD `985a219`（晚于包审计基线 `cbe253b`），
  与 origin 同步，工作区仅 `.zcode/` 未跟踪（GLM 会话本地）。`main` 仍 `4ca46ea`。
- 继承缺陷重查（不假定、不回退旧实现）：R02 已修（`288991a` REVISION-01）；
  R05 已修（`288991a` EVENT-01）；R03 本会话已修（`985a219` ARCHIVE-01）；
  R06 部分（`288991a` VERIFY-01，实跑受 HTTPS/凭据 gated）。
- 入口登记：`AGENTS.md` §6、`docs/DOCUMENTATION_AUTHORITY_INDEX.md`、
  `docs/CONFIGURATION_AUTHORITY_INDEX.md`、`DECISION_SUPERSESSION_LEDGER.yaml`
  SUP-018、intake `workspace/intake/2026-09-10-next-taskpack-0910.md`。
- 乱码检查：扫描 `docs/`、`workspace/`、`config/` 及根级 md/yaml 共 451 个文件，
  活动入口文档无乱码；唯一 U+FFFD 出现在
  `docs/authority/legacy/T17-semantic-review-samples-2026-09-05.json` 的
  `head_snippet`（二进制 PNG 头片段），属合法数据而非文档损坏，历史材料不改。
- 待办：R01/R02/R03… 的验收仍以各切片独立证据为准；Q00/Q01 只由独立 GPT 判定。
