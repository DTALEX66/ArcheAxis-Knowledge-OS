# Session handoff — 2026-09-11/12 (0910 pack install, R00–R04)

Audience: the next executor or the independent auditor. Authority is
`EXECUTION.md` + `STATE.json` in this directory; this file is a summary only.

## Rounds 2–4 (added after the first handoff)

- **R02 evidence** (`7d4bfc1`): domain suite exit 0 / 9 groups, incl.
  `accept_with_new_body_is_rejected_and_body_stays_immutable`,
  `accept_commits_status_and_one_event_atomically`,
  `failed_review_leaves_no_partial_state`, `modified_creates_traceable_successor`
  (receipt `.project-local/runs/r02-domain.log`).
- **R03 completed** (`de02ffa`): git history showed schema v3 shipped **four**
  export layouts (10/11/12/13 tables) while my earlier ARCHIVE-01 accepted only
  11/13 — genuine 10- and 12-table v3 archives would have been rejected. Fixed by
  exact table-set matching over `V3_LAYOUTS`; v1 (7 tables, no `workspace_meta`)
  stays unsupported. Evidence is now the real thing: **four non-empty v3 archives
  exported by the historical implementation** in a scratch worktree (store+archive
  from `968c479`, `a2dbef5`, `60b355a`, `3d65609`), committed under
  `crates/archeaxis-archive/tests/fixtures/v3-{ten,eleven,twelve,thirteen}-tables`
  with `PROVENANCE.txt`, and restored by
  `genuine_v3_era_archives_restore_with_content` (upgrade to v4, 2 knowledge rows
  with body bytes preserved, 1 source row). Archive suite 7/7 ok; workspace 52
  groups ok (`r03-archive-v2.log`, `r03-full-workspace.log`).
- **Environment pitfall (important)**: a scratch git worktree placed **under the
  repo** inherits the main tree's `.cargo/config.toml` `target-dir`, which mixed
  v3 and v4 artifacts in one build directory and produced 6 *false* test failures
  (the archive tests linked a v3 store). The worktree wrapper now pins
  `CARGO_TARGET_DIR`; if a future slice uses a worktree under `.project-local`,
  do the same or keep the worktree outside the repo.
- **R04 landed** (`d38b6b2`): real fail-open hole fixed — `Launch::from_stdin`
  accepted any `actor` and the middleware mapped every non-`machine` value to
  human, so an unknown role silently received human authority. Unknown actors are
  now rejected at bootstrap (no session, no workspace) and the mapping is
  exhaustive. Real-process regression
  `unknown_launch_actor_is_rejected_and_never_grants_human_authority`
  (`alien`/`Human`/`owner`/empty/trailing-space rejected; `human` still serves
  200). API suite exit 0 (launch_auth 6 tests), workspace 52 groups ok
  (`r04-api.log`, `r04-full-workspace.log`). Remaining, not claimed: real
  host-signed and host-held machine/human credential scope.
- Housekeeping `8d85f87`: my own fixtures normalised (final LF, LF-only
  PROVENANCE) so the conventions gate is back to **only** the two documented
  frozen-package issues (`TASKS.json`, `MANIFEST.json`).

## State at handoff

- Branch `codex/full-loop-0906`; entry plan = this pack (SUP-018).
- Slices: **R00, R01, R02, R03, R04 IMPLEMENTED_PENDING_AUDIT**;
  R05–R16 TODO in `STATE.json`.
- Earlier in the same working session (before this pack was installed):
  `985a219` ARCHIVE-01 (R03 scope) and `cbe253b` machine correction loop (R11
  scope: proposal -> review -> new version -> re-call, restart-consistent).

## What was done

1. **Package install + verification.** ZIP extracted single-level here with its
   `reference-r2/` snapshot; `python -X utf8 verify_package.py` exit 0
   (`PASS: hashes, 17 task dependencies, all 23 original tasks retained`).
   `-X utf8` is required on this GBK-locale machine; the frozen files were not
   modified. As-shipped `EXECUTION.md`/`STATE.json` bytes preserved at
   `.project-local/runs/taskpack-0910-shipped/`.
2. **R00 registration.** SUP-018 appended; `AGENTS.md` §6,
   `DOCUMENTATION_AUTHORITY_INDEX.md`, `CONFIGURATION_AUTHORITY_INDEX.md`,
   intake note; baseline review `R00-BASELINE-REVIEW.md` maps all 17 slices and
   re-checks the inherited defects at HEAD `985a219` (R02/R05 fixed in
   `288991a`, R03 fixed in `985a219`, R06 partial and environment-gated).
   Baselines then: cargo 52 groups exit 0; Python 2347 passed / 7 skipped.
3. **R01 capacity baseline.** `R01-CAPACITY-BASELINE.md`: census 23.423 GiB /
   130,120 files (opaque roots excluded), `.hermes` 42.853 GiB fingerprinted
   separately with zero growth, D: free 231.15 GiB, and a real bare
   `cargo build -p archeaxis-api` proving root `target/` is byte-identical
   before/after while output lands in `.project-local/build/cargo`.

## Open items and known deviations

- Two frozen package files (`TASKS.json`, `MANIFEST.json`) lack a final LF, so the
  conventions gate always reports these two lines; not patched because that would
  break the package's own manifest hashes (see `PACKAGE-STATUS.md`).
- R03 acceptance still wants a **non-empty v3 sample produced by the old
  implementation** (current evidence reconstructs the historical shapes).
- R05 FSRS wiring, R04 unknown-launch-actor rejection, R06 live cloud
  cross-check (needs approved credentials -> `BLOCKED_EXTERNAL` otherwise),
  R02/R03 acceptance evidence, then R07–R16 in order.

## Reproduce the verified state

```
python -X utf8 docs/authority/taskpack-0910-r3/verify_package.py   # exit 0 at install
cmd /c call .project-local\runs\cargo-full-workspace.bat           # cargo workspace, 52 groups
pwsh -NoProfile -File scripts/ci/run_tests.ps1 --full              # 2347 passed / 7 skipped
cmd /c call .project-local\runs\cargo-build-api.bat                # root target/ unchanged
```

Boundaries in force: no E: access, no `.hermes` content access or new writes, no
paid APIs, no publishing, Q00/Q01 only by independent GPT audit, no fabricated
passes (use `BLOCKED_RESOURCE` / `NOT_TESTED` honestly).
