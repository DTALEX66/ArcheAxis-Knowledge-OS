# 给 CODEX / DSH / HERMES 的统一启动指令

项目仅限 DTALEX66/ArcheAxis-Knowledge-OS（ArcheAxis Knowledge｜星环知识平台）。DSH 是执行工具，不是产品。先读 TASKPACK.md、TASKS.json、BLUEPRINT-COVERAGE.json、SOURCES.json，再读目标仓库的真实 AGENTS 和当前权威入口。

当前为 R2，增强附件已合并在同一队列中。阅读 ENHANCEMENT-REVIEW.md 与 ENHANCEMENT-COVERAGE.json；SOURCE-ENHANCEMENT.md 仅作原文追溯，采用逐项裁决后的语义。不得重新按附件 P0/P1 创建第二份活动任务图，也不得跳过个人知识、机器实际调用或恢复当前跨项目联动。

1. 先运行 `python verify_package.py` 检查本包。该检查仅证明文件、任务依赖和需求映射一致，不证明产品完成。
2. 查询当前远端和本地状态。本包核查起点是 4ca46eaf94c486dadcf200aac6b41cd968b1ce6e。若已有新提交，比较变化并吸收；不 reset 到旧 SHA、不覆盖已有工作、不把本包日期当最新代码证据。
3. 执行 X00：通过现有合法入口记录本轮方向及任务映射，保留旧来源与历史状态。本包不伪造签名、授权、审批或新权限。不以 GPT/DeepSeek 品牌决定授权。
4. 执行所有就绪的 X 任务至 X11，得到 M0 候选后立即提供可启动产物和真实证据；继续 X12/X13 至 M1 候选。X14 本地清理在 X01/X02 后即可提前执行，必须交实际释放量和剩余目录。全程不需要中途切模型。Q00/Q01 留给最后独立 GPT 审计；F01–F06 保留冻结。
5. 选定的执行模型可以完整推进同一实现队列。不要同时分别跑两份完整队列。若用户主动并行，分配互不重叠任务与 worktree，共享契约/锁文件/迁移/CI 串行集成。
6. 块级任务须落成小切片。优先沿用仓库已有任务/schema/检查器；缺少 grant 只按真实机制处理，禁止自己伪造。常规已授权工作继续；缺凭据、目标机器或特殊权限的步骤明确 BLOCKED_RESOURCE，并执行无依赖的其他就绪任务。
7. 优先利用旧代码、样本、公开格式和现成产品。自研只允许必要连接与实际缺口修复；Tutor、OCR、复习、3D/仿真、通用 Agent 引擎不得默认重写。大宫殿等需求保留，不要删除后称范围收敛。
8. vNext 主库只有 Rust 写。Python/前端/Agent 通过协议。旧库先一致快照后只读导出，不双写，不把上游学习记录误当可删缓存。
9. 新开发运行材料用受控 `.project-local`；旧 `.hermes` 只读保全。不要访问 E 盘、擅自改工具安装位置、覆写 HOME/CODEX_HOME、静默使用其他项目或 C 盘短根。
10. 每个完成声明附受测源码 SHA/tree、实际命令及退出码、必要环境/模型版本、样本与产物哈希、成功/失败路径和回滚。自身正常提交不使历史证据失效；新集成候选必须重新验证受影响行为。

输出候选状态而非自行宣布最终验收：IMPLEMENTED_PENDING_AUDIT。分别说明 M0、M1、原蓝图的完成情况；未来功能冻结不能计入完成率。不得凭测试数量、README 或静态页面宣称人机闭环可用。

CODEX+GPT 和 DSH+DeepSeek 使用完全相同的上述指令。不再使用旧分包中“DP 只能等 GPT 发信封/只输出建议”与“双方互不依赖”的矛盾组合；涉及仓库保护机制仍须真实授权，不能通过改文件关闭机制来规避。
