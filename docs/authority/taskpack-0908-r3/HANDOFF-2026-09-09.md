# R3 Handoff — 2026-09-09 (session: taskpack install + X00 + X01 increment)

Single live entry: `docs/authority/taskpack-0908-r3/EXECUTION.md`. This file
is a session handoff summary, not an authority record.

## What happened this session

1. **R3 taskpack installed and registered.**
   `ARCHEAXIS-FOLLOWUP-R3-2026-09-08.zip` (29 files) extracted to
   `docs/authority/taskpack-0908-r3/` (single level). `verify_package.py`
   PASS: 23 tasks / 29 features / 26 scenarios / 16 capabilities / 16 format
   groups / 11 enhancements / 14 gates / 12 governance slices. Implementation
   baseline `cbe253b` == HEAD at registration; branch `codex/full-loop-0906`;
   `main` untouched at `4ca46ea`.
2. **X00 done (IMPLEMENTED_PENDING_AUDIT).** Commit `1191a77`: package +
   R3 ledger (`EXECUTION.md`) + SUP-017 in `DECISION_SUPERSESSION_LEDGER.yaml`
   + AGENTS.md §6 active-plan switch + intake note
   `workspace/intake/2026-09-08-followup-r3-taskpack.md`. Commit `1f1b44c`:
   `docs/DOCUMENTATION_AUTHORITY_INDEX.md` and
   `docs/CONFIGURATION_AUTHORITY_INDEX.md` entry points aligned to R3.
   Commit `1969005`: X00 evidence recorded in the R3 ledger. All entry
   points now resolve to AAK-FOLLOWUP-20260908-R3; R2 (0907) stays as
   inherited-task-text source and historical receipts; D01-D16 remain
   SUP-012..016.
3. **X01 r3 increment done (IMPLEMENTED_PENDING_AUDIT).** Commit `65b28f9`:
   `.cargo/config.toml` redirects default bare cargo output to
   `.project-local/build/cargo`. Real-write path diff (PATH-01): root
   `target/` fingerprint byte-identical before/after bare
   `cargo build -p archeaxis-api` (6,151,810,038 bytes / 19,949 files /
   newest 2026-09-08T08:03:23 — zero growth); `.hermes/` untouched (newest
   2026-09-06); artifact landed at
   `.project-local/build/cargo/debug/archeaxis-api.exe`;
   `scripts/ci/run_tests.sh tests/runtime-paths` 10 passed with run root
   `.project-local/runs/be268a2d33/e0f49b6f3ed0`.

## Current branch state

- `codex/full-loop-0906` at `65b28f9`, clean tree (except untracked
  `.zcode/`, session-local).
- Local Python 3.13.14 venv: `.venv/Scripts/python.exe` (bare `python` is
  not on Git Bash PATH).
- Working Rust toolchain: MSVC wrapper `.project-local/runs/cargo-full-workspace.bat`
  (`vcvars64` + external `D:\All projects\OS External Configuration\toolchains\rust`);
  the default `~/.rustup` gnu toolchain cannot build `libsqlite3-sys` /
  `windows-sys` (no gcc/dlltool) — do not treat that as a code failure.

## Wave A state and next steps

- X00: done pending audit. X01: r3 increment done pending audit (remaining
  X01 scope: packaging-side path diff in CI, browser short-socket exceptions
  unchanged from R2). X02 (donor table, GOV03) and X14 early slice (GOV04)
  not started.
- Wave order: A (X00/X01/X02 + X14 early) -> B (X04/X05 + X07/X08 defect
  slices: REVISION-01, ARCHIVE-01, VERIFY-01, EVENT-01) -> C (X06/X07/X10)
  -> D (X03/X08/X09) -> E (X10/X11/Q00) -> F (X12/X13/X14/Q01).
- Inherited C-board (from R2): C01/C02 DONE; C03-C10 PARTIAL. R3 binds the
  four P1 fixes to X04/X05/X07/X08.

## Boundaries in force

No E: access; no new `.hermes` dev artifacts; no paid APIs; no publishing;
R2 originals preserved; FAIL/BLOCKED_RESOURCE/NOT_TESTED never reported as
PASS; Q00/Q01 verdicts only from independent GPT audit.

## Rollback

Each slice is one commit: revert `65b28f9` (X01 seal), `1191a77`+`1f1b44c`+
`1969005` (X00 registration). Package files are inert data.

## Addendum (same day, second batch): X02 donor table + X14 early census

- `8384bbd` X02 minimal donor table (`X02-DONOR-TABLE-2026-09-09.md`,
  GOV03/REUSE-01): five current-loop donor rows with blob SHAs @ `d7d392d`
  and regression evidence — 27 passed / 0 failed (deeptutor 4, due_queue 6,
  longterm adapters 8, bulk workers 9). 1,246-row manifest untouched.
- X14 early slice (GOV04/CLEAN-01, read-only): same-tool census receipt
  `.project-local/runs/x14-early-inventory-20260909.json` — observed
  20.29 GiB / 115,231 files (not a repo total; `.hermes` opaque at
  46.5 GB, zero growth since 09-06); D: free 234.0 GiB. Regenerable-cache
  candidates recorded for future per-path authorization (root `target/`
  5.729 GiB now growth-sealed; `.project-local/build` 9.413 GiB; cache
  1.148 GiB; `.venv` 0.858 GiB). No deletions performed.
- Wave A is now complete in its early form; next session starts Wave B
  (X04/X05 + X07/X08 defect slices: REVISION-01, ARCHIVE-01, VERIFY-01,
  EVENT-01).

## Addendum 3 (2026-09-09/10): Wave B P1 contract fixes

- `288991a` single-commit P1 batch (cargo exit 0, 52 groups; Python 2346
  passed / 7 skipped; classifier fixed by `.cargo/**` risk registration in
  the same batch's follow-up commit):
  - REVISION-01: review actions can never rewrite body bytes; only
    `modified` creates a new superseding revision with the corrected body.
  - EVENT-01: learning events REQUIRE persistent `client_event_id`; key
    bound to item + payload hash; same retry returns original receipt;
    same key different payload/item = conflict; missing key = rejected.
    Store schema v4 (guarded migration; v2 archive wire still restorable).
  - VERIFY-01: x07 probe now requires object+unit+number evidence quote
    AND explicit model 支持; anything else is NOT support (offline unit
    checks green; live run needs HTTPS+ollama).
- ARCHIVE-01 remains PARTIAL (full restore-time layout discrimination ->
  X05/X10). Ledger: `EXECUTION.md` "Wave B slices landed".
- Next: ARCHIVE-01 restore discrimination (X05/X10), then C07/C08 carryover
  into X06/X11, UI wiring for supersede (X08), FSRS trajectory (X05).

## Addendum 4 (2026-09-11): ARCHIVE-01 landed (Wave B close-out)

- ARCHIVE-01 is no longer PARTIAL. `crates/archeaxis-archive/src/lib.rs` now
  resolves the archive layout from the manifest table set instead of the
  version number alone:
  - v2 = the original 8 tables (unchanged behaviour);
  - v3 = either the historical 11-table shape (`V3_TABLES_ELEVEN`: no
    supersedes table, no learning-event key table) or the 13-table shape that
    already existed before the v4 column migration;
  - any other v3 set = `unknown v3 archive layout`, rejected with no database
    published; a current-version archive missing an exported table is likewise
    rejected (`current table set`).
  - Row-level upgrade path: older rows may omit additive nullable columns
    (v4 added `payload_hash`, `event_id`, `streak_after`, `next_review_days`
    to `learning_event_keys`); those are filled NULL. An unknown extra column
    is rejected (`unknown column in archive row`) - nothing is dropped or
    guessed.
- Evidence: 5 new unit tests in the archive crate `version_tests` module,
  archive crate 7/7 test groups ok (`.project-local/runs/archive-245.log`),
  full workspace `cargo test --workspace --offline` exit 0 / 52 groups
  (`.project-local/runs/cargo-archive-01-full.log`). Fixtures reconstruct the
  two historical v3 shapes from the schema history; no real archived v3 file
  exists on this machine (stated, not substituted). Rollback: revert the
  slice commit (one file).
- Also fixed in the same session: `workspace/intake/2026-09-08-followup-r3-taskpack.md`
  line 28 carried trailing whitespace, which failed
  `scripts/check_repository_conventions.py`; stripped, gate now passes.
- Next: C07/C08 carryover into X06/X11 (single-pipeline worker registry +
  Windows candidate entry), UI wiring for supersede (X08), FSRS trajectory
  (X05) - unchanged priority from Addendum 3.
