# ArcheAxis Knowledge v0.6.8 执行总结

日期：2026-08-23（Asia/Shanghai）

## 结果

v0.6.8 已完成实现、验证、合并、正式发布与独立资产读回：
<https://github.com/DTALEX66/ArcheAxis-Knowledge-OS/releases/tag/v0.6.8>。

本轮把任务包中的“规划/存在”进一步推进为可执行闭环：

- Library 通过内容摘要读取不可变原件，不接受调用方路径；响应启用
  `no-store`、`nosniff` 与 sandbox。
- Evidence 可对待审 Research 执行真实审批并刷新状态。
- AI Assets 支持带主体、范围、版本、证据来源和幂等键的审批，并支持独立弃用；
  两类动作均生成 append-only receipt。
- Workspace、Activity Dock、Inspector、Learning 与 Settings 接入真实 BFF/API；
  Settings 可初始化四库并创建、验证命名备份。
- 修复 Tauri origin 的 CORS/launch-token 处理、Workspace 路径前缀和 Learning
  绕过统一 runtime client 的问题。

## 验证

- 本地 Python 全量：1977 passed、7 skipped。
- 前端：25 passed；TypeScript 与 Vite production build PASS。
- Rust/Tauri：14 passed；Rust 格式门禁 PASS。
- 真实 Chromium：以 Tauri origin、真实 launch token 和真实后端依次加载六空间，
  控制台 0 error / 0 warning。
- PR head CI：`32606263256` success。
- main merge SHA CI：`32607097436` success；精确绑定
  `93e58a3b2c537dd348903dd2296933e0cfb5a503`。
- Release：`32607789507` success；Setup/Green/Portable 生命周期 PASS。
- 独立读回：9/9 provider digest 与下载 SHA-256 一致，8/8 发布载荷由
  `SHA256SUMS.txt` 精确覆盖，identity schema 为 3.0.0。

## 仍属后续增强

整体产品完成度仍为 `PARTIAL`，因为任务包中的完整首次启动向导、Recovery
日志/安全模式、四类账本统一静态白名单、完整 Tier A 格式矩阵、键盘/读屏/
高 DPI/reduced-motion 矩阵，以及同一干净机上的端到端 Golden Journey 仍需
独立证据。它们不影响 v0.6.8 已验证发布事实，也不应被本次发布反向宣称完成。

## 性能结论

CI 已做到风险分类、并行门禁、依赖缓存和 exact-SHA 候选上传；但 Release 仍在
注入正式 identity 后重复执行一次约 11 分钟的 NSIS 构建。下一轮应把 identity
设计为可在不重编译主二进制的情况下封装，或将可复用构建层与最终签章层分离，
同时保留 tag/main/exact-SHA、安装生命周期和下载读回门禁。
