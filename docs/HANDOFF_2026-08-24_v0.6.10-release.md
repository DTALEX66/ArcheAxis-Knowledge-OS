# ArcheAxis Knowledge v0.6.10 发布交接

日期：2026-08-24（Asia/Shanghai）

## 权威坐标

- Release：<https://github.com/DTALEX66/ArcheAxis-Knowledge-OS/releases/tag/v0.6.10>
- tag/main 发布提交：`3428a65cf6445918365f76b114cc11630d9640bb`
- 源码树：`828ffe3039b65d1b2fccf9c9348233342818cea1`
- main exact-SHA CI：`32665051446`，success
- Release run：`32665840172`，success
- 发布时间：`2026-08-23T21:02:53Z`
- 公开资产：9 项，public、non-draft、non-prerelease

机器可读收据：[`../reports/release/v0.6.10/release-evidence.json`](../reports/release/v0.6.10/release-evidence.json)。

## 本版交付与运维规则

1. Activity Dock 可读取真实 Job/Delivery/Receipt 聚合，并对可操作任务调用 dispatch/retry；没有后端取消合同的控件保持明确不可用，不伪造取消成功。
2. 前端保持薄投影：日常 `frontend/src/**` 使用 Vite HMR，后端接口/实现可独立修复；普通风险选择门禁不构建 Windows 安装包。
3. main CI 为 latest-SHA-only：新 main SHA 自动取消被替代的 run；Tauri/NSIS build 子进程有 20 分钟硬期限。Full Qualification 仍保留在版本、锁、Tauri 配置和打包输入等高风险变更或显式 `[full-qualification]`。
4. Release 只提升同 SHA 的 CI candidate；不得以 Release 重建安装包来替代候选验证。

## 后续任务（不等于本版已完成）

1. P1：在一台干净 Windows 机器执行完整 Golden Journey：四库初始化→六空间→备份/恢复→升级→卸载保留→重装读回，并产出单一 receipt。
2. P1：完成 Tier A 格式矩阵及键盘、焦点、读屏、高 DPI、分页、reduced-motion 可访问性矩阵。
3. P2：补齐 Activity Inspector 的冲突/rights/完整版本历史，以及真实取消合同；没有后端语义前不得把禁用控件改为成功操作。
4. P2：继续治理 AXR-060-302/303/503 的跨库 append-only 白名单、来源独立性/rights 合同、外链下载和卸载保全运行时证据。

## 工作树边界

发布提交和 tag 均是干净源码树。主工作区保留用户自己的未提交 `tests/fixtures/readability_article.html` 改动；它未被 reset、restore、clean、暂存或纳入任何 v0.6.10 提交。后续工作先确认该文件归属，再决定处理方式。
