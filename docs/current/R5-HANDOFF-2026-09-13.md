# R5 额度收口交接 · 2026-09-13

本交接是继续工作的入口，不是产品完成证书。首次59%额度时曾收口，之后用户以“开始”恢复推进；续跑最新实查通用额度剩余52%。
项目为星环知识平台 ArcheAxis Knowledge：`D:/All projects/ArcheAxis-Knowledge-OS`。
本文件随本次checkpoint提交；精确提交SHA由Git及上传回读确定，不在自身正文循环自签。

## 当前权威和Git

先读AGENTS.md、docs/CONFIGURATION_AUTHORITY_INDEX.md，再读docs/authority/taskpack-0912-r5/的EXECUTOR-START、TASKPACK、TASKS和REMAINING-WORK。
当前R5包173个active文件完整性校验通过；原23任务和新增专项仍按原依赖/验收。进度在R5-STATE.json、R5-EXECUTION.md，不改冻结正文。
分支codex/full-loop-0906；首次checkpoint测试基于`c06b234ca335b9cbb2c1fde270851e2390c36b89`加当时工作树修改。续跑源与证据分别见R5-EXECUTION.md，禁止拼接为同一SHA全门禁通过。
上传前fetch后origin同分支仍为该基线，左右差0/0；origin/main及本地main为`1e9813ea2bd49f47d334ba6717c78d3e9feda6ce`。
实际origin为同仓库SSH地址，与公开文档HTTPS不同；本次不修改配置，上传使用明确HTTPS仓库地址。
尚未merge、PR、release、签名或覆盖Green。早期上传曾被Never审批策略拒绝；用户调整权限后已正常推送，旧“上传未执行”结论不再适用。
续跑已上传23fe970：锁定CI Python环境、worker摆脱隐式开发导入路径、默认桌面Cargo路径，以及Windows进程退出观察修正。
[该SHA的vnext-ci](https://github.com/DTALEX66/ArcheAxis-Knowledge-OS/actions/runs/34730748251)已实查success，含Rust/桌面检查；不代表全部主CI或GUI验收。
随后项目侧新增DeepTutor笔记custody适配及真实上游服务资格脚本：合成原问题/回答/元数据/时间完整读回，独立进程重启、损坏索引重建保持原笔记哈希。上游Markdown本身省略问题和元数据，不能独立作完整备份。
下一项仍是实际DeepTutor Web默认入口、Core桥接与学习活动；不要继续重复治理盘点。笔记服务通过不替代GUI、附件/会话回收或模型资格。
未知docs/current/SESSION-RESTART-2026-09-12.md保持未跟踪，未上传；最新本地HEAD及dirty paths须重新实查。

## 本次实现范围

- 规范化：补目录归属、R5权威指针；本轮tracked与191个当时新增文件规范检查通过。冻结原包JSON以精确路径+SHA256保护，未修改原字节。
- 止增：dev.py统一项目临时/缓存/日志/环境；NuGet四类路径回读在项目内；主checkout Cargo复用build/cargo，linked worktree独立。新增预算UNKNOWN退出3，超限退出2。
- 路径/取消：旧probe、批量清单、候选包输出和校验边界修正，自有进程取消清理和截图失败清理有定向测试。
- 桌面/Core：v2双令牌、人/机器角色隔离，严格worker profile，开发启动入口；正式Avalonia仍未挂载默认学习工作台。
- 学习：FSRS完整精度、step/last_review；新/learning/reviews由Core原子追加完整状态及幂等收据，跨重启恢复。旧/events保留原协议。
- Scheduler：默认20秒deadline、有界IO、直接Python子进程超时退出；损坏/超时保留明确未调度收据。任意后代/外部pipe持有者回收仍有边界。

## 实测及限制

可上传的结构化收据见R5-CHECKPOINT-EVIDENCE.json；完整本地运行证据在.project-local/runs/be268a2d33/，不纳入Git。

|检查|结果|
|---|---|
|R5包完整性|PASS，173文件；不是产品测试|
|Python受影响汇总|204 passed、11 subtests passed|
|Rust工作区|cargo test --frozen --offline --workspace exit0；已有unused警告|
|OCR实际分支|1+3项通过，--show-output无skipping；仅合成样本，不是资料精度|
|C# supervisor|9 PASS，含真实C#→Core→Python、角色隔离、取消|
|Avalonia构建|0 warning/0 error；无可见GUI验收|
|C# vocabulary|29共享wire cases通过|
|正式程序--smoke|无界面握手/停止自有Core通过|
|workers-vnext|PASS；text/canvas/subtitles/html/docx实际样本，其余Office仅probe|
|Python主集初跑|2744 passed、2 failed、7 skipped、135 subtests passed，203.92秒|
|两失败修正|架构原调用行号19→20；审计区分并验证Git commit/tree/blob及历史上游pin；55相关测试通过，Ruff通过|
|修正后主集重跑|NOT RUN；不能将初跑改称PASS|
|旧0910包verify_package|FAIL于历史EXECUTION.md哈希，旧文件未改；不同于本次R5包校验|
|wheel/完整阶段门禁/exact-SHA CI|未完成；上传不等于门禁或发布通过|

## 清理真实状态

实际删除量0，未宣称释放空间。旧`target/`历史逻辑大小6151810038 bytes（约5.73 GiB），精确路径删除确认仍未收到。
`.project-local/build/be268a2d33`现在含通过验证的.NET产物，已从旧候选中移除；`.project-local/build/cargo`为当前Rust产物，保留。
`.zcode/`未读取、未证明无用、未删除，仅加入.gitignore。未知原有`docs/current/SESSION-RESTART-2026-09-12.md`保留且不纳入提交。
不要沿用旧容量盘点推算当前体积，目录有新增测试/构建写入；实际清理前重查路径、链接、占用和大小。

## 剩余任务（仍非DONE）

|ID|状态|具体缺口|
|---|---|---|
|X00|PARTIAL_NEEDS_WORK|R5包安装和当前入口已登记；HL01来源/资格导入行为仍未测，X00父任务保持PARTIAL。|
|X01|PARTIAL_NEEDS_WORK|开发路径、CI路由、预算诊断、6个旧探针、启动probe隔离及真实截图OCR已验证；浏览器失败/取消清理补回归。dev.py捕获取消时清理自有进程树，真实Windows父/孙进程及无关进程保留已测；强杀运行器、孤儿进程与实际构建/打包取消仍未闭合。批量导入清单按来源/Core地址区分。正常启动持久数据库不自动迁移；全入口差分、保留轮转未闭合，Core地址复用仍需工作空间身份。 NuGet实际locals回读已修正HTTP/plugins缓存和scratch全部进入项目；不代表旧用户缓存已清理。 主checkout的dev.py与bare Cargo已复用build/cargo；linked worktree保留独立目录。40项定向测试及真实离线metadata回读通过；未删除历史构建。 显式容量预算UNKNOWN现exit3，已知超限exit2；20项CLI/边界测试通过。分类阈值和轮转仍未定稿。|
|X02|PARTIAL_NEEDS_WORK|1793 tracked路径归属已补齐；可见范围盘点0错误。M0已登记12组/19源版本及215个V15候选定位，逐行标明审查范围；调度供体拒绝行为已修复验证。其余M0全文语义核查、1246项M1吸收和真实默认Windows调用仍未闭合。|
|X03|PARTIAL_NEEDS_WORK|CoreSupervisor双身份和MainWindow的严格worker profile接线已实现，正式Avalonia工程构建及同加载器C#→Core→Python实跑通过；开发入口自动准备隔离配置，未运行可见窗口。窗口主体仍占位；DeepTutor默认入口挂载/产物回收未闭合，不修改外置上游。|
|X04|PARTIAL_NEEDS_WORK|Core/C#已实现显式v2 stdin双令牌/单写者，Rust14项与C#真实Core/Python链路通过；独立静态审查建议已处理，非Q00/Q01。DeepTutor跨进程机器adapter仍未接线。learning-feedback/v1 additionalProperties=false不能承载新增曝光/原回答版本语义；学习与结构寻址契约尚待定稿。|
|X05|PARTIAL_NEEDS_WORK|已有CAS/作业/恢复；新增完整FSRS状态使用现有追加事件表，与事件键原子提交、失败回滚及重开已测，未新增schema。其余R5学习字段、来源命名空间、修正追加与归档仍未闭合。|
|X06|PARTIAL_NEEDS_WORK|已有Core-worker路由和PDF/OCR/HTML接线；动态网页实时抓取、结构定位、复杂样本质量与真实模型资格未闭合。|
|X07|PARTIAL_NEEDS_WORK|修订/撤销及公开检查probe已有；云端逐主张核查需配置及预算；HL02关系/阅读深度和HL10研究更正影响未测。|
|X08|PARTIAL_NEEDS_WORK|新/learning/reviews已将完整FSRS状态与幂等收据原子追加，真实认证Core/Python及同DB进程重启继续复习通过；无效worker结果保留未调度收据。旧/events保留旧阶梯，默认GUI尚未切换；曝光/题目修正/样例/交错/讲回/负担/互动未完成。 本轮补齐FSRS默认20秒deadline和有界IO，8适配测试、2失败事务测试、13真实Core/API回归通过；直接Windows子进程退出已测，任意后代和默认GUI仍未闭合。|
|X09|PARTIAL_NEEDS_WORK|真实MCP SDK客户端和未见例评测已实现，不能回退成完全没有；原任务全部方法类型/纠错后真实客户端资格及R5研究更正链未测。|
|X10|PARTIAL_NEEDS_WORK|非空合成迁移已有；附件/关系为损失记录、时间未保留。新学习字段需补兼容，真实用户库资格需精确授权。|
|X11|PARTIAL_NEEDS_WORK|Core候选hash绑定/验证已有；完整worker/宿主依赖、Windows非开发机GUI闭环、重启恢复、安装签名卸载未闭合。|
|Q00|BLOCKED|独立M0审计尚未执行；需要同候选完整G01-G14原始证据，执行者不得自签。|
|X12|PARTIAL_NEEDS_WORK|当前格式记录0 complete/14 partial/2 custody-only；目录续跑驱动已有但非UI入口。全格式链、真实Vault客户端往返、三个主题未验收。|
|X13|PARTIAL_NEEDS_WORK|历史69归属缺口已关闭并保留旧SHA测量；全仓语义吸收、默认入口、完整依赖安装体验仍未闭合。|
|X14|PARTIAL_NEEDS_WORK|旧target仍待精确路径批准，历史逻辑5.73GiB不是可释放量；worktree build已含本轮验证产物，移出当前候选并保留。已加.zcode Git忽略，未读或删除私有内容。尚无删除和空间回收证据；逐路径批准后重新核对占用、链接、大小并测删除后卷空闲。|
|Q01|BLOCKED|受Q00、X12、X13、X14前置约束；当前不具备独立M1资格。|
|F01|DEFERRED_RETAINED|原文与依赖保留，未激活。|
|F02|DEFERRED_RETAINED|原文与依赖保留，未激活。|
|F03|DEFERRED_RETAINED|原文与依赖保留，未激活。|
|F04|DEFERRED_RETAINED|原文与依赖保留，未激活。|
|F05|DEFERRED_RETAINED|原文与依赖保留，未激活。|
|F06|DEFERRED_RETAINED|原文与依赖保留，未激活。|

## 下一次执行

用户恢复推进后先核Git和本次提交/上传结果，不重复安装R5或重做已验证切片。首先处理上传对应CI具体失败（如存在）和修正后主集验证；wheel需干净、项目内隔离构建，不覆盖已有构建目录。
产品下一关键项是X03：按原包要求挂载上游DeepTutor现成学习工作台；不能把Avalonia欢迎页或Core投影面板当最终入口，也不另造学习工作台替代原目标。
DeepTutor公开运行时在共享工具链deeptutor/1.5.17；既有launcher可能写用户缓存/绑定0.0.0.0、读其他客户端凭据的路由，必须经项目侧受控host/明确接口边界设计；本轮未启动它、未读凭据、未改上游。
X04学习反馈/结构地址契约仍需具体定稿；Q00/Q01须独立审计，不得执行者自签。清理仅在精确路径确认后执行。

## 工具与边界

Python `.venv/Scripts/python.exe` 3.13.14可用；不沿用失效venv，不在共享环境装依赖。
所有运行通过`scripts/runtime/dev.py`，输出只在项目ignored `.project-local/`；.hermes为只读旧材料，E盘禁访。
Rust既有离线runner：`.project-local/runs/be268a2d33/r5-rust-runtime-binding/artifacts/rust-runner.py`；调用前PYTHONPATH设仓库根，--frozen --offline，复用build/cargo。
.NET：`D:/All projects/OS External Configuration/10-toolchains/dotnet/dotnet.exe` 10.0.400；--no-restore，本机既有依赖。
禁止真实资料库/Green迁移激活、共享库修改、发布签名、全局配置、凭据/私有代理状态访问。不要修改HOME/CODEX_HOME。
回滚按本次提交中各切片显式路径撤销代码，保留学习事件、原件、冻结包和所有未知改动；禁止reset/clean覆盖工作区。
