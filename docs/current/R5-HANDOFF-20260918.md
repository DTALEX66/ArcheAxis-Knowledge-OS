# ArcheAxis R5 当前交接（2026-09-18）

> **HISTORICAL SNAPSHOT（2026-09-18 记录）**：本文件中的 HEAD/`origin/main` SHA 与「领先 N 个提交」计数**不再随当前状态更新**，旧 SHA 一律保留不改写。它们记录当时那次（失败的）远端写入尝试，不是当前事实。当前远端事实见文末「CURRENT REMOTE SNAPSHOT（2026-09-18 读回）」。

## 历史快照：当前版本（HISTORICAL SNAPSHOT）

- 本地分支：`main`
- 当前 HEAD：`873320472a4854bafd6462456cdf4d2606af54dc`
- 本地 `main` 相对 `origin/main`：领先 38，落后 0。远端写入本轮未完成：直接 push 被执行策略拒绝，GitHub Data API 对当前 SHA 仍返回 `403 Resource not accessible by integration`。
- 工作树的已跟踪内容无修改。未跟踪的 `docs/history/**`、`docs/current/SESSION-RESTART-2026-09-12.md` 等历史/私有材料保持原样，未读取、未加入提交。
- 已复核并删除本地过时分支 `codex/branch-disposition-20260918` 与 `codex/append-only-sql-allowlist`：前者只有引用旧远端状态的文档，后者的 SQL 白名单修复已由主线提交 `779e71f1` 吸收；两者均无需要保留的工作树。
- 后续复核确认 `codex/evidence-anchor-pagination`、`codex/tier-a-format-matrix`、`fast/mastery-signal` 的有效代码/测试已在主线由后续提交吸收，删除这三个无工作树的重复分支；仍有独特代码或独立工作树的分支保留。
- 再次核对后删除 `fast/phase7-runtime`、`fast/sleep-task-adapter`、`work/tp13-facades`；其实现已分别由主线的运行时收敛提交和 facades/sleep 后续提交吸收。`work/tp12-facades` 等仍含独特旧系统边界，暂保留。
- `feat/a0-gates` 与 `feat/a0-truth-baseline` 也已核对为早期 A0/发布基线的重复分支：当前 CI、发布清单、浏览器烟测和工作区接口均已有后续主线实现，且无独立工作树，已删除。

## 本轮已验证

- Python 全门禁：`scripts/ci/run_tests.ps1 -- --full`，`2840 passed, 10 skipped, 13 warnings`，exit 0。
- Rust 全工作区：`cargo test --workspace --all-targets --quiet`，exit 0；日志在 `.project-local/runs/cargo-workspace-test-20260918.log`。
- Canvas Core 端到端：节点/边写入 Rust Core 的 `canvas_projection_*` 表，相关测试通过。
- Canvas 几何事实：`worker_canvas.py` 保留合法 `x/y/width/height`，不伪造缺失字段；颜色/端口仍明确为未投影。
- Green 候选：`ArcheAxis.Knowledge.Green-vclean-archive70226a42-x64` 及完整 runtime 候选均通过逐文件清单与 runtime/workers 门禁；既有 `D:\All projects\ArcheAxis.Knowledge.Green-x64` 未修改。
- 候选 smoke、Core 启动依赖、桌面 supervisor 生命周期均在项目内路径完成；这些不等于签名安装器或干净机器 GUI 验收。

## 当前 R5 状态

- X00–X14：`PARTIAL_NEEDS_WORK`，不是 DONE。
- Q00/R14：`FAIL/BLOCKED`；在 clean 候选上 G01、G13、G14 仅限静态/候选资格范围通过，G02–G12 仍缺同一候选的真实多格式、学习重启、迁移恢复、GUI 首用和运行目录证据。
- Q01/R16：`BLOCKED`，不能由执行者收据自签通过。
- R10：DeepTutor 宿主可用 sidecar/合同回归，但真实宿主内挂载与首用重启读回未闭合。
- R12：保留用户数据和未知私有状态；旧 ACL-deny 残留需逐路径管理员处理，未擅自改 ACL 或强删。
- R13：完整 Green 候选已可重建，仍缺代码签名、安装器/卸载器生命周期、干净 Windows 机器首用及真实 Green 安装验收。
- R15：`0 complete / 14 partial / 2 custody-only`。Obsidian 正文关系索引、目标解析、Canvas 几何与 Rust Core 投影已有增量，但附件哈希、frontmatter/锚点/别名完整语义、真实 Vault 往返及全格式质量矩阵仍缺。

## 下一步顺序

1. 在当前 clean candidate 上补 Q00 G02–G12 的同候选原始证据；完成后才可重跑独立 Q01。
2. 取得受控 Windows 打包资源后，完成 R13 安装/卸载/签名/干净机验收；未取得前保持 BLOCKED。
3. 按 R15 逐格式和 Vault/附件差分补证据，不把结构测试提升为真实质量通过。
4. 远端接口恢复后，推送本地 19 个提交，并以远端精确 SHA 回读确认双端一致；在此之前不得声称已上传。

## 证据边界

所有生成日志、候选包和临时目录均在项目 `.project-local/`。没有访问 E/F 盘，没有读取 `.hermes/.zcode/.codex`、凭据、浏览器数据或其他私有代理状态，没有覆盖既有 Green 或真实资料库。

## 实时读回记录（HISTORICAL SNAPSHOT，2026-09-18）

- 当前 HEAD：`873320472a4854bafd6462456cdf4d2606af54dc`；`origin/main`：`6bc26c213637c4b9423f2c02063e3cd3e692c0e6`；本地领先 38 个提交。
- 对当前 HEAD 重试 GitHub `update_ref`（2026-09-18）仍返回 `403 Resource not accessible by integration`；远端同步保持未完成。

## Continuation correction — 2026-09-18 latest local readback

- Supersedes the numeric snapshot above for the current checkout: local `main` HEAD is `713c7a922299978be052c62969356f30bcd7b0c5`; local `origin/main` remains `6bc26c213637c4b9423f2c02063e3cd3e692c0e6`; ahead count is `104`.
- Since the earlier snapshot, local receipts include full Python gate rerun (`2860 passed, 10 skipped`), R10/R15 real probes, external resource-boundary checks, absorbed-branch cleanup, packaging/WebView2 diagnostics, and a failed Git-data upload attempt with HTTP 403.
- Remote equality is not claimed. Existing untracked history/private files remain untouched.

## CURRENT REMOTE SNAPSHOT（2026-09-18 读回）

> 独立于上方历史快照；只登记读回当时的事实，不硬编码为永久真值。

| 项 | 值 |
|---|---|
| GitHub 仓库 | `DTALEX66/ArcheAxis-Knowledge-OS`（public，默认分支 `main`） |
| `origin/main` | `44bd821da82d9beeacf4e3c6f581c0fd90521ba4` |
| 远端分支 | 18 个（其中 17 个非 `main`） |
| 打开的 PR | 0 |
| 同一 SHA 的 nightly | run `35323175367`：`py-compat (3.11)` 与 `py-compat (3.13)` 通过；`full-suite` **FAILED**（2877 passed / 35 skipped / 5 failed），`browser-smoke`、`windows-runtime` 因 `needs: full-suite` 被跳过 |
| 读回方式 | GitHub REST/`gh` API。本机 `git fetch` 只配置了 SSH 远端，而该 SSH 在当前执行环境被拒（`couldn't create signal pipe`），因此本轮**未做本地 fetch**，只声明 API 可见事实 |

上方「远端写入本轮未完成 / 领先 38 / 领先 104」是当时记录：此后主线已推进到上表 SHA，本地与 `origin/main` 在该 SHA 上一致。这**不**表示历史快照里那次失败的推送被改写成成功——两条记录并存。
