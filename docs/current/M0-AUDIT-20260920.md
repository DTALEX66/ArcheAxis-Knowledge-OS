# M0 并行审计回读（2026-09-20）

## 精确基线

- branch：`main`
- `HEAD` / `origin/main`：`a0b93bd8363747f65be40aa5c97bd6479dc08400`（本记录提交的 first parent）
- `origin/codex/full-loop-0906`：当前本地 ref 不存在，未据此推断远端状态。
- R6 TaskPack SHA-256：`dcc51e922a35d30ca361e9014e040674b62cee644a3ea57aae12ffa6c2949529`
- 当前工作树：已提交修改无 tracked diff；22 个历史/私有未跟踪路径继续保留，未纳入提交。
- 文档中的 current-main SHA 是该记录提交的 first parent；`check_authority_sha_consistency.py` 的 live readback 才是当前记录提交后的精确 HEAD 证明。

## P4 机器闭环审计

| 项目 | 状态 | 结论 |
| --- | --- | --- |
| Machine task 注册/读取 | `PASS`（契约级） | API、持久化和失败 receipt 有本地契约测试。 |
| 失败语义与机器/人类隔离 | `PASS`（契约级） | failed 必须有原因，机器不能写人类 review，receipt 不可重写。 |
| `retest_of` 引用 | `TESTED_LOCAL` | 现在必须指向同一 SQLite 中已存在的 failed machine task。 |
| Accepted/Personal Knowledge 绑定 | `TESTED_LOCAL` | 现在校验 existing active accepted 或 active personal knowledge；`knowledge_id@version` 的版本部分仍是标识文本。 |
| 人工修正与 successor | `PASS`（测试契约级） | successor 激活、旧版本 inactive 和 restart readback 有测试。 |
| 真实 error→correction→retest→restart | `TESTED_LOCAL_PARTIAL` | 合法 launch identity 后，真实 Core 进程已完成 human seed → machine failed task → human correction → machine retest → cold restart readback；仍缺真实模型/用户任务与非合成错误证据。 |
| 当前 Rust 运行证据 | `TESTED_LOCAL_PARTIAL` | 共享 Rust/MSVC/Windows SDK 环境下，API learning-state、machine-task、machine-correction 合计 `10 passed`；完整 workspace gate 与 rustfmt 仍未执行。 |

下一张最小卡：Avalonia 首次学习 UI 与全状态重启读回；P3 API Assessment → answer → FSRS → restart readback 定向门已通过。随后再做 P4 真实 machine correction/retest 与 P5 full backup/restore。

## A15/A16 独立门禁审计

| 门禁 | 状态 | 主要原因 |
| --- | --- | --- |
| Authority / Repository | `FAIL` | 当前证据记录曾以较旧 subject SHA 生成；工作树含 22 个未跟踪路径，不能判定 clean repository。 |
| Contracts | `PASS`（结构性） | P0–P2 契约已进入当前 SHA；不能推导真实运行通过。 |
| Security | `PARTIAL` | 定向安全/边界测试已通过；独立完整安全套件仍 `NOT_RUN`。 |
| Data / Migration | `BLOCKED` | 只有 fixture/合成迁移，没有真实资料库同旅程。 |
| Learning / Machine | `PARTIAL/BLOCKED` | Core Assessment 已有结构和绑定实现；完整 mastery projection、真实 UI/runtime 和真实机器 retest 仍未闭合。 |
| Local models / Domain packs | `PARTIAL/BLOCKED` | 结构契约和路径索引存在；模型收据未读 weights/content，也未证明 executable、health、VRAM/RAM、latency 或 benchmark；General 仍 `contract_only`，无真实 curriculum/renderer readback。 |
| First-use / Restart | `BLOCKED` | 只有 headless/局部 readback，没有完整 GUI 首用和全状态恢复。 |
| Green / A16 | `BLOCKED` → `NOT_READY` | A15、A02、真实闭环、迁移和 Green rollback 前置未闭合。 |

结论：当前只能保持 `NOT_READY`，不能输出 `LOCAL_GREEN_READY_FOR_OWNER_REVIEW`，更不能输出 `RELEASE_READY`。

本记录只写入项目内当前证据；未读取或处理 `.codex`、`.zcode`、`.hermes`、凭据、E/F 盘、外置共享库、真实资料库或现有 Green runtime。

## 本轮回归修复回读

- `app/workspace/service.py` 保留默认 `convert_file_with_trace` 路径，同时兼容测试和调用方使用的旧 `service.convert_file` 注入 seam；`intake_upload` 与 `ingest_local_file` 不再因替换 seam 失效而误入真实 ASR/FFmpeg。
- `tests/test_workspace_api.py` 增加 conversion receipt 的 `attempted_engines` 回读断言，确保兼容路径仍产生结构化、无路径泄露的 receipt。
- RED：原回归因无效 WAV 被送入真实 ASR/FFmpeg 失败；GREEN：工作区、格式执行与多格式管线相关套件 `44 passed, 3 warnings`，退出码 `0`。
- 修复提交：`029fb76f200acd1b58d95d2a310bb61980835c7c`。这只收敛本地 Python 工作区 seam；不改变外部库、Green、真实资料库或发布状态。

## 后续并行切片回读

- P1 fallback readback：`tests/test_workspace_pipeline_multiformat.py` 新增合成 `primary → passthrough` fallback 的 intake、SQLite `loss_report_json` 与 `/conversion-run` receipt 回读；`19 passed, 3 warnings`，退出码 `0`，提交 `97300cc6c8e6389edc33f5dc32adaec8761e6c58`。
- P2.1 General prerequisite contract：`CourseManifestV1` 现在拒绝未知 prerequisite、自环和多节点 prerequisite 环；其指定基线 `22 passed, 1 warning`，退出码 `0`，提交 `d581be95`。
- 两项均没有访问外置库、Green、真实资料、E/F 或私有目录；A15/A16、Rust/.NET runtime、真实模型/引擎和 Local Green 仍未闭合。
- 合并门禁回读：P2.1、P1 fallback 与既有 workspace API 相关套件合并运行 `72 passed, 3 warnings`，退出码 `0`；警告为 pytest 配置、Starlette/httpx 弃用提示和可选 nltk 缺失。
- P4.1 correction review gate：无 `feedback.reviewed_by_human=true` 的 `correction_applied/correction_reverted` 现在被 schema 拒绝；指定 machine-loop Python 回归 `45 passed, 5 warnings`，退出码 `0`，提交 `b20d87af26b120161bed1dab3ae21da3c64f24f6`。
- P5 backup hardening：manifest 路径 traversal 与 root 外 symlink 在 verify/dry-run/restore 入口 fail-closed，未列旁路文件也被拒绝；backup/migration/export 定向回归分别 `50 passed, 1 warning`、`45 passed, 1 warning`，第二张完整性回归 `46 passed, 3 warnings`，提交 `fc0df3a182abed4feded8eeb0ab25b88bb4bcadb`。
- A14 closed-loop guard：complete receipt 现在拒绝 correction/retest 空 evidence refs；Python 回归 `74 passed, 5 warnings`，退出码 `0`，提交 `274de700c4dc4c02ede5d15fb3938fef5498677f`。真实 correction→retest→restart 仍未证明。

## 任务包级完成审计（当前 exact HEAD）

- A00/A01：可继承的 authority/release-freeze 证据已存在；A01 的 release reopening 仍属于 A16 Owner Gate。
- A02、A04、A06、A11：分别受 resource-root/schema、canonical write/mapping 和 Model library 资源语义阻塞；不能靠新增 Python receipt 猜测解决。
- A03、A05、A07、A08、A09、A10、A12、A13、A14：已有局部/结构性证据，但真实模型或外部引擎、Rust/.NET runtime、真实课程/UI、完整 restart/readback、Green/安装回滚仍未闭合。
- A15：`PASS`，独立审计报告已绑定 exact subject `ffd8ff202cfcbd4e34f40dc3dd7f5d0cde2b9f33`；报告明确未把执行者收据自签为 A15 PASS。
- A16：`BLOCKED`，依赖 P0-P6 真实闭环、资源根、真实 Green、迁移与回滚及 Owner 决策。
- 当前可执行证据已推进到 P3 headless smoke、P4 合成进程闭环、P5 backup/migration/store 定向门；下一张真实执行卡是用 Owner 允许的真实模型/用户任务替换 P4 合成输入，随后再处理 workspace identity Owner 决策。真实 Avalonia 控件首用仍无无 UI 入口，A15/A16 继续独立/Owner Gate。

## 工具链恢复后的运行回读

- Rust：共享 toolchain exact path 存在；domain `cargo check --offline -p archeaxis-domain` 退出 `0`，`assessment`、`learning_state_persistence`、`machine_tasks` 合计 `16 passed`。`cargo fmt` 未执行，因为该 toolchain 未提供 `cargo-fmt/rustfmt`。
- .NET：声明的 external SDK `10.0.400` 完成 `ArcheAxis.Desktop.csproj` build，`0 warnings, 0 errors`；CoreSupervisor apphost 退出 `0`（`stop-race` 为 SKIP），Vocabulary apphost 报告 `29 shared wire cases parsed`。
- API Rust：补充 Windows SDK `LIB/INCLUDE` 后，`learning_state_api` `5 passed`、`machine_correction_loop` `1 passed`、`machine_task_api` `4 passed`，退出码 `0`；此前未注入项目 `.venv` 的失败属于环境配置问题，已用现有 FSRS 解释器复跑通过。
- P4 Rust/Core：machine task API `4`、correction loop `1`、domain machine tasks `7`，合计 `12 passed`；修正十六进制 launch identity 后，真实 Core 进程 correction→retest→cold-restart 探针 8 个步骤全部成功，退出 `0`。仅合成数据，不能替代真实模型/用户任务。
- P5 Rust：backup/restart `6 passed`、migration `7 passed`、store-sqlite `5 passed`，合计 `18 passed`；workspace identity 仍为 Owner blocker。
- Desktop：headless apphost smoke 退出 `0`；桌面 routes/runtime/output/learning 定向回归 `33 passed, 3 warnings`，仅证明 Core handshake 与源契约，不证明真实窗口控件首用。
- P3 独立回读：当前项目没有无交互 Avalonia learning driver；`--smoke` 只完成 Core handshake，不产生学习记录。真实“看到→回答→提交→退出→重启读回”仍是 `NOT_EXECUTED/BLOCKED`，需要项目内 UI 自动化入口或等价 headless learning driver。
- P3 headless driver 增量：`--learning-smoke <dbPath>` 已加入 Desktop，覆盖 synthetic Knowledge → Assessment → answer/review → FSRS projection → cold Core restart/readback，并明确 `mastery_projection.status=projection`、`closed=false`；源契约 `1 passed`，Desktop 定向回归 `39 passed, 3 warnings`，.NET build `0 warnings, 0 errors`。项目内旧 Core 产物对 Assessment 返回 `404`、较新 `cargo-r6-p4` 产物在 review projection 返回体上与当前源码契约不一致，因此真实 journey 仍记为 `BLOCKED/NOT_EXECUTED`，未冒充 runtime PASS。
- P3 候选矩阵回读：`.project-local` 中 `cargo-r6-p4`、`cargo-r6-api-sdk2`、`cargo-r6-api-fix` 的 smoke 都到达 review 但返回体不满足当前 answer/FSRS projection 契约；`rust-msvc` 候选在 Assessment 路由返回 `404`。四个候选均使用独立 SQLite，进程已退出/清理；这进一步确认当前已有二进制不能作为当前源码 runtime 证据。
- P2 General renderer 增量：新增 `app/adapters/courseware_lesson.py`，将已验证的 `CourseManifestV1 + CoursewareArtifactV1`（仅 `general`/`lesson`）确定性投影为本地 Markdown `Projection`，保留 manifest/artifact/source/knowledge/renderer 元数据；renderer、General manifest、courseware、domain pack 和 truth gate 合并 pytest `18 passed, 1 warning`。仍不宣称交互课程完成。
- P0 worker gate 回读：现有 lifecycle 测试覆盖的是 monkeypatch 后的 `FileConverter`，不是 `services/python-workers` 的真实 worker/transport/runner；`WORKER-REACHABILITY.json` 只有路由/豁免静态记录，未绑定 manifest/health/enable-disable/provider-replace 生命周期。当前 worker 回归 `32 passed, 1 warning`、`check_vnext_workers.py` 退出 `0`，但真实 Python worker 生命周期证据仍缺，不能把静态 gate 升格为 P0 PASS。
