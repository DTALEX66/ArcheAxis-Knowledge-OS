# Intake: ARCHEAXIS-NEXT-TASKPACK-2026-09-10 (R3.1 follow-up, 17 slices)

Date: 2026-09-11 (session of the 0910 pack install and R00 registration).
Owner-delivered package: `D:\All projects\ARCHEAXIS-NEXT-TASKPACK-2026-09-10.zip`
(109,438 bytes) plus its definition `ARCHEAXIS-NEXT-TASKPACK-2026-09-10.md`
(12,830 bytes, sha256 prefix `50ab667a85dddf61`; byte-identical to the pack's
`TASKPACK.md`).

Installed single-level at `docs/authority/taskpack-0910-r3/` (24 files
including the `reference-r2/` snapshot of 15 inherited R2 files).
`verify_package.py` exits 0: hashes, 17 task dependencies, all 23 original
tasks retained. It must run in Python UTF-8 mode on this machine
(`python -X utf8 ...`), because `Path.read_text()` defaults to the GBK locale
here and fails on the UTF-8 `TASKS.json`; the frozen package file was not
modified.

What changed in framework direction:

- The 0910 pack becomes the single live plan entry
  (`docs/authority/taskpack-0910-r3/EXECUTION.md`, progress in `STATE.json`);
  recorded as SUP-018 in `DECISION_SUPERSESSION_LEDGER.yaml`.
- AGENTS.md §6, `docs/DOCUMENTATION_AUTHORITY_INDEX.md` and
  `docs/CONFIGURATION_AUTHORITY_INDEX.md` now resolve to that entry.
- R3-0908 stays the source of inherited task text and its own receipts; R2-0907
  and 0906 remain historical; the audit baseline Q00 fail / Q01 not eligible
  carries over. No existing implementation is rolled back.
- `reference-r2/` inside the pack is a read-only historical snapshot, not a
  second active plan.

R00 outcome (read-only baseline review, `R00-BASELINE-REVIEW.md`): HEAD was
`985a219`, later than the pack's audit base `cbe253b`, so the inherited defect
list was re-checked rather than assumed - R02 and R05 are already fixed
(`288991a`), R03 was closed this session (`985a219`), R06 is partial with the
live cloud run environment-gated. Fresh baselines at that HEAD: cargo
workspace exit 0 / 52 groups; Python `run_tests.ps1 --full` 2347 passed /
7 skipped / 0 failed; conventions gate and architecture guard green.

Boundaries respected: no Windows cleanup, no `.hermes` access, no E: access, no
credential use, no fabricated cloud-verification claims, no self-signed audit.
