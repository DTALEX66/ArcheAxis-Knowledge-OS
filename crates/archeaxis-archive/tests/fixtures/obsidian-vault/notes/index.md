---
tags: [vault, sample, roundtrip]
canvas: "[[vault.canvas]]"
---

# Index

This note exists so the round-trip sample has a hub: every other file in the
fixture is reachable from here.

- Read [[atomic]] next.
- The heading anchor points at [[atomic#Why this matters]].
- The block reference resolves to [[atomic#^why-block]].
- Same note, aliased: [[atomic|the atomic note]].
- The diagram is embedded: ![[diagram.png]]
- The canvas is linked as a plain markdown link: [canvas](vault.canvas)
- An external link must never be treated as vault-internal: [example](https://example.invalid/not-in-vault)

> [!note] Callout
> Obsidian callouts are markdown, not a separate file format.

Back to [[index]] so the link graph has a cycle.
