[package]
name = "archeaxis-contracts"
version = "0.1.0"
edition = "2024"
description = "ArcheAxis vNext stable vocabulary (knowledge types, review statuses)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
