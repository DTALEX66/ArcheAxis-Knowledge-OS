# Public example only. Do not put secrets in this file.
#
# AXC-030: this template is a MINIMAL project pointer, not a Codex config.
# Official Codex defaults + WORK-LAB user overlay own sandbox/approval/
# provider/reporting; this project does not vend private copies of those.
# This project is standalone: WORK-LAB is an optional external coordinator.

profile = 'development'

[workspace]
root = '<project-root>'
default_scope = 'current_repository'
protected_drives = ['E:/']

[repository]
remote = 'https://github.com/DTALEX66/ArcheAxis-Knowledge-OS.git'
branch = 'main'

# Optional external coordination (never a runtime prerequisite):
# WORK-LAB stable CLI/registry may observe this repo's .worklab profiles.
