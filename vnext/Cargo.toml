[package]
name = "archeaxis-vnext-core"
version = "0.1.0"
edition = "2024"
description = "ArcheAxis vNext authoritative Rust Core (sole SQLite writer) - Day-0 closed-loop data layer"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled", "backup"] }
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tempfile = "3"
