# OSUI 云端交接记录 · 2026-08-12

## 交付状态

- 云端目标：`DTALEX66/ArcheAxis-Knowledge-OS` 的 `main` 分支，`OSUI/` 子目录。
- 主入口：`archeaxis-knowledge-ui-v2/index.html`。
- 交付性质：可编辑的静态 HTML / CSS / JavaScript 高保真交互原型。
- 数据边界：`Mock Adapter`；所有真实接口为 `UNBOUND`。未上传个人资料、凭据、运行时缓存或 `.hermes` 任务文件。

## 本次主变更

1. 新增 `archeaxis-knowledge-ui-v2/app-v3.js`：统一接管工作台、摄取、资料库、阅读器、证据、Canvas、学习和复习等核心工作流路由。
2. 更新 `liquid-glass.css`：固定六空间、多栏桌面壳层、Inspector、回执坞，以及桌面/紧凑/窄屏重排。
3. 所有 v2 HTML 入口均在既有 Mock Adapter 后加载 `app-v3.js`；保留原 `app.js` 作为合同与回退层。
4. 更新本 README 与生产移交 manifest，使入口和已知限制可追溯。

## 当前工作流覆盖

- F01：资料 → 转换 → Claim → Evidence 的可审查结构与锚点入口。
- F02：知识 → 学习路线 → 专注复习 → 证据揭示的演示流程。
- F03：AI 资产相关页面仍保留治理门禁、EvidenceBundle 与人工批准边界。
- F04：任务中断、原件安全、草稿保留与回执入口仍保留在 Workspace / Jobs & Receipts。

## 验证证据

- `node --check archeaxis-knowledge-ui-v2/app.js`：通过。
- `node --check archeaxis-knowledge-ui-v2/app-v3.js`：通过。
- 16 条语义路由入口存在，且均加载 v3 工作台层。
- 静态检查：无模板占位、无 `scrollIntoView`、无真实 API 调用、六个一级空间完整、未来页面保留蓝图标记。

## 未绑定与限制

- 不连接真实后端、数据库、目录、模型、网络核验或用户数据。
- 不将演示对象、状态或回执声明为真实结果。
- 当前源工作副本未完成 OSUI 目录内的真实截图导出；请在目标运行环境重新导出，并对 1920×1080、1440×900、1280×800、390×844 进行视觉验收。

## 后续绑定顺序

1. 先绑定只读资料/原件与页码锚点读取。
2. 再绑定派生产物、Claim、Evidence 与 EvidenceBundle 的查询合同。
3. 完成原件安全、失败恢复、人工批准和撤销回执。
4. 最后处理学习写入、AI 资产治理、外部授权与真实能力回读。

## 回滚

以本次 Git 提交作为回滚边界。若仅需切回旧 v2 呈现，可从各 v2 HTML 入口移除 `app-v3.js`，原 `app.js` 与既有页面资产均被保留。
