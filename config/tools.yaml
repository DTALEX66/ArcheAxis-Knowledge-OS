# AXC-110: product-internal controlled tool risk registry only.
# NOT global Agent tool permissions. General Planner is DEFERRED; do not
# build an agent platform here. WORK-LAB does not read or override.

tools:
  echo:
    risk: low
  file_read:
    risk: low
  safe_write:
    risk: medium
  code_exec:
    risk: high
    default: dry_run
