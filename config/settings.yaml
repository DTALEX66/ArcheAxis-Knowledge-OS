# Legacy settings shim (AXC-020).
#
# This file used to duplicate config/defaults.yaml in full. Single default
# truth now lives in config/defaults.yaml; per-profile differences live in
# config/profiles/<profile>.yaml; local overrides are ignored files; env
# overrides use ARCHEAXIS_* (legacy COGNITIVE_* accepted for migration).
#
# Load order (shared/config.py): built-in -> defaults.yaml -> this shim
# (kept for backward compatibility) -> profiles/<profile>.yaml -> env.
#
# Keep this mapping EMPTY. Do not reintroduce duplicated default values.
{}
