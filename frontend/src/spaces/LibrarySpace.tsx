import { SpacePlaceholder } from "../components/SpacePlaceholder";

export function LibrarySpace() {
  return (
    <SpacePlaceholder
      title="Library"
      description="原始资料库（Source Archive）与多格式摄取。"
      hint="后续批次：文件导入、自动拆解与转换链。"
    />
  );
}
