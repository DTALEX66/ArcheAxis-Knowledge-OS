import { SpacePlaceholder } from "../components/SpacePlaceholder";

export function WorkspaceSpace() {
  return (
    <SpacePlaceholder
      title="Workspace"
      description="当前工作区、来源与任务状态。"
      hint="后续批次：工作区列表、导入入口与任务队列。"
    />
  );
}
