import { SpacePlaceholder } from "../components/SpacePlaceholder";

export function EvidenceSpace() {
  return (
    <SpacePlaceholder
      title="Evidence"
      description="可信证据与知识账本（Evidence & Knowledge Ledger）。"
      hint="后续批次：证据链、冲突与版本核验。"
    />
  );
}
